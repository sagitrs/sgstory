# docs/readings —— 读数与基线（**在用**，✗ 不是作废稿）

> 本目录放**仍被权威表引用**的读数类文件。它们**不属 `docs/archive/`**：
> `archive/` 的定义是「**已作废稿（禁止回流）**」，而本目录的件**仍是权威表的读数面** ⇒ 两者口径不同（`#1512` S08）。

## 什么时候读这里

| 我要…… | 读 |
|---|---|
| 看节奏 / 相异度基线（生成物，随样本漂移） | `baselines.md` |
| 看 UI 覆盖与未覆盖的判定口径 | `ui-coverage-gaps.md`（盘点面另见 `../ui-inventory.md`） |

## 边界（✗ 别混）

- **权威表**在 `docs/README.md` §二；本目录的件由那张表点名 ⇒ 改动它们等于改权威面 ✓
- **生成物不要手改**：`baselines.md` 是生成物（原 `report:rhythm`，**该脚本已下架** ⇒ 现存内容为冻结快照）；
  若哪天重新生成，须**同笔**更新本目录与权威表 ✓
- **进件条件**：一件**要么**在 `docs/archive/`（作废、禁回流），**要么**在这里（在用、被权威表点名）⇒
  ✗ 不允许"放在 archive 里但被权威表当权威用"（`#1512` S08 修的正是这种自相矛盾）✓
