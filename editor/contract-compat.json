{
	"note": "车道 G 切片 1c（`#215` 报备 `18503697` / `18503987`）：**`N-1` 兼容层登记表** —— 每读一个旧方言号，必须在此**逐条登记**（`reason` ＋ `ticket` ＋ `retireWhen` ✓）。形状**镜像** `editor/escape-hatch.json`（清单外出现即红；条目缺任一项即红；**退出条件成立而条目还在即红** ＝ \"登记腐烂 ⇒ 红\" ✓）。",
	"why_empty": "**今天为空，且这是有意的** ✓（经 `#215` 评论 `18503703` 明批）：`CURRENT ＝ 1` ⇒ `N-1 ＝ 0`，而 **0 号方言不存在** ⇒ **今天没有任何真实消费者** ✓。本表今天交的是**形式**（＋配套的三件哨兵）⇒ 将来那次真的破坏性改方言**只需照表填一行** ✓。",
	"retireWhen_谓词": "本件**只认**一个谓词：`{ \"storiesAtLeast\": <int> }` ⇒ **所有故事的 `contractVersion` 都 ≥ 该数** ✓（即\"仓内已全部升到新面\"✓，与 `editor-pivot.md` §2 的 L1/L2/L3 全绿同向）⇒ 成立而条目还在 ⇒ **红** ✓。⚠️ **仓外**的旧包（浏览器里存的、导出的文件）**不可机检** ✗ ⇒ 那一半靠 `ticket` 的人审锚；「**一个发布周期**」**判不了** ✗ ⇒ 写进 `reason`／`ticket`，**不假装能判**。",
	"entries": []
}
