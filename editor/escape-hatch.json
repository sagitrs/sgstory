{
	"note": "K4 逃生舱登记表（`#762` 车道 C）：契约成员里**含任意逻辑**（分类器 C 桶）的必须在此逐条登记（理由＋票号）。清单外出现即红；已不是 C 桶的登记也红（清单只许收缩，收缩留痕）。B 桶（需声明式扩展）与 D 桶（可下沉引擎）都**不算**逃生舱。",
	"hatches": [],
	"why_empty": "**本表只登记 C 桶**（真逃生舱：含任意逻辑、既不能用声明式 kind 表达、也不是引擎能力）。**当前为空 —— 但这是「收缩后的空」，不是「一直没东西」**：\n· `#777` 修正分类器多站点扫描后，洞窟的 `eventPool`（从声明的 `roads` 折叠出实例清单）**首次被认出来**（此前\"空\"的输入是错的）；\n· `#778` 把它**下沉引擎**（`Game.Combat.roadInstances()` ⇒ 纯派生，声明面 `roads` 仍由故事持有）⇒ C 桶归零；\n· ⇒ 本表随之**收缩回空**（`#780` 洞窟端到端零差那一票）。**收缩留痕在此**（棘轮只许收缩，不许长期挂账）。\n**B/D 的实测清单与数量由门打印**（`node editor/k4.mjs` 的『欠账（B 待补 kind）／欠账（D 待下沉引擎）』两行），本文件**不手写数字**（手写必腐烂）。⇒ 不要读成「登记表短 ⇒ 没欠账」：本表只记**必须靠任意逻辑**的那一类。"
}
