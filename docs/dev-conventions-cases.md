# §17 证伪清单——案例与沿革（`#1080` 从 `dev-conventions.md` 外移）

> 正文每条只留「一句可判定的话 ＋ 出处票号」；案例细节、反例形态、事故记录住本文件（按条号检索）。
> **非必读面**（`docs/README.md` 反向判据）。

## §① 这条断言**能为假**吗

| ① | 这条断言**能为假**吗 | 字面真值／自比自／用定义验证定义（`rebuilt === B`） | 语义反了但恰为真的断言 | `#866` `#871` |

## §② 读数／声明**有没有指名来源**

| ② | 读数／声明**有没有指名来源** | 比的是哪份对象：产物归属 ＋ 构建指纹 ＋ 树/commit；**引用**时还要指到**哪份载体**（body／comment／review 三者互不可见 ✗） | 指对对象但读的是**旧副本** | `#874` |

## §②b 我这句话说的是"**已做**"还是"**打算做**"

| ②b | 我这句话说的是"**已做**"还是"**打算做**" | **先指凭证再定调**：要看"已做" ⇒ 把凭证（commit／state／输出）打出来再说 | 报的是**未来**的对象（时态指错） | 本仓 `#886` |


## §（原行未解析·正文保留原样）

| ②c | 我说"**常驻／已登记**"，它**在哪一段** | `grep -n <条目 id> scripts/test-plan.mjs`（`npm test` 表的 id ✓）—— 找不到 ⇒ 它只活在叙述里 ✗ | 表里有 id 但那段**从没跑过** | 本仓 `#845` |

## §③ 搬代码时**行为被搬歪**了吗

| ③ | 搬代码时**行为被搬歪**了吗 | 自证的**期望值逐字不变** | 期望值本来就写错 | `#877` |

## §④ 读数里**混进它没声称在比的东西**了吗

| ④ | 读数里**混进它没声称在比的东西**了吗 | **确定性**（同环境）＋**换环境对**（换 cwd/TZ ⇒ `diff` 必须为空） | 两个环境都没覆盖的那个因子 | `#875` `#878` |

## §⑤ 删除式改动**检查面覆盖全部消费者**了吗

| ⑤ | 删除式改动**检查面覆盖全部消费者**了吗 | 改完**立刻跑门** ＋ 全仓 grep 该符号 | 动态拼出来的消费者 | `#877` |

## §⑥ 不适用的一格**点名说"本块不涉及"**了吗

| ⑥ | 不适用的一格**点名说"本块不涉及"**了吗 | 交付表逐条标明 ＋ 逐面一行 `info` | 声明本身写错 | `#877` |

## §⑦ 在不该整齐处**出奇地整齐**吗

| ⑦ | 在不该整齐处**出奇地整齐**吗 | 先疑**器具**：每窗首行印出来自证边界 | "乱得可疑"（该一样却不一样） | `#877` |

## §⑧ 判据的结论**有没有被真正消费**

| ⑧ | 判据的结论**有没有被真正消费** | 判据命令**不进管道**／`set -o pipefail`；合并门 fail-closed | 消费端在红时仍继续 | 本仓 `#874` |

## §⑧b **部分消费 ＝ 没消费** ✗（判据核了一部分就往下走）

| ⑧b | **部分消费 ＝ 没消费** ✗（判据核了一部分就往下走） | 合并门把它**全部**判据都消费掉（head ＋ `mergeable` ＋ 检查） | 还没被写进门的那些判据 | 本仓 `#882` |


## §（原行未解析·正文保留原样）

| ⑧c | **部分前置条件 ＝ 没满足** ✗（同一族，用在"动手之前"） | 动手前的条件**逐条**核（还开着吗 ＋ 评审是否已发生 ＋ base 是否还是我读的那份） | 没被列进前置表的那些条件 | 本仓 `#885` |

## §⑨ 改测试文件时**锚点失配**了吗

| ⑨ | 改测试文件时**锚点失配**了吗 | 改完 `node --check` ＋ 打印"**注入确认 N 处**" | 注入生效但没改到被测面 | 本仓 `#874` 诊断 |

## §⑨b 量的时候，**尺子能覆盖全部**吗

| ⑨b | 量的时候，**尺子能覆盖全部**吗 | 枚举式匹配（`(①\|②\|…\|⑫)`）＋**逐条列名**；区间式 `[①-⑫]` 只匹配端点 ⇒ 会报"少了 9 条" | 尺子本身只覆盖了一部分 | 本仓 `#885` |

## §⑩ 合成夹具的键／路径**与被测代码构造方式同源**吗

| ⑩ | 合成夹具的键／路径**与被测代码构造方式同源**吗 | 从被测代码取构造式（不手写） | 同源但语义不同 | 早期迁移 |

## §⑪ 我这个**还原动作的粒度**，和我想撤的改动一样大吗

| ⑪ | 我这个**还原动作的粒度**，和我想撤的改动一样大吗 | 还原用**备份副本**或精确回改（不用整文件级命令）＋ 推前核**改动面清单** | 改动面里看着像噪声的那一行 | 本仓 `#882` 前身 |

## §⑫ 多片**撞同一个共享文件**时，冲突面点清了吗

| ⑫ | 多片**撞同一个共享文件**时，冲突面点清了吗 | 点清冲突面 ＋ 保留双方各一行 ＋ **rebase 后在新 base 重取读数** | 以为是同一处冲突，实为两处 | 本仓 `#880` `#882` |

## §⑬ 我报的"**在哪**"，是**问的那个对象**吗

| ⑬ | 我报的"**在哪**"，是**问的那个对象**吗 | **换对象就重读**：`git show origin/main:<file> \| grep …`／`gh pr view <n> --json state` | 分支上有、main 上没有（"在支上"≠"落 main"） | 本仓 `#885` |

## §⑭ 这个读数的**取值域**里，有没有"**还没算出来**"这一格

| ⑭ | 这个读数的**取值域**里，有没有"**还没算出来**"这一格 | **有界重试** ＋ **只有显式允许值才放行**（`mergeable` ⇒ 只有 `MERGEABLE` ✓；`UNKNOWN` 不算绿 ✗） | 把"未就绪"读成"通过"或"失败" | 本仓 `#882` |

## §⑮ 我从名字里推出的"**这是 PR #N／那个东西**"，查过它的**类型**吗

| ⑮ | 我从名字里推出的"**这是 PR #N／那个东西**"，查过它的**类型**吗 | 推出来的对象**再查一次它的种类**：`gh pr view <n> --json state`（**PR 号与 issue 号同段** ⇒ 名字里看不出来 ✗）；非 MERGED ⇒ 一律**留** ✗ | 号对了但**指向另一件事** | 本仓支清理（`#785` 是 issue ✗） |

## §⑯ 我是不是把一个**通道现象**（超时／静默／无回音 ✗）读成了**对方的状态**（不在听 ✗）

| ⑯ | 我是不是把一个**通道现象**（超时／静默／无回音 ✗）读成了**对方的状态**（不在听 ✗） | 问"这条读数**比过**那个状态吗" ✗；动作: **开工先发一条「收到，开始核」** ✓ ⇒ 之后的"静默"才有意义（＝正在跑 ✓） | 对方**真的**不在听（这条只是**提前定义**静默的含义 ✗） | 本仓 `#895` 复核撤单（超时 ⇒ 误判 ✗） |

## §⑰ 我这棵**基线树**干净且完整吗（残留夹具 ✗／缺依赖 ✗）

| ⑰ | 我这棵**基线树**干净且完整吗（残留夹具 ✗／缺依赖 ✗） | 动手前先量三样：`git status --short`（应空 ✓）＋ 依赖就位（node_modules ✓）＋ 该树的 commit 是**我读的那个** ✓；夹具清场要遍历**所有动过的树** ✓ | 树"看着能跑"但成分与它自称的不同 | 本仓 `#898`／`#900` 复核（残留 `stories/zz-probe/` ⇒ 读到 1098B **假差异** ✗；另一树的 worktree 缺 `node_modules` ⇒ 50 段**假红** ✗） |

## §⑱ 我的**夹具落点**清全了吗（`stories/<slug>` ✗／`dist/stories/<slug>` ✗／`build/generated/<slug>` ✗）

| ⑱ | 我的**夹具落点**清全了吗（`stories/<slug>` ✗／`dist/stories/<slug>` ✗／`build/generated/<slug>` ✗） | **前置：该树已 `node build.mjs`** ✓（未 build 就跑"会被污染的段" ⇒ 拿到 rc=1 ✗，看着像"夹具没清干净" ✗）；清场按**该夹具的所有落点**遍历（不是"我动手的那一处" ✗）：三处逐处 `ls -d` ＋ **再跑那条会被污染影响的段** ✓（枚举/发现类 ✓）；判据 ＝ 该段绿 ∧ 三处都不存在 ✓ | 该夹具落在**你没列进清单的第四处** ✗（清单是人写的 ✓）—— 只能靠"跑会被污染的段"兜底 ✓ | 本仓 `#902` 复核（只清 `stories/` ⇒ **19 段假红** ✗） |

## §⑲ 我这条读数的**前置环境**写出来了吗

| ⑲ | 我这条读数的**前置环境**写出来了吗 | 读数旁边**逐条列前置**（树已 build／依赖就位／变量已导出 …）＋**命令块必须能粘贴复跑**（前置要写进那条命令：`node build.mjs && node test/…` ✓）；缺前置 ⇒ 把该读数标成"**未在前提下取**" ✗。**与 ⑧c 分工**：⑧c 管"我**动手前**核条件" ✗／⑲ 管"我**写读数时**标条件" ✗。 **机械半边** ✓：**已覆盖（探针类 ✓）**＝运行器的 `pre` ＋「**缺前置**」与「**不咬**」**分家报错** ✓（缺前置 ⇒ 报缺前置 ✗，**不报成"不咬"**✗ —— **活体读数** ✓，见 `#908` ①／`#927` ✓）；**不上机械（一般读数段 ✗）**：**漏项与"没写"文本同形** ✗ ⇒ 只能人读 ＋ **写明理由** ✓（与 ⑰ 那句同族 ✓）。**将来若做 ⇒ 走探针式** ✓（**删掉某段前置 ⇒ 该段必须点名报缺前置** ✗）；**不采纳"文本代理"式** ✗（台账加"量法/前置"列、取值必须含 `build.mjs` 或"无需前置"／`test-plan` 段加必填 `pre` ✓ —— **写错也通过 ⇒ 假绿** ✗，且与"表要被用、不要被填满" ✗ 相冲 ✓，见 `#906` ✓）。**现成落点**＝台账的『量法』列（`report-gate-ledger.mjs` 已有该概念 ✓） | 前置写了但**列漏**某一项 ✗（漏项与"没写"在读数上长得一样）；命令块能跑但**前置仍漏** ✗（它只保证"能跑" ✓） | 本仓 `#902`（未 build ⇒ 假红 ✗）／`#865` **reviews/`5235876662`**（T 面评审「③ 我的刀（前置先补齐 ✓）」✓ —— **一步可核** ✓；⚠️ **载体是 review ✗，不在 body** ✗：`issues/865/comments` 为**空** ✓） |

## §⑳ 我要开／要写的东西，**已经有人承载了吗**（同族的票 ✗／PR ✗／表行 ✗）

| ⑳ | 我要开／要写的东西，**已经有人承载了吗**（同族的票 ✗／PR ✗／表行 ✗） | **动笔前先查**：`gh issue list --state open --search <关键词>` ＋ `gh pr list --search <关键词>` ＋ 在表／台账里 `grep` 同族词；发现已有 ⇒ **不重复建** ✗，改为**在该载体里补一句**或**回指它** ✓ | **名字不同但同一件事** ✗（语义重复：靠人读标题与正文 ✓，机械查不出 ✗） | 本仓 `#909`（重复开票 ⇒ 作废并回指更早的 `#908` ✗） |

## §㉑ 我这条读数**量的面**，与它**声称在说的面**一样大吗

| ㉑ | 我这条读数**量的面**，与它**声称在说的面**一样大吗 | 结论要**限定到声称的面** ✗：例"未夹带" ⇒ `git diff <被批 head> <合后 main> -- <本 PR 的文件面>` **空** ✓；面**放大**（不限路径 ⇒ 会把"基线前移"算进来 ✗）或**缩小**（只查自己改的那一段 ✗）都要**明写** ✓ | **声称的面本身写漏了文件** ✗（PR 的文件列表／任务的边界写少了 ⇒ 尺子再准也漏 ✗；那要靠 ② 的引用面 ＋ ⑳ 的既存检查 ✓） | 本仓 `#913` 合入记录（不限路径时 diff 非空 ⇒ 只因 `#910`／`#911` 在本 PR 基线之后落 docs ✗） ｜ **与 ⑨b 的分工**：**⑨b** 管「尺子**太窄** ⇒ **假绿**」✗／本行管「尺子**太宽** ⇒ **假红**」✗ —— 两者的正形相同：**把面写出来，并让结论只覆盖那个面** ✓。 |

## §㉒ 我要**让路／撤回**时，确认对方**还在飞**了吗

| ㉒ | 我要**让路／撤回**时，确认对方**还在飞**了吗 | 撤手前先看**对方载体的状态**（那个 PR 还 `OPEN` 吗 ✗／票上谁在写 ✗／有没有认领评论 ✗）＋ **撤完再回看一眼这条线**（`gh pr list --state open` 里本票相关还在不在 ✓）；并发场景下**先问一句再撤**最省 ✓ | **双方同时撤** ✗（两个动作落在同一分钟里 ⇒ 只能靠"撤前问一句／撤后回看"兜底 ✓；机械查不出"对方**正要**撤" ✗） | 本仓 `#917`／`#918`／`#919`（两席同时让路 ⇒ `#917` 一度**无人在飞** ✗；随后重开 `#918` ✓） |

## §㉓ 我这条读数**用的尺子种类**，配得上我要下的结论吗

| ㉓ | 我这条读数**用的尺子种类**，配得上我要下的结论吗 | 结论是**行为**的 ⇒ 用**行为量**（跑一次／取值对照：同输入异取值看输出变不变 ✓）；结论是**文本／结构**的 ⇒ 用文本量（`grep`／`ls` ✓）；两者必须混用 ⇒ 把弱的那半标成**代理** ✗ 并写清它代理什么 ✓ | **行为量也可能量错对象** ✗（跑对了但读的不是那条路 ⇒ 归 ⑬ ✓）；**"没跑"被当成"没变"** ✗ | 本仓 `#924`（我用正则读源码 ⇒ 误报"两臂不同" ✗，行为读数才判出**死三元** ✓）／`#907`·`#910`（只读 body／只查 issue ⇒ 代理 ✗） |

## §㉔ 这一格是**代理**还是**直接读数**

| ㉔ | 这一格是**代理**还是**直接读数** | 判据：**变异前绿 ⇒ 最小变异 ⇒ 必须红 ∧ 点名该判据**（`node scripts/probe-gates.mjs --probe=fast` ✓）；「注入确认 N 处」里 **N=0 ⇒ 红** ✗（刀没下到真语句 ✓ —— 文件/行号不是锚 ✗）；**缺前置**与**不咬**分开报 ✓（缺 `build` 就别读成"没牙"✗）；读数带**新鲜度**（被测件改了 ⇒ `✅` 回落 `—` ✓）；覆盖**只许收缩**（`scripts/probe-budget.json` ✓） | **只有代理** ✗（"写了那句话"✗／"探针件在不在"✗／"跑了多少条"✗ —— 三者都不看"咬没咬"✓） | 本仓 `#908` ① ＋ 运行器 `scripts/probe-gates.mjs`（`#899` ③ 把「自证」列从措辞收紧后，剩下的正是这一格 ✓） |

## §㉕ 我这一条"合规"，**在目的上也成立**吗（还是只在字面上成立 ✗）

| ㉕ | 我这一条"合规"，**在目的上也成立**吗（还是只在字面上成立 ✗） | 问一句"**这条规则当初为什么要立**" ✗ ⇒ 再用它：**字面满足但目的落空** ⇒ 视为**不满足** ✗（例: 换作者再出票 ⇒ **出票人就是补丁的真作者** ✗ ⇒ 形式合规、实质规避 ✓）；缺口就**承认缺口**（显式授权／点名放行 ✓ 留痕 ✓），不自己绕 ✗ | **"目的"本身被理解错** ✗（把某条规则的目的读窄／读宽 ⇒ 需要**引原文**并对齐 ✓）；**形式与实质都成立但其实两码事** ✗ | 本仓 `#932`（"换作者 ⇒ **出票人仍是补丁的真作者**" ✗ ⇒ 按目的拒掉 ✓）／`#848`（`行为化 ✅` 但 selftest **不可能失败** ✗） |

## §㉖ 我这条**读数**，是**本树刚生成**的产物吗（还是上次构建的**残留** ✗）

| ㉖ | 我这条**读数**，是**本树刚生成**的产物吗（还是上次构建的**残留** ✗） | 量产物/生成物之前**先确认它刚由本树生成过** ✗：`dist` ⇒ `rm -rf dist && node build.mjs` ✓；台账 ⇒ 必须**在探针段跑过之后**再重生成 ✓；**改了源/还原之后** ⇒ `node build.mjs` **重建** ✓（产物是 `dist/`：不重建则跑的是旧的 ⇒ `scripts/dist-fresh.mjs` 缺产物时**大声报错**、不静默 ✓） | 「生成过了」≠「是本树刚生成的」✗（陈旧文件照样算出漂亮聚合值 ✓）；报文只说"不一致"时**不点明哪一列** ✗ | 陈旧 `index.html` 让 `dist` 聚合变值 ✓／`gate-ledger` 重生成 ↔ 探针段时序 ✓（`#936`·`#935` 两次实测）；➕ **单跑 vs 套跑分开看** ✗：`npm test` **自带 build** ✓ ⇒ **单独**跑 `audit:golden`／`test/web-preview.mjs` 这类命令，**先 `node build.mjs`** ✗（否则 `dist-fresh` 守卫抛 ⇒ **假红** ✓ —— 四个独立实证：`web-preview` ✓／`audit:golden`（28 开关"不符"）✓／`cli-surface` 的 `lint-story 全链` ✓／新树先跑 golden ✓）；➕ **自证要在"已跟踪"状态下跑** ✗：有几道门扫的是 **`git ls-files`（只扫**已跟踪**件 ✓）** ⇒ **新件在被 `git add` 之前不在扫面** ⇒ **假绿** ✓（实证：`#961` 报 `attribution` rc=0 而 CI 红 ✓ —— 与上面那条**方向相反、同族** ✓：**读数环境**变了，数就不作数 ✓） |

## §㉗ 我这一刀／这一比，打的是**我要的那个面**吗

| ㉗ | 我这一刀／这一比，打的是**我要的那个面**吗 | **动笔/提交前先打"我在哪棵树、哪条枝"** ✗：`git branch --show-current`（**空 ⇒ detached** ✗）＋ `git rev-parse --short HEAD origin/main` ✓ —— detached 上提交 ⇒ **分支没动、推送"Everything up-to-date"** ✗；陈旧 `origin/main` ⇒ **diff 多算别人的件** ✗（一夜里这条坑**两席各栽一次** ✓）。**先把面打出来，再念数** ✗：`grep` 先取到**真写法**（不是你以为的写法 ✓）／`diff` 带 **`-- <paths>`**（gate #3 的"实现 diff"必须带 pathspec ✓）／目录先确认（`ls`／`ls-tree` ✓）⇒ **再**读数 ✓ | **面写宽**（全量 diff 把**基线前进**算成本片带的 ✗）或**写窄**（只查 `test/**` 漏掉 `editor/**` ✗）—— 两者都会让"数"看着合理 ✗ | 本仓: 一刀打偏（`grep` 找错目录 ✗）／一比打宽（**全量 diff 混进 B-1 的改动** ✗）／gate #3 那句"实现 diff **带 pathspec**" ✓ ／**树打偏** ✗（`vote-check` 跑在**错的 cwd** ⇒ `requireStack: []` ✓ ⇒ 换对树才拿到 2 席 ✓ —— 固化：**跑命令前先 `pwd`** ✓，比事后 `git status` 更快看出树对不对 ✓）／**比到了 `tail` 身上** ✗（`… | tail -2; echo $?` 量到的是 **`tail` 的退出码** ⇒ 差点把 `rc=0` 读成"脚本没拦住" ✓ —— **量错对象比量错值更隐蔽** ✓）／**比到了另一段文本上** ✗（漏 `--story` ⇒ 两侧都 `rc=2` ⇒ `cmp` **假同** ✓）／**票锚与头对不上**（`commit_id` 与 `submitted_at` 对不上 ✗ ⇒ 先做**带 pathspec 的 diff** 证明"验的就是被合的" ✓ —— 且纯 rebase 时**票锚会被 GitHub 重指** ✗ ⇒ 那半句只当提示 ✓） |

## §㉘ 我这条**动议／建议**，是打在它**当前的行为**上的吗（➕ **落笔前先过 token 表** ✗：写 §17／注释／票面之前 `grep` 一遍去权威化词表 ✓；**已落成别名** ✗：`npm run check:deauth` ＝ `node test/attribution-gate.mjs` ✓ —— **与门同一份判据、零新逻辑** ✓，约 **0.1s** ✓（写 §17／注释／票面**之后、提交之前**跑 ✓；**别等 CI** ✗） —— 本仓 `test/attribution-gate.mjs` 只 0.1s ✓，而**同门同族已栽 6 次** ✗：`#950` 的 `contractVersion.mjs` ✓／`#956` 的 §17 ㉘ ✓／`#961` 的 §17 ㉙㉚㉛ ✓／`#952` ✓／`#954`（注释里写了席位）✓／`#968`（`k4.mjs:83`）✓ ⇒ **门每次都如实报** ✓，缺的是**落笔前那一眼** ✓）

| ㉘ | 我这条**动议／建议**，是打在它**当前的行为**上的吗（➕ **落笔前先过 token 表** ✗：写 §17／注释／票面之前 `grep` 一遍去权威化词表 ✓；**已落成别名** ✗：`npm run check:deauth` ＝ `node test/attribution-gate.mjs` ✓ —— **与门同一份判据、零新逻辑** ✓，约 **0.1s** ✓（写 §17／注释／票面**之后、提交之前**跑 ✓；**别等 CI** ✗） —— 本仓 `test/attribution-gate.mjs` 只 0.1s ✓，而**同门同族已栽 6 次** ✗：`#950` 的 `contractVersion.mjs` ✓／`#956` 的 §17 ㉘ ✓／`#961` 的 §17 ㉙㉚㉛ ✓／`#952` ✓／`#954`（注释里写了席位）✓／`#968`（`k4.mjs:83`）✓ ⇒ **门每次都如实报** ✓，缺的是**落笔前那一眼** ✓） | **先把它的当前行为打出来，再提改** ✗：跑一次（`node …` ✓）或 `grep` 一次它**现在**的输出／源码 ✓ ⇒ 再写"建议改成…" ✓；拿不到当前行为 ⇒ **先别提** ✗（提了也只是猜 ✗） | **"跑门上瘾"** ✗：没跑就断言"这门不做 X" ⇒ 提议全错但看着像改正 ✓；**把"我读到的"当成"它现在的"** ✗（源码改了／我读的是旧树 ⇒ 归 ㉖／㉗ ✓） | 本仓 `#954`（票内动议："让去权威化门**报出命中的 token**" ✗ ⇒ 票内复核当场给实测反证：`test/attribution-gate.mjs:168` **本就**打印位置 ＋ 命中串 ＋ token id ✓ ⇒ **动议作废**、无需改门 ✓） |

## §㉙ 我这张 **PR 的票，真的齐了吗**

| ㉙ | 我这张 **PR 的票，真的齐了吗** | **把"票齐"当机械前置** ✗（与 CI／main／子 PR **同级** ✓）：`head = gh pr view <n> --json headRefOid` ⇒ `gh api repos/<o>/<r>/pulls/<n>/reviews --paginate` 里数 **`state==APPROVED ∧ commit_id==head` 的**distinct** 席数 ≥ 2** ✓ —— **单票 ⇒ 停** ✗（哪怕其余三项全绿 ✓）；**已落成脚本** ✗：`node scripts/vote-check.mjs <PR> [--want=2]`（自检 `--selftest` ✓）—— 它打印 **head ＋ 锚该 head 的 APPROVE distinct 席 ＋ 席数**，**≥ want 才退 0** ✓（**单票 ⇒ 非 0** ✗）；**谁跑／何时跑** ✗：**合入执行人在 `gh pr merge` 之前**跑 ✓；它**不进 `npm test`** ✗（要 `gh` ＋ 网络 ⇒ 不是 CI 项 ✓）⇒ 本条的牙＝**合入动作前那条命令** ✓，不是"CI 会拦"✗ | **票数够但席不对** ✗（同一席投两次 ≠ 双席 ⇒ 必须 `unique` ✓）；**非纯 rebase 后 head 变了却沿用旧票** ✗（那是 `### 双席` #4 的事 ✓，归 ㉗ 那句"先做带 pathspec 的 diff"✓） | 本仓 `#957`（**合入那一刻只有一张票** ✗ 而**同一命令块里就打印着票况** ✓ —— 不是信息缺失 ✓，是**判据之间标准不一**（CI／main／子 PR 当硬前置 ✓，票况目视带过 ✗）⇒ 事后已请复核席补 post-merge 复核 ✓） |

## §㉚ 我清单上这一项，**是命令还是回忆**

| ㉚ | 我清单上这一项，**是命令还是回忆** | **每条机械项都要有"一条能粘的命令 ＋ 它打印出来的数"** ✗；拿不出命令 ⇒ 这项**等于不在清单上** ✓（要么写命令 ✓，要么删掉它并**明说没查** ✓） | **"我看了/我确认过"** ✗（没有数就没有面 ✓）；**命令存在但数没打印** ✗（跑完只看`rc` ✓——`rc=0` 也可能量错对象 ✓ 归 ⑬／㉓ ✓） | 本仓 `#957`（票况就在输出里 ✗ 我却当"信息"读 ✓ —— 它本该是**一条判据** ✓）；同族：`#944`（机械项都跑了 ✓、**没在清单上的那项**没跑 ✗）／复核席提的通则 ✓ |

## §㉛ 我这条**报备**，把**本片的机械后果**点全了吗

| ㉛ | 我这条**报备**，把**本片的机械后果**点全了吗 | **可预见 ⇒ 必须点名** ✗（例："新增**台账行**（＝新门 id）⇒ 必须 ＋1 探针 ⇒ 必然碰 `probes.mjs`／`test-plan.mjs`／台账"✓；"新容器 ⇒ 必然补**既有** jsdom" ✓）⇒ **一次点全** ✓；**不可预见 ⇒ 帖内先问** ✗（动手之前 ✓） | **把"机械适配"当免责** ✗（是机械的 ✓ 也要先点名 ✓ —— 否则事后补就只能算"报告"✓，不算"先"✗）；**反过来报多**✓ 比报少好 ✓（"可能碰 X，若碰则补一句"✓） | 本仓 `#956`（报备写"**不碰** `test-plan`／`probes`／台账"✗ 而实际碰了 ✓ ⇒ 合入席裁"**不阻断**" ✗ 并给出这条边界 ✓）／`#958`（同一份报备的**正例** ✗："＋1 探针"那格虽写错 ✓，但"会碰哪几件"是**先写出来的** ✓）；➕ **依赖／堆叠 ⇒ 写明先合哪片** ✗：`A 依赖 B` ⇒ 报备里点明"**先合 B**"✓（`lane-d/vote-check` 那片就等 `#960` 合后才起片 ✓）；**后合者 rebase** ✓，且**纯 rebase ⇒ 票沿用** ✓（`### 双席` #4 ✓：非纯才重投 ✓） |

## §㉜ 我拿到的这条**代理／代理式读数**，能直接当**定性**吗

| ㉜ | 我拿到的这条**代理／代理式读数**，能直接当**定性**吗 | **代理给的只是候选；定性要换一句问题再问一次** ✗：例——「**这行在不在 `<table>` 内**」❓ 只是**位置**代理 ✓；要判"表破没破"必须再问「**它是不是一张新表的表头 ＋ 分隔行**」✓；行级扫描（`|` → 空行 → `|`）同理 ⇒ 只给**候选** ✓，定性要用**渲染器**或人眼 ✓（㉔ 的**第二形态** ✗） | **把「没命中／不在里面」当成「没有该缺陷」** ✗（代理的**问句**与结论的问句**不是同一句** ✓）；**反过来也错** ✗：代理命中 ⇒ 直接判红（我 F5 第一版就栽在这 ✓） | 本仓今晚**两席各一次** ✗：复核席问「这行在不在 `<table>` 内」⇒ 读成"**破表**"✓（`docs/game-outline.md` 实为**并列两张正当表** ⇒ 渲染复核 `<thead>` 在 ✓）；F5 **第一版**（`#974` 落地时）「`|`→空行→`|` ⇒ 直接判破」✓ 同处**假阳性** ✓ ⇒ 两条都已写成自证/判据的正常例 ✓ | <!-- path-exempt: #1077 ㉜ 历史案例叙述保持原文，game-outline 已入档 -->

## §㉝ 我这一刀（变异／注入）与这一条断言，**锚在被测的那一支／那一层上了吗**

| ㉝ | 我这一刀（变异／注入）与这一条断言，**锚在被测的那一支／那一层上了吗** | **先打出「被测支」的标识再下手** ✗：变异用**带锚**替换（含行首缩进／层级前缀，如 `\n    if: …` 与 `\n      - if: …` 分写）＋ 断言喂**指向该支**的输入；**改完必 `node --check`** ✓，并**打印替换后的那一行**证明打对了支 | **无锚全串替换＝只替第一处 ⇒ 常打在别的一支上** ✗（实测：拆掉 job 级判据而自证仍全绿）；另：改代码别用多层引号的 shell／python 拼接（易崩且难定位） | `#1054`（D2）· `#1051` |

## §㉞ 这条**豁免／放宽**的**前提**，是在**真执行路径**上验过的吗

| ㉞ | 这条**豁免／放宽**的**前提**，是在**真执行路径**上验过的吗 | 前提本身要**另取一次运行时读数**：走**真执行路径**（jsdom boot 读 `State`／真门调用），**不是**再跑一遍同一函数 ✓；问法：**「这个语法／写法在目标引擎里真的不可执行／不生效吗」**，并把它**当场跑出来** | **成对自证只证明「实现符合前提」** ✗（实测反证：以为「反引号＝解释语法」 ⇒ 运行时宏照执行）；**行级代理门 rc=0 也不等于结构没坏**（表格列数错位即被 `md-format` 放过） | `#1059` · `#1051` · `#1060` |

## §㉟ 这条判据的**「能假」**，覆盖了哪几族注入（**「自证有几格」≠「够不够」** ✗）

| ㉟ | 这条判据的**「能假」**，覆盖了哪几族注入（**「自证有几格」≠「够不够」** ✗） | 判「能假」时至少要有**三族注入**，缺一族就可能被绕过 ✗：①**改坏**（值非法／引用失效 ✓）②**删掉**（输入面消失 ✓ ← `#557` 族）③**掏空**（面在、内容为空 ✓）。逐族各跑一次并**当场报出**读数（不是"写了就算" ✓）；**问法：**「这三族都过了之后，**还有哪一族输入能让它继续绿**？」——**三族是已知下界、不是全集** ✗。 | **只做①就以为完备** ✗：实测 `#1082` 的读路径门自证三格全是①（死链／权威位／字节超限 ✓），而**删掉整节**或**清空列** ⇒ 判据对**空数组**一律通过 ⇒ `rc=0` 且打 `✓` ✗（切段用 `split(章节标题)` ⇒ 标题没了 ⇒ 返回空数组 ✓）；**"删掉输入"比"改坏输入"更容易过** ✗（本票立意是给"表手维护、零门"补门 ⇒ 结果"表被删"反而更绿 ⇒ **门成了可选装饰** ✓） | `#1082`（三组注入：删节／删权威表节／清空先读列 ⇒ 均 `rc=0` ✗）· 同族：`#557`（读不到输入 ≠ 没命中）· `attribution-gate` 的「扫描面为 0 ⇒ 红」· 「隔离树绿不是证据」 |


## §为什么（这份口径是从"某一格没有依据"里长出来的（外移节）

### 为什么（这份口径是从"某一格没有依据"里长出来的）

它经**三次升级** ✓，每次都因为**一个字面上"通过"的格子其实没有依据** ✗：

| 升级 | 起因 | 变成了什么 |
|---|---|---|
| 一 | `#848`：自证**只是把真表再念一遍** ✗ ⇒ 台账那格「行为化」**没有依据** ✓ | 自证必须**能红** ✓（`#853` 把判定抽成 `judgeSurface` ＋ 假 runner 驱动 5 例 ✓） |
| 二 | 在册差异记录**只写 rc** ✗ ⇒ 契约变了它也不响 ✓ | 记录要**量契约本身** ✓（`#855`：`cliErrIncludes` ✓） |
| 三 | 页面预览若在 `editor/web/**` 里自造渲染规则 ✗ ⇒ 两份渲染须同步 ✓ | 判定仍在**内核一处** ✓（`#857`／`#859`／`#861`：只 import 既有内核 ✓），宿主只当**薄 sink** ✓ |
| 四（进行中） | 预览要跑**引擎** ✓ ⇒ "谁来渲染"是**引擎面**口径 ✓ | 按同一口径裁：**渲染只有一处 · 宿主可以有多个** ✓（裁定见 `#761` ✓） |




## §一条更早的检查（在读数**产生之前**就查它 ✓（外移节）

### 一条更早的检查（在读数**产生之前**就查它 ✓）

> **断言必须能是假的** ✗ —— 与"读数须能证伪自己"同根 ✓；两种最偷懒的形态各要一眼:
> · **字面真值**（`t('…', true)` ✓）⇒ 那不是断言 ✓，是注释 ✗；
> · **自比自**（`sha(f) === probeSha`，而 `probeSha` 就是同一个 `f` ✓）⇒ 恒真 ✗，与被测对象无关 ✓。
> · **用定义验证定义**（`rebuilt === B`，而 `rebuilt` 就是按**最长公共前后缀**重建的 ✓）⇒ **构造上恒真** ✗
>   —— 出处: `#871`（第五次拦下，也是**最深**一次: 该句曾作为 `#866` 的"关键读数"**合进 main** ✓
>   后来被替换成能假的一对: **段含标记** ✓（独立事实 ✓）＋ **段长 ≤ 标记长 ＋ 小常数** ✓）。
> ⇒ 判法: 问"**这行代码在比较哪两个东西？它们可能不相等吗？**" ✗ —— 答不出 ⇒ 先别念读数 ✓。
> （出处: `#867` 第四次拦下 —— 前三次由**运行结果**拦（`T1==T3` ✓／刀打回原件 ✓／同一个 `w` ✓），
>   这一次由**断言的字面值**拦 ✓ ⇒ 说明"看运行输出"之外，还要看"**那行代码到底在比较什么**" ✓。）

**人工兜底（不可省 ✓）**: 上表右栏**非空**的每一格，都只能靠**复核时的人工三问** ✓ —— 故规则 5／5b／2（含"两份实现、名字不同"那类）在**每一次**复核里都必须问 ✓，不能因为"门是绿的"就跳过 ✗。




## §一条同族的坑：**同一手下，换执行环境结论会反*（外移节）

### 一条同族的坑：**同一手下，换执行环境结论会反** ✗（`#1012` ✓）

> **我给判定加的那一手，是在「目标环境」里验的吗？** ✗ —— 出处：`#1012`（引擎侧「导航后焦点回收正文」）✓：
> 同一句 `content.focus()`，**jsdom 里同步调用就绿** ✓、**真机 Chrome 里同步调用会被「激活序列收尾」盖回 `body`** ✗
> ⇒ 必须延到 `requestAnimationFrame` ✓（两向读数都有：禁用 ⇒ 落 `body` ✓；启用 ⇒ `DIV.passage` ✓）。
> ⇒ 判法（与「**本地绿 ≠ CI 等效**」（`#1004` ✓）同族）：**环境也是输入的一部分** ✓ ——
> 凡是"本地（jsdom）绿了""只有仓内测试绿了"的读数，写进结论前先问"**目标环境（真机／CI／线上）跑过没有**" ✗。
> ⚠️ 配套：**禁用一手的手法本身也要验** ✗ —— `#1012` 里按 jQuery **namespace** 改名"禁掉"处理器**无效**
> （namespace 不挡事件分发 ✓）⇒ 差点把一个**真会红**的门读成"不咬" ✓；刀打在**事件名**上才成立 ✓。


