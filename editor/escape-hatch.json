{
	"note": "K4 逃生舱登记表（`#762` 车道 C）：契约成员里**含任意逻辑**（分类器 C 桶）的必须在此逐条登记（理由＋票号）。清单外出现即红；已不是 C 桶的登记也红（清单只许收缩，收缩留痕）。B 桶（需声明式扩展）与 D 桶（可下沉引擎）都**不算**逃生舱。",
	"hatches": [
		{
			"member": "eventPool",
			"slug": "hollow-cave",
			"reason": "从**已声明的** `mechanics().roads` 折叠出「事件实例清单」（`entries[kind] = [{id,kind,hint,ref}]`）—— 纯**派生**逻辑：声明面已有数据（引擎的 `Game.Combat.roadsDecl()` 就在读同一份 `roads`），折叠本身不是玩法语义、也不该由故事手写。消费者目前只有洞窟自己的门（`stories/hollow-cave/gates/cave.mjs`：查 kinds 覆盖／每类 ≥1 实例／hint 两两不同／ref 解析得到段落）。⇒ **待消除**：下沉引擎后本条登记应删除（清单只许收缩，收缩留痕）。",
			"ticket": "#778"
		}
	],
	"why_empty": "**本表只登记 C 桶**（真逃生舱：含任意逻辑、既不能用声明式 kind 表达、也不是引擎能力）。**当前 1 项**（`hollow-cave` 的 `eventPool`，见 `hatches`；`#777` 修正多站点扫描后才被认出来 —— 之前\"0\"的输入是错的）。其余成员：`#775` 落完 B 桶四项欠账后 B 桶清零（洞窟剩 2 项 B：`lootText`／`mechanics`，属\"可预见扩展\"）。**B/D 的实测清单与数量由门打印**（`node editor/k4.mjs` 的『欠账（B 待补 kind）／欠账（D 待下沉引擎）』两行），本文件**不手写数字**（手写必腐烂）。⇒ 不要读成「登记表短 ⇒ 没欠账」：本表只记**必须靠任意逻辑**的那一类。"
}
