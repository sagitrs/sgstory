# 判据设计法则

> **读到即用**：写新判据前，先读**第三节那七条** ＋ **面③那张口径对照表**（那张表回答了最常见的一个误解："同一个文件里几种相反口径一定是有人写错了" —— 不是，**口径服从观测对象**）。
>
> 这一页不是"风格建议" —— 它记的是**今年真实翻过的车**。每条都有实例、都在代码注释里指得出。
> 读它的收益：**写新判据时不重犯**；看别人的判据时**知道该问什么**。

---

## 一、总法则

> **判据说的、判据看的、判据喂的，必须同指一件事。**

它有三个面，各有真实事故：

### 面① 读入参 vs 读环境

| 实例 | 病灶 |
|---|---|
| `readKey` 丢 `pc` | 从**环境**取当前角色，而不是用**调用方入参** → 换档/合成角色时判据判的是别人 |
| 写侧 `applyYields`/`applyGrants`/`applySets` 无视入参 | 同族：效果落到**当前档**而不是**被驱动的档** |
| `Game.Social?.asks` 环境回落 | 引擎在**契约不可得**时回落到环境 → "两宿主同源"这条主张被悄悄破坏 |

**问法**：这个判据/函数的输入，是**参数**还是**它自己摸到的全局**？两者不一致时，它相信谁？

### 面② 声称 vs 覆盖

| 实例 | 病灶 |
|---|---|
| `#806` 声称"静态＋动态 import 都挡"，实只挡静态 | 声称 > 覆盖 → **下一个人按声称去信它** |
| K6 ② 声称"壳不许写故事文件"，只扫两个文件 | 覆盖小于声称 |
| K6 ③ 声称"浏览器安全"，只挡 Node 侧 | 同一件事的另一半没人守 |
| L1 旧口径只扫 `cli＋host` | 同上：**覆盖小于声称** → 改成扫全部 `editor/**` 后**不再依赖路径形状** |

**问法**：判据的名字/报文里承诺了几件事？代码真的查了几件？**声称与代码不符，比漏一个形更坏** —— 漏形是"没守到"，声称错是"以为守到了"。

### 面③ 观测对象 vs 遮蔽口径

**这是最容易撞的一面** —— 因为**同一个文件里可以有多种相反设置，而且都对**：

| 判据 | 观测对象 | 遮蔽口径 | 为什么 |
|---|---|---|---|
| **L1** 原语写只许在 `lib/host/**` | **代码里的调用** | **遮字符串** | 字符串里写 `writeFileSync(…)` 只是**片段**（本门自证用例正是如此）→ 不遮会**自咬** |
| **L1′** 删除原语不许落 `stories/**` | **调用参数里的字符串** | **不遮** ＋ 查"调用位置在引号外" | 参数**就是**字符串 → 遮了就把**观测对象自己**遮掉；而"字符串里的提及"用 `outsideQuotes` 排除 |
| **③a** 内核不许 import 宿主模块 | **说明符**（`'node:fs'`） | **不遮** | 说明符住在字符串里 → 遮了什么都看不见 |
| **③b** 内核不许碰宿主全局 | **标识符用法** | **遮字符串** ＋ 只看用法（`名(`／`名.`） | 提及≠用法：core 会**合法地**在字符串与正则里提 `window.` |

**判法**：先写清"**这个判据要看的对象是什么**"，再选遮蔽口径 —— **口径服从观测对象**，不是"统一口径"。

---

## 二、由总法则推出的操作纪律

1. **判据的边界由理由定，不由"看起来像"定。**
   > **判据只判它该判的** —— 范围**窄**不是缺陷，**宽到理由之外才是**。
   - 例：L1 的理由是"故事文件的**写**只有一条路" → 把**删除**也做成路径无关 会连 `rmSync(tmpdir())`／`rmSync('build/generated')` 一起咬（门会被关掉）→ 删除另立 **L1′**，只判落到 `stories/**` 的。
2. **不假装覆盖：边界要写出来，并写理由。**
   - 例：L1′ 明记"**运行期拼路径的删除不在覆盖内**"（要收它得做数据流分析，成本高收益低）；K6 ③b 明记"引号未闭合（非法 JS）那形会漏"。
   - 边界**写出来**＝下一个人知道该不该信它；**不写**＝把"没守"伪装成"守住了"。
3. **新增/改动判据时，必须有一条只对"分支"敏感的用例。**
   - 因为：**多分支写出同一结果时，结果类判据全都沉默**。
   - 活标本：`--out=stories/mist-forest`（相对路径）没走进"包内"分支 → 对拍不报 逐字节不报 只有**分支断言**能抓。
4. **自证要双向。**必红（真用法 → 报）＋ **必不红**（字符串里提及／属性访问／通用全局／边界形 → 不报）。
   - 只有"必红"一半的自证 → 下一次扩表会**误报一片** → 门被关掉。
5. **登记物/签名只描述既成状态。**
   - 顺序恒为：**状态先变 → 再剪登记/白名单 → 再重签**；重启（rebase）时**重签提交丢弃**、落地后重签一次。
   - 理由：登记物是**记录**，不是**驱动** —— 让它先动＝让判据去迎合它。
6. **先落"不改变行为的加法"。**
   - 纯新增 → 再落会生效的块 → 最后删旧路径；动作按**行为可见性**排序（这样每步都能单独验证"没变行为"）。
7. **自证必须能红，且计入退出码。**
   - 自证红而主跑绿 → 门是**装饰**。
   -注意：这条最容易被当成形式 → **配上反例与读数**（当场验过，非声称）：
     ```
     # 把任意一条自证的期望改假（例：`.length === 1` ⇒ `.length === 2`）
     node editor/k6.mjs --selftest ; echo $?   # ⇒ 1  ← 非零 ✓（自证真的计入退出码）
     # 恢复后
     node editor/k6.mjs --selftest ; echo $?   # ⇒ 0  ← 36 例全过 ✓
     ```
   - **任何人都能在 10 秒内复核** —— 这条纪律的价值全在"能被复核"：改不掉红、就把门关掉。

8. **测量/比较之前，先对齐底材。**（写成可取用的形态：**症状 → 前置动作 → 器材纪律**）

**症状** —— 出现任一条 → 这次读数**不可能**成立，别急着信它：
- 同一文件两次读出**不同行数**（实例：107 行 vs 235 行）；
- 某个字段**整体为 `undefined`**（实例：取到的 `line` 全 `undefined` → 现象像是"改了看不出效果" → 白绕两轮）；
- **两个分支写出同一份字节，却不知道自己走了哪一支**（→ 行为对、分支错；对拍／逐字节／全套测试**全不报**）。

**前置动作**（三条，都可机械执行）：
1. **`git fetch` 后记 head SHA，并把 SHA 写进读数**（读数带 SHA → 虚警**当场可比** → 事后不必重新取证）。**最便宜、收益最大**。
2. **被测对象一律"新建 worktree 钉 SHA"**，不复用长期工作树（→ **就地改／就地读是唯一漏点**）。
3. **比较量先归一化**：路径先 `resolve`；分支先**断言分支**。
   -注意：顺带一条**本仓事实**：默认分支是 **`main`**，**不是 `master`** —— 用错名字会得到"分支不存在"这类**看起来是别的问题**。

**引用纪律（读数有时效）**：**引用任何读数之前，重读"状态"那一位** —— `gh pr view --json state` / `git rev-parse origin/main`。
> 实例（当场踩过）：一条"CI 全绿 · head `…`"**取时是对的**（当时确实全绿未合），**引时已陈旧**（合入发生在取数之后）→ 底材一动，读数就过期。
> → 所以"读数带 SHA"只解决**归属**，"引用前重读状态"才解决**时效**，两件都要。

**器材纪律**（把上面变成"**每次都能做、做了就有用**"的动作，不是"要小心"这种口号）：
> **凡就地读文件 → 先核 `git log -1` 与目标 SHA 一致**。

**同一骨架的三段**（与第 5、6 条合起来才闭环）：**动笔前对齐底材 → 落笔后回读 → 读之前再对齐**。

---

9. **读数须能证伪自己。**
   > **这条读数错了，哪个读数会先变红？答不出来 → 它不是读数，是声称。**

   **推论（让它可以执行）**：答案必须是**一个你能跑的读数**，且它应当**比被质疑的读数更早、更便宜地红** —— 否则答了也不管用。
   操作上两步：**(i) 答出是哪个读数 → (ii) 确认它存在且能跑**（**不存在就先补上** —— 这一步才是重点）。

   **为什么不是鸡汤**（四个实例全是实测）：
   | 场景 | 答出的"先红的那条" | 结果 |
   |---|---|---|
   | 注入类探针 | 需答"注入是否生效" → 发现**该读数当时不存在** | 补「打印 pattern 是否命中 ＋ grep 注入标记」才敢下结论 |
   | 归一化脚本坏过 | **已知红样本应当红**（正向对照） | 可跑 → 答案成立 |
   | `k4`／`lint-story` 的守卫 | **删掉守卫后，那条反例应当红** | 可跑 → 答案成立 |
   | **反例（声称档）** | 旧台账那格 `自证 ✅` 是**错误报文里的词**命中的 → 问"它错了哪个会先红" → **答不出** | 当场显形（随后补了真 3 例自证） |

   **它的用处在于"更早"**：不必跑、不必开工具，**自问一遍就能用**。


### 8b. **器具也会错 → "断言"是装备，"小心"不是**

> 一句话命题：**被测量的东西会错，测量它的东西也会错** —— 所以护法不能是"我更小心"，只能是**断言**。

三类（**每类都真的发生过**，各附一条可复现的形与**拦它的断言**）：

| 类别 | 实例 | 拦它的断言 |
|---|---|---|
| **(i) 动作静默没生效** | `git fetch <remote> <ref>:<local>` 对**已存在**的本地 ref **不报错也不更新**（于是拿旧 head 当新 head 用）；文本替换的目标串不符 → **空操作**却照打印"已落"；注入落进了**形参默认值** → 语法错，而 `grep` 命中的是**报错回显的源码行** | **回读你刚改的东西**：断言"注入真在文件里" ／`git show --stat HEAD` 看改动**真进了 commit** ／`git status --short` 看有无**未落**改动 |
| **(ii) 取样错位** | 重定向写到 `~/tmp/x` 却去读 `/tmp/x` → 读到**别的命令的陈旧文件**；`awk` 行窗口取到**错位的窗口** → 报出**假分叉**；`grep` 命中的是**报错回显**而不是文件内容 | **断言对象同一性**：目标 SHA == 权威 SHA ／从**你写的那条路径**读回 ／**两侧皆空守卫**（两侧都空 → 判"读数作废" 不是"相同"） |
| **(iii) 对齐/单位错** | `${H:0:8}`（8 位）与期望的 7 位比；把 `sha256sum`（**内容哈希**）叫作 "blob"（**git 对象 id**）；`prune` 排在 `rm` **之前**；器具自己的搜索串没做同样的空白归一 → `AssertionError` | **比之前先断言"同一把尺"**：类型/长度/口径先断言（最好与**权威来源**全等比对，而不是与自己算的截断值比） |

**三条护法（各自一行）**：
1. **(i) → 回读效果**（改完/写完，读回来确认；别信脚本的成功语）。
2. **(ii) → 断言对象同一性**（先证"我读的就是我写的/我读的就是那个 SHA"）。
3. **(iii) → 比前先对齐单位**（长度、类型、哈希口径**先对齐**再比）。

**为什么单列这一段**（而不是并入第 8 条）：第 8 条管"**底材**陈旧 → 读数假"；这一段管"**器具自身**出错 → 读数假"。两者症状同族（读数看着像结论），但**治理不同**：一条是"动笔前对齐底材"，一条是"给器具也配断言"。

## 二之二、判定线：**该不该上门，看假阳性率，不看重要性**

> **能不能写成一行、且不冤杀** —— 能 → 写成判据；不能 → 留**评审**。

理由：**门被关掉比没有门更糟**。假阳性一多，人就不信门 → 那道门连同它看守的东西一起失效。
→ 所以"这件事很重要"**不构成**上门的理由；"这件事**几乎没有解释余地**"才是。

**两个对照实例**（同一天出炉）：

| 候选判据 | 假阳性余地 | 裁定 |
|---|---|---|
|「`import editor/**` → **rc=0 ＋ 0 字节输出**」 | **零** —— import 期打印/退出**就是它要禁的东西本身** | **上门**（`test/` 里一个测例覆盖全部模块） |
|「同一能力只许一处实现」（按函数体形状判） | ≈**50%** —— 把"只在字面量上不同的兄弟函数"、把两条控制流同形但查不同表的判据 都判成重复 | **留评审**（负结果 ＋ 真发现一起记台账） |

**配套顺序纪律**（去重类改动的正确次序）：
> **发现重复 → 先对打语料证"同"，再删一份**。
> 反序的话，删掉的那份可能带着**未发现的差异** → 先量（差异语料 ＋ 逐字节对拍）再删。
> 例：量之前票面只能写"两份实现存在"；量了语义等价（如 14 例 0 分歧，或**改前/改后输出逐字节相同**）才敢写"纯重复、无行为风险"。

---

---

## 三、怎么用这一页

- 写**新判据**前：把"说的/看的/喂的"三句话各写一行，对不上就先改设计。
- 看**别人的判据**时问三问：①输入是参数还是环境 ②声称与覆盖是否一致 ③观测对象与遮蔽口径是否匹配。
- 撞到新的一面：**给它命名、写实例、留纸** —— 没名字的教训会被重犯。

**交叉引用**：`editor/k6.mjs`（L1／L1′／③b 的实现与注释即上面各条的实例）· `docs/manual/**`（**手册：作者面与引擎面**）· `docs/criteria-ledger.md`（判据册）· `docs/dev-conventions-cases.md`（案例册）· `docs/superpowers/specs/editor-pivot.md`（编辑器转向）· `#794`（内核抽取／防双内核）。

## 四、术语表（门／判据／流程的**共同语言**）

**为什么有这一节**：这些词以前**散落**在 17／14／42 份文档里、无一处统一定义 ⇒ 代价今天就付过：
「自证 ✅」被读成"**断言真会红**" ✗（其实只量"信号出现在代码/字符串面"）／「未接线」与「临时下架」（当时的说法；今为「不进 CI 运行（挂起）」）混说 ／「段/门/牙」混用 ✗。
**本节不是第二权威**：每条都**指回**权威处（校验函数／`REASONS`／门／文件）；表**只做索引与边界** ✓。

> **与台账图例的关系**：「自证」那一列的**权威说明**在生成器里（`scripts/report-gate-ledger.mjs` 的 `LEGEND`，随台账一起生成 ⇒ ✗ 手改不会生效）；本节**指回去**，✗ 不复制它的正文。

### 怎么读（每条四问）
```
① 是什么　② 怎么判（可机检的锚／可复跑命令）　③ **不测什么**（边界：它答不了哪个问题）　④ 权威在哪（读哪一处／跑哪一条）
```

### 4.1 结构层
| 术语 | ① 是什么 | ② 怎么判 | ③ **不测什么** | ④ 权威在哪 |
|---|---|---|---|---|
| **段（segment）** | 跑器里的一条执行单元 | `scripts/test-plan.mjs` 的 `SEGMENTS` 数组 | ✗ 不管"它有没有价值" | `test-plan.mjs`（`SEGMENTS`） |
| **门（gate）** | 一条**自己判红/绿**的检查（通常 ＝ 1～2 段：真跑段 ＋ 自证段） | 台账 `docs/gate-ledger.md` 的一行 | ✗ 不保证"它咬得住"（那要看**探针**） | `docs/gate-ledger.md`（生成物，✗ 不手改） |
| **牙（有刀的门）** | 有**最小变异**能让它**必红**的门 | 该门在 `scripts/probes.mjs` 里有条目 | ✗ 不代表"永不误报"（刀证明的是"能红"） | `scripts/probes.mjs`（每条含 `mutation`＋`expect`） |
| **面（scope）** | 判据判**哪些对象** | 判据里的枚举口径（如"有 `data/passages.json` 的故事"） | ✗ 不判"面外的对象是否正确"（**面外 ≠ 欠账**） | 各门自己的枚举处（如 `prose-vocabulary.mjs` 的改制面） |
| **档位（tier）** | 段在哪一档跑：`fast`（PR）／`full` | `tierOf(seg)`；`full` 段**必须**在 `FULL_REASONS` 里有理由 | ✗ 不代表"降频＝已处置"（`full` 可以只是**暂存**） | `test-plan.mjs`（`TIERS`／`FULL_REASONS`） |

### 4.2 判据质量层（**最容易误读的三个词**）
| 术语 | ① 是什么 | ② 怎么判 | ③ **不测什么** | ④ 权威在哪 |
|---|---|---|---|---|
| **自证（`--selftest`）** | "**信号出现在代码/字符串面**" | `hasSelfProof` 扫描（`t('🔴 反例：…')` 也算） | ★ **✗ 不测"断言真会红"** —— 它**不是**"判据有效"的证明 | `report-gate-ledger.mjs` 的 `LEGEND`（**权威**）＋各件 `--selftest` |
| **探针（刀）** | 一次**最小变异**，要求该门**必红并点名** | `node scripts/probe-gates.mjs --probe=fast` | ✗ 不做"空对象双绿"那一向（见下条） | `scripts/probes.mjs`＋`--probe=fast` |
| **两把尺** | 尺一＝**造刀**（能红吗）／尺二＝**弄空对象**（红的时候在判什么） | 尺一：探针；尺二：弄坏/弄空一个输入件跑一次，看**互否的格子是否同时为真** | ✗ 两者**不互相替代**（尺一漏"红得与对象无关"，尺二漏"从不红"） | 本节 ＋ `criterion-design.md` 各节 |
| **接线** | 该段是否进 `npm test` | 台账"接线"列 | ★ **✗ 不等于"门成立"**（接线只说明"它在跑"）；也 ✗ 不表示"已装进 CI 的某个档" | 台账 + `test-plan.mjs` |

### 4.3 状态层（表格里的三种"没有"）
| 术语 | ① 是什么 | ② 怎么判 | ③ **不测什么** | ④ 权威在哪 |
|---|---|---|---|---|
| **不进 CI 运行（挂起）** | 对象**在**、**样本暂缺** ⇒ 从跑器里摘出并登记（★注意：**不是**"欠账迟早会跑" —— 它**整段不参与链**，直到撤挂） | `SUSPENDED`（每条要 `why` ＋ `until`） | ✗ 不是"下架/删除"（那是退役）✗ 不是"豁免" | `test-plan.mjs`（`SUSPENDED`） |
| **未判（`○`）** | 前提不成立 ⇒ **明说没判**（rc 不因它红） | 输出行以 `○` 开头 ＋ **点名哪一半未判、对象在哪** | ★ **✗ 不等于"绿"**，也 ✗ 不等于"红"；**✗ 不许静默** | 各门自己的前置分支（如零故事态） |
| **空面必红** | 判据的**面**为空 ⇒ 必须**出声**（✗ 不许静默绿） | 命中 0 对象时打印 `○ 未判：… 0 个 …` | ✗ 不是"让空面红"（红是给"判据失败"的；空面**出声**即可） | 各门的空面分支（如改制面 0 故事） |
| **欠账 vs 阶段** | **欠账＝要人去还**／**阶段＝会自己消失** | 挂起表要求 `missingSample`（欠账）；面用**存在性**表达（阶段） | ★ 两者**在计数上长得一样**（都是"N 个没绿"），但**寿命与责任完全不同** ✗ 混记会攒出**无人可还的账** | 本节 ＋ 挂起表校验 |

### 4.4 流程层
| 术语 | ① 是什么 | ② 怎么判 | ③ **不测什么** | ④ 权威在哪 |
|---|---|---|---|---|
| **检视项（checklist）** | **做不了硬牙、但有价值**的"人工走查"项 | `#1344` 的表：项｜**触发时机**｜读数｜谁看 | ✗ 不出红绿；✗ 不是"挂起的第二形态" | `#1344` |
| **读数** | 一次判定的输出，须**自带对象** | 形如「对象（仓/ref）＋ 面/量纲 ＋ 结论」 | ✗ 不带对象的读数**不能作为定案依据** | 本节（`#1315` 那组口径） |
| **票齐 ≠ 门成立** | 票数够 ≠ 该改动**成立** | 只有**锚在当前头**的票算数（`commit_id == headRefOid`） | ✗ "两人 APPROVED"不证明"CI 绿"或"判据有效" | `shared/merge.md`（规则仓）＋ 回读脚本 |

---

## 五、五条路（**改这类东西 ⇒ 读哪本、守哪几条、违约会怎样**）

**这一页解决什么**：以前"规矩没有正文"⇒ 只能"先写 ⇒ 被门咬 ⇒ 回头翻代码猜" ✗。
**每条 =〔为什么 ＋ 判法/锚 ＋ 违约红在哪〕**，且**从代码抽**（✗ 不另写一套）：正文一句话 ＋ **指向权威处** ⇒ 能核、能被咬 ✓。

### 路 L1：**新人入门**（这是什么、我怎么跑起来）
```
先读：根 `README.md`（对外首屏）→ 本页 §四（术语）→ `docs/manual/README.md` §1／§2（一个故事由什么组成／最小可跑故事）
再读：`scripts/module-order.mjs`（**层归属与模块顺序：代码即权威** ✓）· `docs/manual/README.md`（手册目录）
违约红在哪：✗ 无（读者路径不判红）；但**入口页有体量闸**：`md-format` 报「入口页 README.md 98 行（上限 120）」
```

#### L1 分步（✗ 别跳）
```
1. 先跑起来：读根 `README.md` 的「快速开始」⇒ `npm install` ⇒ `npm run build` ⇒ 打开 `dist/` 里的产物 ✓
2. 认识一个故事：`docs/manual/01`／`02`（段落图／最小可跑故事）⇒ 现有夹具 `test/fixtures/m3-chk-e2e/` 可当样本 ✓
3. 想改东西：按"改的是哪一类"对上 L2／L3／L4／L5（本节）✓
4. 卡住时：§四 术语（先统一说法）⇒ `scripts/module-order.mjs`（**层归属**：代码即权威）⇒ `TROUBLESHOOTING.md` ✓
```

### 路 L2：**写故事 / 写数据**
```
先读：`docs/manual/`（§3 散文面 · §5 条件行 · §6 状态与写点 · §7 位点检定 · §12 数据面）
再读：`docs/engine/json/README.md`（每类 JSON 放什么）· `docs/engine/authoring-model.md`
守：散文只写散文 ＋ `{{入参}}`；条件进条件行（✗ 不写 `<<if>>`）；值走声明面
违约红在哪：`test/prose-vocabulary.mjs`
  · 写了逻辑/表达式 ⇒ `[V1] … 正文里出现**逻辑/表达式**宏 <<set>>（段落「…」）—— 作者不写代码（甲-1）`
  · 写了未宣告宏 ⇒ `[V2] … 引擎未宣告的宏 <<goto>>`
  · **改制面**（故事有 `data/passages.json`）另有四条：`[P1]` 正文里出现链接 `[[…]]`（链接一律进 `links[]`）／
    `[P2]` `{{= …}}`（`{{}}` 只许入参名）／`[P3]` `{{名}}` 未在本段 `params` 声明（或与 `slot` 撞名）／
    `[P4]` `links[].to` 悬空 或 `args` 缺必填
```

#### L2 分步（写一段／加一个条件／加一次检定）
```
1. **定位段**：`stories/<slug>/passages/*.md`（一段一文件，`---` front-matter 给段名／tags）✓
2. **改散文**：只写散文 ＋ `{{入参}}`；✗ `<<if>>`／`<<set>>`（分支进**条件行**，值走**声明面**）⇒ 跑 `node test/prose-vocabulary.mjs`
3. **加分支/选项**：条件与可见性写进**数据**（今天的 `data/rules.json` 行；改制的目标是 `data/passages.json` 的 `links[]`）——
   两者**求值同一处权威**（`Sg.rules.matches`）⇒ ✗ 不另造表达式 ✓
4. **要盖新能力**：走 `docs/process/story-engine-loop.md`（需求 → 引擎侧判据 → 采用），✗ 别在散文里塞代码 ✓
5. **验**：`SG_STORIES_DIR=<故事根> npm run build` ⇒ 再用 `cases/` 里的用例跑（`case-run` 汇总行带分母）✓
```

### 路 L3：**改引擎（`src/**`、`editor/**`）**
```
先读：`docs/manual/06-state.md`（状态与写点 · **域表纪律**）· `docs/manual/13-build.md`（产出与发布 · 构建链）· `scripts/module-order.mjs`（层归属，代码即权威）
再读：`docs/engine/data-model.md` · `docs/engine/reference-spec.md` · `docs/engine/json/`
守：契约/存档语义不变；模块顺序与依赖显式；状态键落在某域
违约红在哪：
  · 分层的加载期依赖 ⇒ `test/layering.mjs`（模块图问题逐条点名）
  · 契约形状 ⇒ `test/contract-compat.mjs`（含"退出条件已成立而条目还在"也会红 ✓）
  · 状态键无归属 ⇒ `npm run audit -- --state --check` 报**未归属键**（实证：一次点名 8 个）
  · 产物新鲜度 ⇒ `scripts/dist-fresh.mjs`：「**找不到产物 `<distPath>`**…」＋`读数（对象）：distPath=…｜srcDir=…｜cwd=…`
```

#### L3 分步（改引擎）
```
1. **先读契约**：`docs/engine/data-model.md`（数据是什么）＋ `docs/engine/reference-spec.md`（形状）＋ **本页 §八**（代码级约定）
   §1 渲染路径契约／§2 构建与模块顺序／§3 状态契约／§4 状态域表 ✓
2. **判断影响面**：这一改会不会动**存档语义**／**渲染生命周期**／**状态键归属** ⇒ 对不上就先改设计（§一 总法则）✓
3. **加状态键**：必须在**每个故事**的 `data/tables.json` 域表里**找到归属**（引擎写点也如此）⇒ 跑 `npm run audit -- --state --check` ✓
4. **改完自检**：`node test/layering.mjs`（模块顺序/加载期依赖）＋ `node test/contract-compat.mjs`（退出条件也咬）✓
5. **构建产物**：改了 `src/**` ⇒ **清三层**（`<引擎>/build` ／ 夹具生成物 ／ `dist`）再 build ⇒ 否则读到**旧引擎** ✗（§六 6.4）✓
```

### 路 L4：**改门 / 判据（`test/**`、`scripts/**`）** ← **最常"猜"的一条**
```
先读：本节 §一～§二（判据设计总法则）＋ §三（怎么用这一页）→ §四（术语）→ `docs/criteria-ledger.md`（判据册）
守（四条，每条都有明确的"红在哪"）：
 ① **加一个段 ⇒ 六处登记**（缺一处就红，报文如下）
      · `SEGMENTS`（段本身）
      · `SUITE_MEMBERS`（分组表；**双向完备**）⇒ `validateSuites`：
        `组 engine 里的 \`<id>\` **不在计划里**（表漂了 ⇒ 要么补段、要么删条目）` ／
        `\`<id>\` **未归组**（计划里有、表里没有 ⇒ 完备性破了 ✗）`
      · `tier` ＋（若 `full`）`FULL_REASONS` 理由 ⇒ `validateTiers`：
        `\`<id>\` 标了 tier:'full' 但没写理由（加到 FULL_REASONS ✓ —— 降频必须留痕）`
      · audit 类：`AUDIT_ENGINE`／`AUDIT_STORY` 层表 ⇒ `validateLayers`：
        `未归层：计划里的 \`--zzz --check\` 段没有任何层（新增门请加进 AUDIT_ENGINE／AUDIT_STORY）` ／
        `僵尸层声明：waves 在层表里，但计划里没有对应段（删段时请同步层表）`
      · `inputs`：声明面要么 `['*']`（全跑型）要么给理由（台账"理由（仅登记/未接线必填）"列）
      · 探针：**有刀才算牙** ⇒ `scripts/probes.mjs` 加一条（`mutation` ＋ `expect`），预算见 `scripts/probe-budget.json`
 ② **判据要能假**：尺一＝造刀（探针 `--probe=fast`）；尺二＝弄空对象（互否的格子不许同时为真）
 ③ **面要说清**：判据判"哪些对象"（✗ 面不清 ⇒ 一落链就红，如旧形态故事对新判据）；**空面必须出声**
 ④ **锚要锚对层**：✗ 不锚"本次要变更的那一层"，✗ 不锚实现的中间产物（否则实现一改判据就红）
违约红在哪：见上面每条引号里的**报文原文**（都能在 `npm test` 的输出里原样看到）✓
可复跑：`npm test`｜`node scripts/probe-gates.mjs --probe=fast`｜`node scripts/report-gate-ledger.mjs --selftest`
```

### 路 L5：**发布 / 运维**
```
先读：`docs/manual/13-build.md`（产出与发布 · 用例门）· `docs/process/story-engine-loop.md`（pin 与跨仓纪律）
再读：`docs/gate-ledger.md`（**生成物** ✗ 不手改）· `docs/criteria-ledger.md`（判据册）
守：发布前跑用例门；引擎 pin 与 books 同笔；台账/生成物**重生成而不手改**
违约红在哪：
  · 台账/生成物不同步 ⇒ `node scripts/report-gate-ledger.mjs --check`（✗ 缺 `build/probe-results.json` 时**拒绝出数** ✓）
  · 用例门 ⇒ 发布工作流那一步（`case-run` 汇总行：绿／预期缺口／未归因 的条数）
  · 路径引用死链 ⇒ `md-format`：`引用的仓内路径**不存在** …（同名文件在别处 ⇒ 像是搬家后没跟）`
    ⚠️ **边界（我实测）**：它只报"**同名文件在别处**"这一类；**纯不存在的路径不报** ✗ ⇒ 见 §六
```

#### L5 分步（发布 / 运维）
```
1. **发布前**：`npm test`（PR 档）＋ 该跑的 `full` 档段（§六 6.4 的降频纪律：谁降频谁写理由）✓
2. **产物与用例门**：构建产物 ⇒ 跑 `cases/`（`case-run` 汇总行：绿／预期缺口／未归因 的条数，**带分母**）✓
3. **跨仓 pin**：引擎 ref 与 books 在**同一笔**里动（✗ 不同笔 ⇒ 下游读到"引擎与故事不匹配"）✓
4. **台账/生成物**：`npm run report:gates:update` **重生成**（✗ 不手改）；`--check` 缺 `build/probe-results.json` 时**拒绝出数** ✓
5. **事后**：把这次发布的读数（run 结论/用例门/线上冒烟）记进票面 —— **事实断言随附读数**（§六 6.2）✓
```

### 交叉引用（✗ 不另立体系）
```
本页是 `docs/criterion-design.md` 的**路标层**；判据设计的**法则**在 §一～§二，**术语**在 §四，**判据册**在 `docs/criteria-ledger.md`。
入口：根 `README.md`（对外）＋ `docs/README.md` §一（对内"按任务读"，本页是它的**展开版**）✓
```

## 六、通用纪律（**每条 = 为什么 ／ 判法·锚 ／ 违约红在哪**）

> 本节从**原 `docs/dev-conventions.md`**（代码级约定清单；★该件已按"一份为准"删除，内容即本节 §六/§七/§八 ✓）＋ `docs/dev-conventions-cases.md`（实证与踩坑）＋
> 今天的一手读数**抽取**而来（✗ 不重造）。每条都点到**能核的锚**；**"违约红在哪"引的是门里报文的原文** ✓

### 6.1 提交与 PR
| 纪律 | 为什么 | 判法·锚 | **违约红在哪** |
|---|---|---|---|
| **一票一分支，✗ 不直推主干** | 主干是审计基线；绕过评审＝**无留痕** | 规则仓 `shared/merge.md`（票数门槛）＋ PR 上的 `reviews` | 分支保护：直推被拒（`protected branch`）✗ |
| **票锚在"当前头"才算数** | 头一动票即失效（评审对象变了） | `pulls/N/reviews` 的 `commit_id` ⇔ `headRefOid` | 闸门按"锚在当前头"判 ⇒ 旧票**不算数**（我这边：`readback-review.sh` 直接打"锚在旧头（✗ 不算）"）✓ |
| **作者回避** | 自己不能给自己签收 | PR `author` ⇔ 投票人 | GitHub 硬拒：`Can not approve your own pull request` ✓ |
| **票齐 ≠ 门成立** | 票数不证明 CI 绿或判据有效 | 有效票数（锚在当前头）**且** CI 结论 | 闸门 fail-closed：`UNSTABLE`／`mergeable=UNKNOWN` ⇒ **不给出读数**（拒绝合，✗ 不猜）✓ |
| **未解异议不套锚点过滤** | 否则"推个新头"就成了丢异议的手段 ✗ | 该 reviewer 的**最新一张**是否为 `CHANGES_REQUESTED` | 闸门阻断合入（✗ 不看它锚在哪）✓ |
| **提交信息给票号与出处** | 半年后没人猜得出"为什么改" | `References #N` ＋ 引用的读数（命令＋输出＋**仓/ref**） | 无门（人读面）；但**事实断言**必须随附读数 —— 见 6.2 |

### 6.2 读数与断言（今天最贵的一组）
| 纪律 | 为什么 | 判法·锚 | **违约红在哪** |
|---|---|---|---|
| **读数须自带对象** | 不带对象的读数**不能定案**（"我量了"≠"我量对了"） | 形如〔**对象（仓/ref）** ＋ 面/量纲 ＋ 结论〕 | 我把旧树当 `main` 量过一次 ⇒ 数与结论都自洽、**对象错** ✗ ⇒ 审计类**一律读权威 ref**（`git show <ref>:<path>` 或先 ff ✓） |
| **"在不在"问三层** | 三层**读的是不同的面** | `ls`（磁盘，含未跟踪/被 ignore 残留）／`git ls-files`（**索引面**，随暂存态变）／`git show <ref>:<path>`（**那个 ref 的树**） | 只问一层 ⇒ 结论随暂存态变（今天实测：`git add` 前后同一条命令结果不同 ✗） |
| **计数要带面与量纲** | 不带面＝无法复核 | 分母／单位（字节 vs 字符／"N 件"vs"N 处"） | 我把 `String.length` 当字节报过一次 ✗；手册采用"分母 10／1"写法 ✓ |
| **事实断言随附可复跑读数** | 陈旧记忆会改写事实断言 | 命令 ＋ 输出 ＋ **仓/ref**（**照着它重跑一遍**＝第一步动作 ✓） | 无门（人读面）；反例：写"某路径已下架"却不给读数 ⇒ 后人无法复核 ✗（`#1345` 起按此口径写 ✓） |

### 6.3 豁免与留痕（✗ 豁免必须"有名有目"）
| 纪律 | 为什么 | 判法·锚 | **违约红在哪** |
|---|---|---|---|
| **路径豁免** | 历史叙述确需引用已消失的路径 | 同行写 `<!-- path-exempt: 理由 -->`（`md-format` 的 `PATH_EXEMPT`） | 不留痕 ⇒ `引用的仓内路径**不存在** …（同名文件在别处 ⇒ 像是搬家后没跟）` ✓ |
| **未跟踪豁免** | 新件尚未入库时不该被判"假绿" | 件内任一行 `untracked-exempt: <理由 ＋ 票号>`（`lib/untracked-guard.mjs` 的 `UNTRACKED_EXEMPT_MARKER`） | **理由与票号少任一项 ⇒ 豁免不生效** ⇒ `✗ 本次**未扫**（未跟踪 N 件落在本门扫描面内）… 那是**假绿**` ✓ |
| **去权威化** | 注释/文档里**不许拿"谁定的"当理由** | 三类 token ＋ `judge()`（`test/attribution-gate.mjs`） | 命中即红并给**可复制的替换建议** ✓（`#748` 合入 5 分钟内回潮 4 处，就是"靠人 grep"的代价 ✓） |
| **不许自指哈希** | 引用基线不引"本笔 sha"（提交哈希在写信息时还不存在） | 读数里的 ref 一律指**基线** ref ✓ | 无门（人读面）；反例＝写完就自相矛盾 ✗ |

### 6.4 变更与归档
| 纪律 | 为什么 | 判法·锚 | **违约红在哪** |
|---|---|---|---|
| **三落法（活/史料/归档）** | 不是所有旧件都该"搬进 archive" | 判据＝① **引用面**（有几处引它）② **是否半活**（有的节仍在使用） | 无门（人读面）；落法三选一：**移入 `docs/archive/`** ／ **原地加历史标注** ／ **删除** ✓（`#1331` 实测：9 处引用＋`§2.1` 仍在用 ⇒ 选"原地加标注" ✓） |
| **删改同笔清引用** | 否则留**悬空引用**（下一轮红得莫名其妙）✗ | 用**判据自己的扫描面**复核（✗ 不用自己的 grep） | `md-format`：`引用的仓内路径**不存在** …` ⇒ 实测案例：把件搬进 `archive/` 却漏改一处引用（`#1327`）✓ |
| **生成物不手改** | 手改会在下次重生成时丢失，且掩盖真源 | 生成物带 `@generated`／声明"**由 X 生成**" | 台账类：`✗ 台账文件不存在 → 跑 npm run report:gates:update`／不同步 ⇒ `--check` 红 ✓ |
| **降频必须留痕** | 把段移出 PR 档＝**降低了看护**，必须有人签字 | `tier:'full'` ⇒ `FULL_REASONS[id]` 必须有理由 | `validateTiers`：`` `<id>` 标了 tier:'full' 但没写理由（加到 FULL_REASONS ✓ —— 降频必须留痕） `` ✓ |
| **挂起（不进 CI 运行）** | 对象在、样本暂缺 ⇒ 不该当"失败"，也不该当"通过"（★**不进链** ＝ 该维今天**无看护**，✗ 不是"将来看护"） | `SUSPENDED[id]` 必须给 `why` ＋ `until` | 缺任一项 ⇒ 自证格红 ✓；且挂起条目**要写"why 何时失效"**（`why` 会过期 ✓ `#1333`） |
| **`missingSample`（欠账要写清）** | "迁到夹具"要机器可读地写出**缺哪个夹具** | `missingSample:` 值＝**缺引擎侧夹具：<形状>** 或 **缺可复跑命令：<命令＋期望读数>** | 值写不出／写了动作词／不可核对 ⇒ **机械判红** ✓（`#1315`：32 条里 38% 被这把尺筛出是归类错 ✓） |
| **阶段 ≠ 欠账** | 两者的**计数长得一样**，但"寿命与责任完全不同" | 门的**面**用**存在性**表达（如"有 `data/passages.json` 的故事"） | 混记 ⇒ 攒出**无人可还的账** ✗（无门；靠本节 ＋ 评审把关 ✓） |

### 6.5 门与链（**跑法与读数**）

> 抽自：手册 §14.4 ／ `#1364`（扫索引瞬态）／`docs/dev-conventions-cases.md` ㉖（"已跟踪状态下跑"）／`#1355`（入口无调用点）✓

| 纪律 | 为什么 | 判法·锚 | **违约红在哪** |
|---|---|---|---|
| **门一律在 `git add` 之后跑** | 有的门扫 `git ls-files`（**索引面**）⇒ `add` 前**不在扫面** ⇒ **假绿** ✗；反之把"面内有未跟踪件"当红报的 ⇒ **假红** ✗ | 顺序：`git add` ⇒ 门 ⇒ 链 | ★**实测**：`md-format` 在 `add` **之前**跑 ⇒ `rc=1`（假红，两次都撞）；同一棵树 `add` 之后连跑两遍 `rc=0` ✓ |
| **报"全过"必须带分母** | "全绿"不可复核：不知道共几条、跑的是哪一档 | 写 `N/N`（例：PR 档 **74/74**）；★档变了就重报（PR 档段数会随"出档"变：**86 → 74** ✓） | 无门（属**读数纪律**）⇒ 不带的读数**不能定案** ✓ |
| **偶发红先判"两次之间对象变没变"** | 不判就会当成"门不稳"而忽略真缺陷 ✗ | 同树**复跑两遍** ＋ 对照"单跑该段 rc" ＋ 查该段是否 `exclusive` | ★**实测**：链首跑 `73/74`（红＝`scripts-md-format-mjs`）⇒ 单跑该门 `rc=0` ⇒ **同树复跑两遍 74/74** ⇒ 属"**扫索引瞬态**"假红 ✗（不是本笔的缺陷）✓ |
| **"入口存在" ≠ "在链里跑"** | 自证入口写在件里、但**没有调用点** ⇒ 链里根本不跑它 ✗ | `grep -rn -- "--<入口>"` 看**除定义处外**是否还有调用点；台账看"接线"列 | ★**实测**：`test/layering.mjs --dist-fresh` 只在**定义处**出现 ⇒ 那三格**只有手跑才绿**；台账不把它算一"项" ⇒ 属"**接线遗漏（一行级）**"，踩"未接线必须写明理由"那条 ✓ |



### 6.6 门与判据的七条纪律

> 每条＝**〔为什么 ＋ 权威出处（真值：文件:行）＋ 违约红在哪〕** ✓；**行号取自 `origin/main`（本笔基线）**，✗ 不是记忆值。
> 与 §五 L4 的关系：**L4 讲"加一个段要登记哪几处"**；**本节讲"判据本身要守住哪七条"** ⇒ 互补 ✓。

**6.6.1 `inputs: ['*']`（全通配）须给机器可读的理由**
```
〔为什么〕"全跑型"等价于"**没有面**" ⇒ 实现可以悄悄跳过 ✗；写理由让"为什么全跑"**可审** ✓
〔权威〕`scripts/test-plan.mjs:712`（`INPUTS_WILDCARD_REASONS`）＋ `:802`（`validateInputsWildcardReasons()`）✓
〔违约红〕`✗ \`<id>\` 的 inputs 含**全通配**（去掉 \`*\` 后为空 ⇒ 等价「全跑型」✗）⇒ 须在 \`INPUTS_WILDCARD_REASONS\` 给出**机器可读的理由 ＋ 票号** ✗`（`:809`）✓
```

**6.6.2 仅登记／未接线必须写理由**
```
〔为什么〕"仅登记／未接线"＝"**看不出这门在跑**" ⇒ 不写理由 ⇒ 后人不知"谁在跑、何时跑" ✗
〔权威〕`scripts/report-gate-ledger.mjs:332`（`missing-reason`）✓
〔违约红〕`✗ <门> 仅登记但没写理由` ／ `✗ <门> 未接线但没写理由`（`report:gates:check` ⇒ 红 ✓）
```

**6.6.3 台账的**两条形态对账**（`form-drift` ／ `assertless-gate`）**
```
〔为什么〕① 台账说"仅登记"而门里**已有判定** ⇒ **形态升级未同步** ✗；② 门里**无判定**而台账未标"仅登记" ⇒ 它其实是**纯报告** ✗
〔权威〕`scripts/report-gate-ledger.mjs:336`（`form-drift`）／`:340`（`assertless-gate`）✓
〔违约红〕`✗ <门> 台账标「仅登记」，但门里已有判定路径（bad++/✗/exit(1)/failures.push）——形态升级未同步，请改标并复核理由` ／
          `✗ <门> 门里没有任何判定路径，但台账未标「仅登记」——它其实是纯报告` ✓
```

**6.6.4 两种"零"不同形（"机制在，样本不在" ≠ "机制不在"）**
```
〔为什么〕同一个"零"（如"零命中"）可能来自**两个前提**（前提不成立 vs 真为零）⇒ 混同 ⇒ **把"没跑"当"跑过且通过"** ✗
〔权威〕★真值出处＝**手册 §7.4**（`docs/manual/07-checks.md:37`「**两侧都齐 ≠ 闭环**」那条，本仓实证 ✓）＋ `#1112` 多例 ✓
   （★草稿原写"正文 §12 已有 `:597`" —— 我核过：**主干 `:597` 处没有该内容** ✗ ⇒ 已改为**手册 §7.4** ✓）
〔违约红〕**"未判"必须出声**（例：`○ 未判：… 0 个 …` ／ 台账 `读数补齐后再判结构（本次结构面未判）` ✓）；✗ **不静默判绿** ✓
```

**6.6.5 前置断言（先证"对象变成我要的样子"，再判效果）**
```
〔为什么〕不先证"负态真的生效" ⇒ 后面每条红/绿**都可能读的是别的对象** ✗（本仓 `#1341`／`#1351` 各实证一次 ✓）
〔权威〕样板＝`test/chk-source.mjs:111`（`② 前置：负态产物里写入行由 3 降到 2（证明补丁真进了构建）` ✓）；
   已成形态＝`test/codex-panel.mjs`／`test/layering.mjs` 等 ✓
〔违约红〕负态读数为红时**先看前置格**；★**前置没过 ⇒ 后面每一条红都不是证据**（而前置红本身是**高价值读数** ✓）
```

**6.6.6 锚面按"判什么"选（判能力 ⇔ 数据面；判内容 ⇔ 呈现面）**
```
〔为什么〕判"**引擎能不能**"（能力面）⇒ 锚**数据**（产物里有没有）；判"**用户能不能**"（呈现面）⇒ 锚**渲染输出**（DOM／文本）
   ⇒ 混用 ⇒ 判据判的不是它要判的那件事 ✗（本仓实例：`#1372` 的"值不许烘进 **body**"⇒ ✗ 不判整份产物 ✓）
〔权威〕`#1112`（多例 ✓；含手册 §7.4 那条"**两侧都齐 ≠ 闭环**"—— 命名面＋读取面齐了，**来源面**（谁写）仍空 ✗）
〔违约红〕判据要**写清它锚哪一层**（"抽的是 `tw-passagedata` 还是渲染后文本" ✓）；与"读数必须带层"同规 ✓
```

**6.6.7 降频（`tier: 'full'`）＝"**暂存**"，✗ 不当"永久降频"**
```
〔为什么〕降频的**代价**（PR 期不验证）必须留痕 ✓；但若**忘掉"待清理"** ⇒ 门在 full 里安静地跑、没人看 ⇒ **比没有更盲** ✗
〔权威〕`scripts/test-plan.mjs:538`（`validateTiers`）／`:556`（`full` 段必须有理由 ⇒ 红）＋ `FULL_REASONS` 表内逐条写"**本项属阶段二待清理**" ✓
〔违约红〕`✗ \`<id>\` 标了 tier:'full' 但没写理由（加到 FULL_REASONS ✓ —— 降频必须留痕）` ✓
```

**★本节验收（可判"进没进"）**：七条在正文里**各 ≥1 命中**（关键词＋换词两遍核 ✓）＋ `docs-read-path`／`md-format` rc=0 ＋ 链不红 ✓。

### 6.7 取证方式必须**可复算**（`#557` 的教训）

| 纪律 | 为什么 | 判法·锚 | **违约红在哪** |
|---|---|---|---|
| **取证必须可复算** | 不可复算的"证据"＝**声称**（下一次没人能验） | 一次取证＝**对象（仓/ref）＋ 命令 ＋ 输出**；★**同一次拍板的取证形状要一致**（否则两次结论不可比 ✗） | 无门 ⇒ ★**但它是"读数三件套"（6.3）的来源** ⇒ 违反它＝读数不可复核 ✓ |
| **读点两口径要分清** | 两个都叫"读点"会让统计**对不上** | `readKeys()`（**文本层**：字面出现的键）vs `literalReadKeys()`（**字面量层**：排除注释放行内的键）⇒ ★判"谁在读某键"时必须点名**用哪一把** ✓ | `scripts/audit.mjs` 的读点族（两口径各自的计数）⇒ 混用 ⇒ 数字对不上、归因错 ✗ |
来源：`dev-conventions.md` §13（沿革归案例册 ✓）✓

## 七、变更与归档的**操作清单**（照 6.1–6.5）

```
① 动手前：这一笔**改的是哪一类**（引擎／门·判据／文档／数据）⇒ 对上 §五 的那条路，先读它列的几本 ✓
② 改的时候：加段 ⇒ **六处登记**（§五 L4）；降频 ⇒ `FULL_REASONS`；豁免 ⇒ 留痕带**理由＋票号** ✓
③ 提交前：`node scripts/md-format.mjs` ／ `node test/docs-read-path.mjs` ／ `npm test`（两门 rc=0）✓
④ 提交后：**回读你刚做的动作**（分支/推送/票）—— "我执行了动作 ≠ 对象处于我以为的状态" ✓
⑤ 归档/删除：三落法 ＋ **同笔清引用**（用判据的面核，✗ 不用自己的 grep）✓
```
---

## 八、代码级约定（**抽取自原 `docs/dev-conventions.md`**；★该件已按"一份为准"删除 ⇒ 本节即其宿主 ✓）

> **本节按协调席裁的"逐条抽取清单"搬运**（`#1359` 的清单评论）：**只搬还活着的代码级纪律**，✗ 不搬沿革与个案 ✓。
> 每条＝**〔规则 ＋ 为什么 ＋ 判法/锚 ＋ ★违约红在哪〕**，并**指回权威处**（脚本／门／`ORDER`／常量表 ✓）。
> ★与手册的关系：**手册管"作者今天能怎么写"**；**本节管"引擎代码必须怎么长"** ⇒ 两者**互补**，交叉处给指针 ✓。

### 8.1 渲染路径契约 [锚]

```
规则：同页改写走 `<<replace>>` 这类**落 history moment** 的路径；✗ 不靠"改完当前活动状态"——
      因为 SugarCube 序列化的是 **history moment**，不落 moment 的改写**存档/读档后丢失** ✗
为什么：这不是洁癖，是**存档语义**（玩家读档后看到的东西必须与他当时看到的一致 ✓）
判法/锚：机检两条（缺一不可）——① 渲染路径白名单 ② **退出条件成立而条目还在**也报红 ✓
★违约红在哪：`test/layering.mjs` 家族的渲染路径那两条（超范围改写 ⇒ 点名）
来源：`dev-conventions.md` §1（规则／为什么／正确写法／机检）✓
```

### 8.2 构建与模块顺序：**单一权威 ＝ `scripts/module-order.mjs`** [锚]

```
规则：① 加载顺序**显式**声明在 `module-order.mjs` 的 `ORDER`（✗ 不靠文件名前缀隐含 ✓）
      ② **登记分两层**：引擎件（`src/**`）每个都必须登记 `ORDER`（并进 `MODULES`）；故事件由**它自己的清单**登记
      ③ 跨文件依赖只声明**加载期真实需要的边**（`MODULES[file].deps`）且**只能指向更早的模块**
      ④ `MODULES[file].defines` 声明该文件必须定义的顶层符号（`window.X` / `widget:X`）—— 抓改名与挪位置 ✓
      ⑤ ✗ **不往 `package.json` 的 `test` 脚本里塞 `&&`**（那里只有一行 `node scripts/run-tests.mjs` ✓）
为什么：靠文件名前缀 ⇒ 新件命名不对就**静默**先加载（构建不报错、运行期才炸 ✗）
★违约红在哪：`scripts/module-order.mjs` 的登记校验（未登记/顺序错/依赖指向后面 ⇒ 点名）＋ `test/layering.mjs`
来源：`dev-conventions.md` §2／§5 ✓
```

### 8.3 状态契约（**与手册 §6 互补**）[锚]

```
规则：① `pc.ev` / `pc.world` 的每个键必须落在 `Game.State.domains` 的**某一个**域（按 `prefix` 或 `keys` 匹配），**归属唯一**
      ② **只有写没有读**的键 ＝ 红；确属"**仅记账**"的键必须显式写进域表的 `note`
      ③ **只有读没有写** ＝ 红（**幽灵条件 ＝ 死分支**）
      ④ 扫描必须覆盖**三类动态形式**：`<<setflag>>`／`<<firstTime>>`（动态写并动态读）· `$pc.ev["k"]` · 表内谓词 `p.ev?.k`
★与手册的分工：**手册 §6** 讲"**作者面**的域表纪律 ＋ 7 个引擎保留槽"；本节讲**引擎侧的读写配平与扫描面** ✓
★违约红在哪：`npm run audit -- --state --check` 报 `未落入任何域`／`只有写没读`／`只有读没写`（点名键 ✓）
来源：`dev-conventions.md` §3（＋`§12.6` 域表归属纪律，见 8.9）✓
```

### 8.4 常量与字面量 [锚]

```
规则：① **时代**一律 `Game.Era.PAST` / `Game.Era.PRESENT`（条件／赋值／模板都用常量）；
         字面量 `'past'`/`'present'` 只允许出现在数据字段（`era:`／`flagEra:`）与 `Game.Era` 的定义行
      ② **伤害**一律 `Game.Damage.*`（`graze 1`／`hurt 2`／`hard 3`／`heavy 4`／`lethal 99`）；剧情文件禁**裸** `<<damage N>>`
      ③ ★**宏的裸词参数会被当成字符串** ⇒ 必须写 **backtick 表达式**：`<<damage \`Game.Damage.lethal\`>>`
         （写成 `<<damage Game.Damage.lethal>>` 会**静默传字符串** ⇒ `hp` 变 `NaN` ✗ —— 实测踩过 ✓）
★违约红在哪：`test/integrity.mjs`（常量与字面量那一族）＋ 渲染覆盖（缺元素 ⇒ 抓住 `NaN` 那类 ✓）
来源：`dev-conventions.md` §4（含"坑11"实测）✓
```

### 8.5 断言口径：**信息可见** vs **键盘可续** [锚]

```
规则：就地反馈（`<<replace>>`／结果槽）的断言**拆成两轴**，✗ 不合成一条、✗ 不绑定具名元素：
  | 维度 | 该验什么 | **不要**验什么 |
  | 信息可见 | 「本次内容**在屏**」（槽存在、未 `hidden`、文本含本次内容） | ✗ 不要用"焦点在哪"当可见性证据 |
  | 键盘可续 | 「**焦点仍在 `#passages` 内**」（`activeElement.closest('#passages')`） | ✗ 不要绑定"焦点落在某个具体元素" |
为什么：焦点落在**哪一个**元素是实现路径（仓里三条：就地反馈定位／子对话返回行动区／丢焦回收）⇒ 绑死它＝**假红**
★咬合力（改测试时照做）：**只关掉"丢焦回收"不该红**（那是安全网非契约）；**关掉内容本身必须红** ✓
★违约红在哪：该族测试自身（渲染/A11y 族）—— 断言写成绑死元素 ⇒ 一改实现就假红 ✓
★**同族的实例（✗ 不重述，只给指针）**：引擎侧实现里的那条口径写在 `src/80-script.twee` 的"导航型交互后送回焦点"块
（`:518` 起）—— 它把**同样的两条件**（焦点本来在正文里 ＋ 替换后确实丢了）写成代码，并明写"**断言不得绑元素**" ✓
来源：`dev-conventions.md` §6 ✓
```

### 8.6 全局命名空间 [锚]

```
规则：| 挂什么 | 挂到哪 |
      | 数据表／规则／形状 | `Game.*`（按域分子空间：`Game.Checks / Rules / Pc / Items / Social / Combat / Dragon / Star / Truth / Codex …`）|
      | UI 与运行时 | `Sg.*`（`Sg.UI` / `Sg.Codex` / `Sg.Ending` / `Sg.save.*`）|
      | **裸全局** | ❌ **不新增**（历史例外在白名单里，逐个迁移，**迁移完就从白名单删掉** ✓）|
★违约红在哪：`test/globals.mjs`——A1 新裸全局 ⇒ 红；**A2 白名单腐烂**（白名单里有、源码里没了）⇒ 红（✗ 替幻影名字放行 ✓）
  ★扫描只认**赋值**（`window.X =`）；`===`／`==`／`+=` 与注释都不算 ✓
来源：`dev-conventions.md` §7 ✓
```

### 8.7 定位与断言分离：选项用**稳定 key** [锚]

```
规则：测试里**定位**一个选项用**目标段落名派生 key**（`data-choice`），✗ 不用中文文案；**文案只用于断言**：
      · 定位：`click(key)` / `clickByKey(key)` / `byKey(key)`（歧义处给容器加 `data-key="…"` 显式覆盖）
      · 断言：文案／状态（`text().includes('…')`、`passage()`、`pc()`）✓
为什么：文案是**最常改**的东西，而它曾是**定位键** ⇒ 实测改一句文案连动 **5 处**测试调用点 ✗
★违约红在哪：用例族（改文案后跑用例 ⇒ 定位失败会点名；而按 key 定位则不受影响 ✓）
来源：`dev-conventions.md` §8（＋`#317②`）✓
```

### 8.8 `Codex` / 条件键形 / `yields` 形状（**作者面数据纪律，与手册 §5／§8 互补**）[锚]

```
① **条件语言的键形**：`req`/`any`/`exclude` 的键形 ＝ `n_*`（note id）／裸键名／`ev.x`／`world.x`（状态路径）＋ **两种前缀形**：
   **`inv:<道具>`**（持有物）· **`era:<时代>`**（`past`/`present`）
   ★**前缀必须由引擎宣告**（`Sg.rules.prefixes`，与引擎侧 `holds()` 的求值能力**一一对应**）
   ★**为什么**：引擎不认的前缀会让条件**永假** ⇒ 行**静默死掉**（最坏那种 bug ✗）
   ★**前缀键不参与状态契约**（`--state`）—— 它们是**求值词汇**，不是状态键 ✓
② **`text` 里的分支**：判定结果 → 两段文案（同一次拍板）⇒ ✗ 不在 `text` 里写表达式
③ **授予家族三类**：`yields`（笔记，幂等）／`gives`（道具，幂等）／`sets`（**只置真**）；★**有笔记的键不得走 `sets`**
④ **键形与命名空间的边界**：前缀键 ✗ 不属于 `pc.ev`／`pc.world`；域表**不登记**它们 ✓
★与手册的分工：**手册 §5** 给字段表（"是什么"）；**本节**给**键形与命名空间的边界**（"到哪为止"）✓
★违约红在哪：`scripts/audit.mjs`（条件项/前缀宣告）＋ `--state`（前缀键若被登记 ⇒ 露馅 ✓）
来源：`dev-conventions.md` §11（含 4 个子节）✓
```

### 8.9 故事作用域与域表归属 [锚]

```
① **分析宇宙**：故事门的受判面 ＝ **引擎文件 ∪ 本故事文件**（`scopedFiles(story)`）—— ✗ 不是"全仓"、✗ 不是"只本故事"
② **条件项两种形状**：字符串键 ／ **对象算子**（门侧先就位 ✓）
③ **`yields` 两种形状**：note id ／ `{ id, path }`（多源笔记的**路径选择**）✓
④ **域表归属纪律（`#660` 片三-4）**：**引擎不声明域**；**引擎写点必须在每个故事的域表里找到归属**（新增引擎写点 ⇒ 同步各故事域表）；
   **读侧口径**：**经历走键、知识走笔记**（引擎不得把"知识"写进 `pc.ev.*` ✓）
⑤ **边界机检三档**：文档路径／层间依赖／作用域——**三档各自可判**（✗ 不合成一档含糊的"边界检查" ✓）
★**这条纪律是可机检的**：`test/story-runtime.mjs` 的 `[域表纪律]` 段钉住（每故事两条断言：作用域 ⊇ 引擎文件 · 引擎写点全有归属 ✓）
  ★**反向探针两条都验过**：① 删某故事的 `runtime` 域 ⇒ `--state` 点名 `last_roll`/`last_result`/`ending`/`fight` 未落入任何域 ✓
★违约红在哪：`--state`／`test/story-runtime.mjs`／`test/contract-compat.mjs`
来源：`dev-conventions.md` §12／§12.5／§12.6 ✓
```


### 8.9b 故事文本源与**归属面**（单一权威 ＝ `storyText()`）[锚]

```
**单一权威**：`scripts/audit/lib/shared.mjs` 的 **`storyText()`** —— `段落名 → 故事文本`，其中
  **故事文本 ＝ 内容段落原文 ∪ 归属到它的条件表行 `text`**（**归属** ＝ 行 `scope` 的 `#` 前那一截；多行按**表序**追加 ✓）
规则（三条）：
  ① 凡判**正文**的门（锚句／禁词／代词／传说线／措辞密度／总字／位点…）一律从 **`storyText().text`** 取文，
     ✗ **不再**直接 `passageSrc.get(…)`（否则"表行里的文本"在判据面上**看不见** ✗）
  ② 凡判**条件**的门，除段落里的 `<<if>>` 外还要认**表行的 `req`／`any`／`exclude`**
     （范本：`--echoes` 的 `rowCondsOf()` —— 表侧条件与段侧条件**同一处读** ✓）
  ③ 表行归属到**不存在的段落** ⇒ **不静默**：`--rules` **判红**（`scope` 是**结构**、不是备注 ✗）；
     `storyText().orphans` 交调用方报告 ✓
★**为什么**（实测）：阶段 4 之后"这段文本属于哪个段落"由**归属**决定 —— 一次搬家**同时红六道门**
  （`--truth`／`--echoes`／`--npc`／`--notes`／`--interact`／`--text`）⇒ ★那不是六个 bug，是**一个横切面**（**门的文本面窄了** ✓）
★**已接线的面**：`truth`·`echoes`·`npc`·`notes`·`interact`·`text`·`choices`·`craft`·`canon`·`systems`·`investment` …（都走 `storyText()` ✓）
★**已知边界（如实写明）**：`--investment` G3 的「登记合龙门」**条件面仍只在段落里找**（等它真搬进表时再接 ✓）
★与 8.9 的分工：**8.9 管"域表归属"（状态键落在哪个域）**；**本节管"文本归属"（表行文本归属到哪个段落）** ⇒ 两件事，**互补** ✓
★违约红在哪：判正文的门若绕过 `storyText()` ⇒ 会**漏掉表行文本**（搬家时表现为"多门同时红" ✗）；表行归属到不存在的段落 ⇒ `--rules` 判红 ✓
来源：`dev-conventions.md` §11（沿革归案例册 ✓）

> ★**顺记一条门的边界（✗ 不是本节的判据）**：`md-format` 的围栏面**只判"总数奇偶"**（＝"**配平**"），
> **✗ 不判"配对"**（错位一位仍可能成对）—— 它**已算出**"被吞标题"这个症状，但那是**诊断、不是判据**（只报不红）✓
> ⇒ 所以"**绿灯 ≠ 结构对**"；本仓实证：`8.16` 缺闭合而"多出来的闭"落在 `8.17` 头上 ⇒ **总数仍偶、门过**，只是 `8.17` 标题被吞 ✗
> （定位那把尺＝**逐节配平** ✓；"把被吞标题升为判据"的提议已记 `#1112` ✓）
```

### 8.10 调试开关：只许「构建期 / URL 参数」，**不进玩家 UI** [锚]

```
规则：调试开关只许两种形态——**构建期**（构建参数）／**URL 参数**（如 `?seed=123&pool=short,long,chest`）；**✗ 不进玩家 UI** ✓
为什么：S6 的分布/路径验收需要**确定性证据**（否则只能靠多种子复跑猜 ✓）；而这类开关**一旦进了 UI 就变成玩家可见的功能契约** ✗
★违约红在哪：该族门（UI 面）—— 玩家可见 UI 里出现调试开关 ⇒ 点名 ✓
来源：`dev-conventions.md` §14（＋`#491` 判据 10）✓
```

### 8.11 仪器 vs 规格：**随机游走不是门** [锚]

```
判据：门禁只放两种东西——① **可枚举的覆盖**（例：按 `$era` 分叉的段落逐条覆盖）② **可机检的规格**
      （场景 × 道具/线索集合 → 期望：可见选项 key 集合／状态增量／去向段落/结局 ✓）
      **随机游走**两者都不是：它断言的是"**这 N 条抽样路径上**没崩、不变量没破" ⇒ 绿 ＝ **没抽到**问题，✗ 不是"没有问题" ✓
★违约红在哪：不适用（这条管"**该不该上门**"的判定，见 §二之二"判定线" ✓）—— 若把随机游走当门 ⇒ 覆盖缺口被"绿"掩盖 ✗
来源：`dev-conventions.md` §15（沿革链归案例册 ✓）✓
```

### 8.12 判定抽纯 · 执行分离 · 假件驱动 · **必须能红** · 读数指名来源 [锚]

```
（"同一条路"口径 —— ★与 §六 的"读数三件套"**互为表里**：本节讲**代码怎么切**，§六讲**读数怎么写** ✓）
① **判定抽纯**：把"判"从"跑"里剥出来（纯函数可注入、可在 node 里驱动 ⇒ ✗ 不必进浏览器 ✓）
   ★**本仓实例**：`src/10-core.twee` 的**随机源唯一取值口**（`:35` 起）—— "为什么要有它"＋纪律「**不得直调 `Math.random()`**」
   ＋**三态注入方式**（浏览器/jsdom 走 SugarCube `random()`；node 侧 `Game.Rules.rng.set(...)` ✓）⇒ 这就是"**判定抽纯**"的落地样板 ✓
② **执行分离**：算（sim）与呈现（present）分开；✗ 不把"写状态 ＋ 渲染"挤在一个 widget 里 ✓
③ **假件驱动**：判据用**最小假件**驱动（注入式），✗ 不依赖真实产物 ✓
④ **必须能红**：每条判据都要能说出"**它红的时候在判什么**"（两把尺：造刀 ＋ 弄空对象 ✓）
⑤ **读数指名来源**：产哪棵树、哪个头、哪条命令 ✓
★违约红在哪：见 §五 L4（"加段六处登记"的报文原文）＋ §六 6.3（读数三件套）✓
来源：`dev-conventions.md` §17（其中"**一批一 PR**"已并入 §七；"**内联只许引用形状**"见 §八.13；"**证伪清单**"移交案例册 ✓）
```

### 8.13 内联只许引用形状（`#1036` 甲 · `#1048` 落地）[锚]

```
规则：散文里的内联扩展**只许"引用形状"**（值／宏的引用），✗ 不许表达式、✗ 不许逻辑 ✓
★与改制的关系：`#1234` 把这条**收紧**为"散文＝纯模板（零链接、`{{}}` 只认本段 `params`）" ⇒ 以那份冻结为准（见 §五 L2 与手册 §12 ✓）
★违约红在哪：`test/prose-vocabulary.mjs`（`[V2]` 未宣告宏／`[P1]`–`[P4]` 改制面四条 ✓）
来源：`dev-conventions.md` §17 子节 ✓
```

**★本节自检**（✗ 不靠"我记得搬过了"）：每条都①有**规则** ②有**为什么** ③有**判法/锚（或明确"不适用"）** ④有**违约红在哪的出处** ⑤有**来源节号** ✓；
**与手册/§六/§七的交叉处都给了指针**（✗ 不留两份真源 ✓）。
### 8.14 CI 测试计划与并行跑器：**单一权威 ＝ `test-plan.mjs` 的 `SEGMENTS`** [锚]

```
规则：① 「CI 跑哪些段」**只有一处定义** ＝ `scripts/test-plan.mjs` 的 `SEGMENTS`；
      ✗ **不要**再往 `package.json` 的 `test` 脚本里塞 `&&`（那里只有一行 `node scripts/run-tests.mjs` ✓）
      ② 段的字段：`id`（稳定标识，`--only=<子串>` 用）／`phase`（`build` 段**先跑且独占**，其余可并行）／
         `cost`（本机实测秒数，**仅供打印与预估**，✗ 不参与判定）／`needs`（**前序段的产物依赖**：本段要读某段落盘的产物就写它）／
         `cmd`（与旧链**逐字一致**的 shell 命令 ✓）
③ **跑器**：`scripts/run-tests.mjs`（`npm test` 并行，默认 jobs ＝ min(8, 核数)）
★为什么：两处定义 ⇒ 必然分叉（"计划里没有的段"与"计划里有但没人跑"两种情况都会发生 ✗）
★违约红在哪：台账/计划校验族（`validateSuites` 的「**未归组**」「组里 `<id>` **不在计划里**」／`validateLayers` 的「**未归层**」「**僵尸层声明**」）
  ＋ **加一个段 ⇒ 六处登记**（见 §五 L4 的报文原文 ✓）
★与 §五 L4 的分工：**L4 讲"加段要登记哪几处"**；**本节讲"为什么只有一个权威 ＋ 段的字段语义"** ✓
来源：`dev-conventions.md` §5 ✓
```

### 8.15 设定集忠实性：**不入门禁**，由**票工作流**保证 [锚]

```
裁定：「正文是否忠实于设定集」**不进 CI 门禁**（✗ 不放门）⇒ 改由**票（issue）工作流**保证：
  ① 凡**改故事正文或条件表**（`stories/<slug>/**`）的票 ⇒ **票内点名**本次触及的设定面 ✓
  ② **按票核对**：设定条目 ↔ 文本是否一致 ✓
  ③ 与设定**冲突**的改动 ⇒ **票内显式说明并请裁**（✗ **不许静默改** ✓）
★**为什么不能放门禁**（判据面的理由）：这一维要成立**必须有一个"忠于设定集的内容样本"**；当内容故事被裁掉之后，
  **端到端面已没有对象** ⇒ 放门只会得到一个**恒真格**（比没有更坏 ✗）
★**仍然在册的部分**：判定**机制**（**5 条合成反例**）**随 `npm test` 跑** ⇒ **机制不腐**，缺的只是**端到端样本** ✓
  ⇒ 这正是"**两种'零'不同形**"的应用：**"机制在，样本不在"**必须说出来（✗ 不静默放过 ✓）
★违约红在哪：不适用（**不放门**）；但机制那 5 条合成反例**在链里跑** ⇒ 机制坏了会红 ✓
来源：`dev-conventions.md` §11.5（沿革"为什么不能放门禁"归案例册 ✓）
```

### 8.16 CI 作业分层：**只改文档就不跑真机/确定性面**（`soak` 跳过）[锚]

```
规则：一个 PR 的**全部**改动文件都落在**文档面**（`docs/**` 或任意 `*.md`）⇒ `soak` job **跳过**；否则照跑 ✓
      判据由 `changes` job 给出（`gh api …/pulls/N/files`，**无 checkout**，≈3-5s）；★跳过的理由**留在 run 页上**
      （`soak: skipped` ＋ `changes` 的输出）⇒ **✗ 不做静默降级** ✓
★**两条硬边界**（✗ 都不许放宽）：
      ① **失败开向 ＝ 照跑**：算不出改动面（API 失败／空列表）⇒ `code=true`，**✗ 不静默少跑** ✓
      ② **`push`（main）恒 `code=true`**：`deploy` 依赖 soak ⇒ **main 上不省这一段** ⇒ 发布前的全量验证不变 ✓
★**有意不缩覆盖**：soak 的局数与「位点双支清扫」照旧（双支是**确定性定向扫描**，与局数无关 ✓）
★**为什么**：实测纯文档 PR 的 run 仍 **167s**，其中 soak **124s**（walker 60s ＋ 真机浏览器 24s ＋ playwright 装机 19s ＋ build 6s），
  对文档改动**零价值**；而 `npm test`（98s，含文档相关门）**照跑** ⇒ "文档改动"这件事**并没有失去保护** ✓
★**顺带两条否定结果**（实测过，✗ 避免重复走）：① 调度改 **LPT**（最长段优先）无效（FCFS 81.3s vs LPT 84.3s ✗）；
  ② "每段各自 boot JSDOM"不是大头（boot 0.6s/次 ＋ `newGame` 1.0s ⇒ 16 段 ≈24s）⇒ 重构测试基建**不成比例** ✗
★违约红在哪：**不静默降级**由 run 页的 `soak: skipped` ＋ `changes` 输出保证（✗ 无裸跳过）✓
来源：`dev-conventions.md` §10 ✓
```

### 8.17 去权威化：注释／文档**不写"谁定的"** [锚]

```
规则：注释与文档里**不许拿"谁定的"当理由**——要写**理由本身** ✓（**解释性引用也一样**：引原句、引票号可以，✗ 不写"某某说"）
为什么：不是洁癖 —— ① **可读性**（读者要的是判据，不是人事）② **实测回潮**（这种写法会反复出现 ✓）
★判法/锚：`test/attribution-gate.mjs` —— 三类 token ＋ `judge()`；★**命中即红**并给**可复制的改法** ✓
★这条**正对着本仓既有的门**（`attribution-gate` 就是它的对象 ✓）⇒ ✗ 不是"建议"，是**门在守的东西** ✓
来源：`dev-conventions.md` §16（＋`#748` 口径 · `#752` 门）✓
```

### 8.18 自证（`--selftest`）的**有效性条件**（判据面）[锚]

```
（★与 §四术语「**自证**」互为表里：术语表说"**它不测什么**"（✗ 不测"它真会红"）；本节说"**它要成立还缺什么**" ✓）
规则（有效性条件，缺一不可）：
  ① **失败必须计入退出码**（`--check` 时退 1）—— 否则自证只是"打印了一行" ✗
  ② **失败路径不能崩**（✗ 否则"该判红"变成栈回溯，更糟：可能被当成环境问题忽略 ✓）
  ③ **计数器声明必须早于任何使用**（TDZ）
  ④ **抽纯函数时方法不要裸传**（`f(C.pick)` 会丢 `this` ⇒ 门直接崩）⇒ 包一层 `(…args) => C.pick(…args)` ✓
  ⑤ **注入式判据**：判据住在共享分类器/工具里时，给该工具**可选注入参数**（默认取真实来源 ⇒ 向后兼容 ✓）
      —— ✗ 否则等于"**用被测对象证明被测对象**" ✓
  ⑥ **判据变更 ≠ 补自证**：✗ 不在"补自证"的 PR 里顺手改语义（先用一条自证把**现有语义**钉住并写明观察，改动另开一笔 ✓）
  ⑦ **零边际断言要删**：新增用例**必须能改变"至少一种错法下的红数"**，否则它只是仪式（还会抬高维护面 ✗）
★**机器兜住的部分**：`scripts/report-selftest-validity.mjs` 静态扫描 `scripts/`＋`test/` ⇒ 出现 `自证·` 就必须有
  「被增量的计数器进了可达 `process.exit` 的条件」；任何 `X++`／`X += …` 的声明必须早于使用（置后按**警告**报）✓
  ★要绕过得写进脚本的 `WHITELIST` **并写理由**（**白名单腐烂会报红** ✓）
★违约红在哪：自证族＋ `report-selftest-validity`（静态扫描）＋ §二第 7 条（自证必须能红且计入退出码 ✓）
来源：`dev-conventions.md` §9（沿革与个案归案例册 ＋ `criteria-ledger.md` ✓）
```
