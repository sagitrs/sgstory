:: 门厅取物 [widget]
/% 归属（#460）：**故事 1 的取物结果**（哨子/坏哨/whistle_taken）原本住在引擎 `10-core` 的 `Widgets` 段里
   —— 引擎不该知道"哨子"是什么（第二/第三故事跑 `--state`/`--consequences` 会因此**假红**）。
   本文件把它搬回故事侧；调用点仍是 `40-ch2.twee` 的 `门厅`（`<<hallResult "翻检">>`）。
   #185：取物结果单源；replace 原地重绘，goto 不再冲掉结果 %/
/% #403：门厅取物——**同一颗骰只显示一遍、同一句结果只显示一遍**。
   ① 去掉原先开头的 <<lastcheck>>：骰面由 <<sitecheck>> 自己渲染（结果槽再承载一次进 dist），
      两句都渲染就会在槽里出现两次骰面；
   ② 结果句在「结果槽已承载本次结果」时不再复述（槽由 #270 的 goto 包装捕获 link 输出）；
   ③ 状态变更（give/damage/setflag）**与显示无关，始终执行**。 %/
<<widget "hallResult">><<nobr>>
	<<set _slotHasIt to $pc.ev.last_result and $pc.ev.last_result.p is passage()>>
	<<if $args[0] is "翻检">>
		<<if $last_check.success>>
			<<if not _slotHasIt>>你把哨子从钉子上提起来，收进行囊，可在''物品栏''查看。<</if>><<give "坏哨">><<setflag "whistle_taken">>
		<<else>>
			<<if not _slotHasIt>>哨子卡在钉子上。你拨了半天，指头刮破了一道口子——到底还是弄下来了。<</if>><<damage `Game.Damage.graze`>><<give "坏哨">><<setflag "whistle_taken">>
		<</if>>
	<<else>>
		<<if $last_check.success>>
			<<if not _slotHasIt>>你借了箱子的高度，一把攥住哨子拧了下来。<</if>><<give "坏哨">><<setflag "whistle_taken">>
		<<else>>
			<<if not _slotHasIt>>箱子一滑，你磕在墙角上。好歹把哨子攥下来了。<</if>><<damage `Game.Damage.hurt`>><<give "坏哨">><<setflag "whistle_taken">>
		<</if>>
	<</if>>
	/% 「这里你已经翻找过了」由**门厅段落自己的 else 支**渲染（单源）——放在 widget 里会被 #270 结果槽连同 link 输出一起捕获，于是同一句在槽与段落各显示一遍（#403）。 %/
<</nobr>><</widget>>


:: 时代翻转 [widget]
/% 翻转时代（v16 §3.3）：原地生效、位置决定年代；耗隐藏星力；从过去回来时雾淡一些 %/
<<widget "flip">><<nobr>>
	<<if $pc.world.fog_thin>>
		/% #291 I1-G1：现象层钩子——跑得多了，雾的变化要能被感觉到（不显示任何数字） %/
		<<if $pc.star.spent gte 3>>
			<div><span class="flip-note">你回到现在。雾比上回更''薄了一层''，林子里的声音听得清楚了些。</span></div>
		<<else>>
			<div><span class="flip-note">你回到现在。身边的雾，''淡了一些''。</span></div>
		<</if>>
		<<set $pc.world.fog_thin to false>>
	<</if>>
	<<if not $pc.inv["时光护符"]>>
		<div><span class="era-drained">你身上没有能校进另一段时间的东西。</span></div>
	<<else>>
		<<if $pc.star.spent gte 3>>
			<div><span class="sys-note">（你翻得越来越熟了。可每次回来，身上都像被抽走了一点点什么。）</span></div>
		<</if>>
		<<link `($era is Game.Era.PRESENT ? "翻转护身符：坠入『过去』" : "翻转护身符：回到『现在』")`>>
			<<if not $pc.star.first_free>>
				<<set $pc.star.first_free to true>>   /% #219 C1②：头一次翻转不收费——造它的人留的一点余量 %/
			<<else>>
				<<set $pc.star.spent to $pc.star.spent + 1>>
			<</if>>
			<<set $era to ($era is Game.Era.PRESENT ? Game.Era.PAST : Game.Era.PRESENT)>>
			<<if $era is Game.Era.PRESENT>><<set $pc.world.fog_thin to true>><</if>>
			<<goto `passage()`>>
		<</link>>
	<</if>>
<</nobr>><</widget>>
