# sgstory 实施图（impl-map）——M1 骨架落地规范

> **状态**：v2.7（**M1 骨架 + M2–M4 文案 + M5 终局 + M6a–f 打磨 + M5c–M5g 口径 + M7 v17 全案（去神秘化 / 传说层 / 好感合一 / 封印战）+ M8 图鉴 + M9 互动化 + M10 判定可见 + M11 A 组 + M12 B1 战斗动作池 + M13 B2 交涉 + M14 温室撤除与月光花改口径 + M15 结局页收尾 + M16 序章与回指口径** 已完成；v2.7·D **文档对齐**：门数/路线数/段落数/覆盖基线/键数全对着代码核过一遍）
> **定位**：`docs/game-outline.md`（玩法大纲）的**工程落地版**——把大纲翻译成**段落图 / 文件结构 / 状态模型 / 测试策略**。
> **权威关系**：实现细节稿。**与设定书冲突以设定书为准**；与大纲冲突以大纲为准。
> **读法**：§0 的里程碑表是**施工记录**——表里的门数/路线数/段落数都是**当时值**；**现状**看 §2（状态）、§3（段落图）、§5（测试策略）与 §5 末尾的「当前规模」。

---

## 0. M1 切片计划

| 切片 | 内容 | 验收 | 状态 |
|---|---|---|---|
| **M1a-1** | 实施图 + `audit.mjs` 去硬编码（文件发现式） | `npm test` 绿 | ✅ #127 |
| **M1a-2** | **原子换骨**：新 `src/**`（11 文件 / 68 内容段 → 现 66 段；`封印·涂毒`/`封印·冲` 在 M12 并入动作池后退休）+ 新状态模型 + 命题集 v1 锚句在位 + 测试套重写 | `npm test` 绿 · `npm run soak` 0 违法 | ✅ 本 PR |
| **M1b** | canon 门（`audit --canon` 读设定书 §10 黑名单回流检测 + 行覆盖） | 六门绿 | ✅ #129 |
| **M1c** | 覆盖率 ratchet 收紧（5 门）+ 场景矩阵扩展（20 路线）+ `npm run soak` 接回 CI | 覆盖率门 + CI soak job | ✅ 本 PR |
| **M2** | 序章 + 一章文案（雾影·龙梦回放 / 酒馆四传说 / 女巫半知交底 / 翻转教学） | 六门 + 20 路线 | ✅ 本 PR |
| **M3** | 二章文案（塔门翻转教学 / 雾之魔物 / 守林人证物分层 / 塔内五层 + 顶楼 / 过去侧氛围） | 六门 + 20 路线 | ✅ 本 PR |
| **M4** | 三章文案（证据链八节点：质询/取图/换哨/喂花/告知/交付/唤醒/归位） | 六门 + 20 路线 | ✅ 本 PR |
| **M5a** | 结局文案（10 出口收束） | 六门 + 20 路线 | ✅ 本 PR |
| **M5b** | boss 五打法口径（**Q1/Q2 拍板**）：单挑彩蛋需天然 20 + 劣势（≈0.25%）· 移除优势道具 · 败给龙＝死亡 | 六门 + `audit --dragon` 彩蛋率门 | ✅ 本 PR |
| **M6a** | canon 门扩为「§10 回流 + §9 双读断言」 | 六门 + 覆盖门 | ✅ #137 |
| **M6b** | 文案加量（设定集补塔/术语页 + 各章质感）· 内容段落 60 → 62 | 六门 + 覆盖门 | ✅ #138 |
| **M6c** | 塔内三楼「过去」分支（书房/温室/工坊）+ 各章质感 · 时代分叉 7 → 10 段 | 六门 + 覆盖门 | ✅ #139 |
| **M6d** | 情报优势接线（`Game.Checks.knowledge`：老猎人的话 → 洞穴·战斗双骰取高）+ 术语页 | 六门 + 规则单测 | ✅ #140 |
| **M6e** | 情报表归一（`书房·检视` ← `witch_hint` 由表驱动，正文删手写 adv）+ 表一致性单测 | 六门 + 规则单测 | ✅ 本 PR |
| **M6f** | 文案加量：正文 10756 → **11752** 字（序章/一章/二章/三章/结局/设定集/车卡） | 六门 + 覆盖门 | ✅ 本 PR |
| **M5c** | 月光花去功能化（canon 补正 #5）+ **花田昏睡**（体质豁免 DC12 / 失败昏睡 / 守林人警告→憋气优势） | 六门 + 规则单测 + 场景路线 | ✅ 本 PR |
| **M5d** | 花田挪到**塔基外侧**（canon 补正 #6）+ **贸然采＝DC16 体质豁免，失败＝死亡结局**（`结局 死亡` 花田变体）+ 情报＝**免判定**（守林人／哥布林） | 六门 + 规则单测 + 场景路线 | ✅ 本 PR |
| **M5e** | **换哨＝与当时的女巫本人**（canon 补正 #7）——新增 `当时的女巫` 段落；老巫女只交**卷轴**；坏哨＝旧史线丢在塔中的引路哨 | 六门 + 场景路线 | ✅ 本 PR |
| **M5f** | **老巫女去信息化**（canon 补正 #8）——删 `老巫女·跑`；`ev.failure_cause` 改由**日记**（书房）给 | 六门 + 场景路线 | ✅ 本 PR |
| **M5g** | **藏杖任务 + 卷轴并入日记 + 光点收束**（canon 补正 #9）——新 `寻杖` 段落与 `family_favor` 好感门；删 `告知与告别`；日记/卷轴只在**现在**书房 | 六门 + 场景路线（含**双门槛**负例） | ✅ #148 |
| **M7a** | **设定书 v17**（去神秘化 + §3.9 传说层 + 主角动机）· 设定集页 `三家` → `守塔的人家` · 酒馆传闻重写（长者 / 跑生意的 / 游客）· 女巫小屋"我儿子在塔边" | 六门 | ✅ #149 |
| **M7b** | **补正 #1 一族的姿态**：守林人删"其实那不是封印"台词；新段落 `守林人·信`；`no_seal` 命题迁通路；§4.0 + §9 纪律 #8 | 六门 + 场景路线 | ✅ #150 |
| **M7c** | **补正 #2 好感线合一**：`witch_favor` → **`family_favor`**；删 `keeper.trust_dragon`；二章单程 | 六门 | ✅ #151 |
| **M7d** | **补正 #3 封印战**：`Game.Dragon.sealAt` / `bladeDamage` + `Items.poisonReduce` + `advSites`；新段落 `守林人·封印` / `封印·并肩` / ~~`封印·涂毒`~~ / ~~`封印·冲`~~（**后两者 B1/M12 并入动作池**）；`audit --dragon` 封印战推演 + 三门 | 六门 + `--dragon` + 场景路线 | ✅ #152 |
| **M7e** | **一致性审计**：设定书内部 14 处冲突 + 设定书↔正文 2 处 + 文档残留引用 | canon 门 + 全绿 | ✅ #153 |
| **M7f** | **PR-B 文档对齐**（大纲 v1.4 / 实施图 v2.0） | 文档一致 | ✅ #154 |
| **M7g** | **PR-C 传说层**（§3.9 补两条 + 传说覆盖门） | canon 门 + 场景路线 | ✅ #155 |
| **M9** | **互动化改造**：信息类检定一律玩家发起（`sites[].auto` 标注战斗位点）· 酒馆 9 桌打听 + 请一轮酒 · 女巫小屋 7 问 · `<<snapshot>>`/`<<lastcheck>>` 骰面复显 · `Choices.sites` 臂数更新 · **互动门** | 七门 + 2 条新路线 | ✅ 本 PR |
| **M11** | **A 组·假代价清零**：`Game.Gear` 行囊表（伤害 / 优势，不进 `Items.defs`）· 秘典药膏 → `salves` · `Game.Star.budget = 4` 软限（超限 → 真结局降级「再度沉睡」+ `$pc.ev.star_short` 变体）· `seer_refused` 删 / `old_witch` 接进 `归位` · 侧栏药膏与行囊 · **行囊门 + 经济门（--gear）** | 九门 + 28 路线 + 规则单测 | ✅ 本 PR |
| **M10** | **判定可见 + 反 S/L**：`Rules.check/save` 返回 `label`（属性标注）/ `parts`（加值拆项）/ `rolls`（双骰）/ `advWhy`；渲染成一行完整算式 + `↳` 优势行；`SgUI` 开关（设定集页脚）；`Checks.keyYields` + `sites[].yields` + **反 S/L 门**；`<<lastcheckFor>>` 防旧骰面串页；8 个关键产出补第二路（含 3 处"失败仍到手"） | 八门 + 27 路线 + 规则单测 | ✅ 本 PR |
| **M21（标题与守林人住处）** | 标题 `送它回家` → **《迷雾森林》**（`StoryTitle` + README；原名剧透真结局落点）· **守林人住在他母亲家**（v17 补正 #6）：塔门火光从顶楼漏下来 · 工作台移到塔顶瞭望位 · 女巫改口“我儿子跟我住” · 女巫小屋补两个人的痕迹 · `70-codex` 同步 · canon §4.1/§5.1/§10/§11 + `CANON_ROWS` 禁词（`住在塔边` 等）——顺带关闭 #168 待确认① | 十一门 ＋ 四道内容门 ＋ `--craft` · 32 路线 | ✅ main `415cfc0` |
| **M32（#197：playtest P3×3）** | ① **女巫小屋首访祝福不再空转**：满血到访时不再"完全恢复"（原 wq_blessed 无条件 hp=max，满血＝浪费治疗量）→ 有伤才治、无伤给一句「没找到要处理的地方」；② **过去进塔补半句**：门厅（过去）开头补「守林人替你把门撑开，朝里头点了下头，自己留在了门外」——收下钥匙→门厅之间的空间过渡闭合；③ **护符呼应**：塔门（过去·未遇守林人）「没法证明自己是谁」后补「你捏了捏脖子上的护符。造它的人就在门里，可门口这些人不认得它」 | 全量 npm test ＋ soak | ✅ 本 PR |
| **M48（#237 数值小项 ×3）** | ① 花田·下风失败＝**本次拿不到花**（土散花折，保留 3 伤；置 flower_mud 回场渲染后果——⓪p 带伤失败必有状态解）；② audit --checks 矩阵对 nat 位点改标彩蛋口径（龙·终击 30%★(51%) 假数 → 「天然20×劣势 ≈0.25%」，与 --dragon 段同算法）；③ 封印·并肩「你只有一次机会，他也只有一次」悬空文案 → 「他念到最后一句话之前，你不能倒下。」（对应真实机制：活着即继续，退场才落再度沉睡） | 文案＋门（npm test/soak 全绿） | ✅ 本 PR |
| **M47（#231 寻杖失主在场）** | `寻杖`：门边撑门的守林人（过去线在场）低声一句「我的杖不见了。别声张，今晚全指着它。」——失主不再隐身：寻杖线有了合理入口，「留意谁总在看那张空架子」有了着落，还杖的人情落到看得见的人身上（family_favor 链的经手人） | 文案（npm test/soak 全绿） | ✅ 本 PR |
| **M46（#229 花警告无差别）** | 守林人初遇：花警告（flower_warned）从 `_t>0` 分层提为**与楼板警告同级、对所有人直说**（canon §5.4「会警告」本就是无差别语气）；社交面板「守林人·花」保留＝问**来历**的加深路径（done 改挂 keeper_told，面板无条件渲染）；路线零改动（问花点击照走，flower_warned 断言仍立） | 文案＋ask 重定 scope（npm test/soak 全绿） | ✅ 本 PR |
| **M45（#236 封印战 MC 三门）** | audit ⓪f 封印战从期望值改**分布感知蒙特卡洛**（2 万局/种子固定/贪心选牌/道具优势/武器 +1/药膏缺口≥4 自动/败次 rage），复用真实 `Combat.offer/playerEff`、`Items.battleDamage/advAt`；三门阈值＝canon §3.8 口径（满配≥80/半配≥50/裸装≤20），只带花毒无门；数值对齐：基档 5/6/6、护臂/日记 −2、毒液 −3 **且压怒**（poisoned→rage 0）、躲吐息 con DC14→**洞悉 DC13**（与书「预判吐息」同轴）；满配定义含药膏 4 副、半配 3 副（「备齐」含耗品）；单挑段保留期望劝退；rules ③/⑤、properties 界限 8 同步 | MC 门＋数值（12 格全绿：满 87–97/半 58–84/裸 3–16） | ✅ 本 PR |
| **M44（#228 巫女在场感）** | `喂花`：长桌尽头握哨那位回头，目光跟着花走到它鼻端；「这花她没见过。你看得出来」＋喂完「她朝你微微偏了偏头，什么也没问。宴要开始了，她转回去，把哨在掌心转了半圈」；`当时的女巫·换`：flower_fed 感知变体——「那朵花。她忽然说。你没答上来……到底还是递了过来」（换哨的半分挣扎）；不点破花的来历与用途（守 §9 纪律 #5/#6）；routeExchangeGate 预置 flower_fed 断言变体文案 | 文案＋条件变体（npm test/soak 全绿） | ✅ 本 PR |
| **M43（#227 观星者半拍）** | `观星者·图`：风化的书回到案上，补半拍迟疑——指尖一寸一寸捻过风化、「纸不该旧成这样。他没有抬头。他自己的年纪，他比谁都清楚。」、「罢了。今晚没工夫想这个。」再合书抄图；克制人设不变（canon §4.4 工作台前骸骨的暗影不点破，守 §9）；「你从未来」这个事实自此有被看见的瞬间 | 文案（npm test 全绿） | ✅ 本 PR |
| **M42（#230 劝杀说服链）** | 守林人「逼他动手」从一键直落改为说服段 `守林人·劝杀`：三路论据——摊开日记（历史 DC12，须持日记：下早了）／摆出雾情（洞悉 DC13：先撑不住的是村子）／自己担下来（游说 DC13：破绽指给你看）；任一成＝守林人击杀（结局文「你说服了他」自此有实感）；败＝各有拒辞、可换路重试、可「把话收回来」空手退；无日记时日记路不渲染只给指引；路线 38（中性骰三路全败＋收回）＋routeKill 改走日记路（预置日记，同 #178 手法）；覆盖基线重签 81/84 | twee＋sitecheck×3＋路线（npm test/soak 全绿） | ✅ 本 PR |
| **M41（#235 武器活线）** | 长剑/短刃 `damage:1` 从死数值接进真实战斗：`Combat.playerEff(actionId,res,pc)`（pick＋`Gear.damageBonus`，**克隆 eff 不改动作表单源**，`gearBonus` 标记）；`fightresolve` 改走 playerEff，fight-meta 显示「（长剑 +1）」（M10 说出为什么）；新增 `Gear.damageSource`；死字段清理：`Dragon.bladeDamage/bladeOf/rage`、`Items.perItemDamageReduce` 删（`hitBase/sneakHit` 保留——audit ⓪f 期望模型在用，归 #236 换分布模型时对齐）；rules ②c 重写为 playerEff 八断言（含动作表不污染/火把不加伤） | 数值接线＋单测（npm test/soak 全绿） | ✅ 本 PR |
| **M49（#239 塔门线收口：canon/正文同步＋差事口径）** | ① §3.3 两条旧规则改写（门外翻转＝门为客开＋值守先人可选拦人；宴当晚地下门不锁、现在铁门须钥匙）＋塔门现在侧提示行同步（删「进不去」假第一课）；② 门厅铁门时代分态（过去＝敞着漫灯光暖气，无钥匙可下楼赴宴；现在＝钥匙门不变）＋routeEraBranches 补双断言＋7 处过去侧点击改「下楼赴宴」；③ **差事即身份口径**（§4.1 新 bullet＋§8 先人行加注＋补正 #13）：守林人段加时代印记/家训回环句、日记台词分代（过去＝「我娘的」）、观星者的书台词分代（过去＝「他今晚就在楼下」）、钥匙交接分代（过去＝差事信物「认符也认人」）、护臂谢媒限现在；15-tables「我妈让你来的」→「我们家的星铁」「我家的字」＋lever note 同步；link-whitelist 补 守林人\|past 三条（同脚本两时代渲染，present 侧已覆盖） | 文案＋canon＋canon 门（npm test/soak 全绿） | ✅ 本 PR |
| **M50（#185 阶段二：四场景试迁移）** | ① 门厅（探索）：取物结果 `replace #hall-act` 原地留屏＋`hallResult`/`hallPick` 单源 widget（D3 inventory 锚句随迁 Widgets）；② 守林人（场景）：`keeper_intro` 首遇门控（不重放介绍/不重给钥匙），`socresolve` 提出门外（每次渲染结算），话题收进 `.scene-acts`；③ 雾之魔物·战（战斗）：fightbegin/fightresolve/fightpanel 三宏 `<<nobr>>`（非叙事换行不再转 br）＋CSS 回合紧凑块（像素验收留浏览器阶段）；④ 子对话返回：`sgBackToActs`（前段名前缀规则→落 `.scene-acts`，滚动＋焦点，通用） | 38 路线＋soak＋全门绿；体积基线重签 854828B | ✅ 本 PR（不关伞票） |
| **M53（#252 规则对照收口 ×4）** | ① 现在·天文台补观星者骸骨（canon §4.6 落地）；② 地下宴会厅（现在）攻击入口旁补「你打不过它」（seal/ally＝守林人说，否则门框旧刻字——§4.5「最多提醒一句」）；③ §3.5/§3.8 删「打不起来」死机制＋游戏侧删龙·战非阈值死分支＋systems rule 同步；④ §3.3 自动回未来收窄为宴散场合（与补正 #11 并条）；canon 补正 #14 | 文案＋canon＋死代码（npm test/soak 全绿） | ✅ 本 PR |
| **M54（#249/#250 月光花贺礼＋道具 ≥2 用途律）** | ① 宴上贺礼：`当时的女巫` 持花可见赠礼链接（赠出即失，三选一）；落点＝`witch_gifted` 态度位（女巫·看哨 base→friendly，非 family_favor）＋采药人台词「它是药，也是毒」（§3.9 出处）；② 封印·吹哨 `invAny:坏哨\|好哨`＋好档 good（好哨＝迟疑更久 skipFoe+3）；③ 封印·读术式持卷好档（跟读 +3）；playerEff 加 good 合并克隆＋fight-meta 渲染 goodNote；④ audit 道具消费门升级：真结局链 7 件 ≥2 类用途（含新「战斗/机制表」类），钥匙豁免登记；routeExchangeGate 预置花补赠礼四断言；canon 补正 #15＋§5.0/§5.4 同步 | 机制＋文案＋门（npm test/soak 全绿） | ✅ 本 PR |
| **M52（#253 D7 NPC 动机登记簿）** | `Game.NPC` 表 34 条（28 让渡＋6 场景拾取）落 15-tables；**audit --npc 三查**（give 位点 9＋社交 yield 6＋立场旗标 6 全覆盖；锚句在段内；动机非空）；弱锚补台词 ×4：女巫「等的不是它回来…替我进去把这件事了结」（盼了结动机桥）／witch_hint「塔我进不去——那条路认他们家的钥匙」（遗赠执行）／老猎人「这话值五枚，爱听不听」（交易腔）／老板娘「一冬天能送走三拨」（两诫动机）；挂 #239 的 2 条已按新锚登记（「门为送星的人开着」「认符也认人」） | 新门＋文案（npm test/soak 全绿；体积重签 862691B＝登记簿基建） | ✅ 本 PR |
| **M51（#248 挣扎归位）** | ① 换哨（flower_fed 变体）：「到底还是递了过来」犹豫让步 → 「这次没有多看，把哨径直递了过来」果断（决定时刻她做主）；② 喂花：新增两难拍「半起了个身，又坐了回去。指节在哨上收紧……她让你走过去」（拦/不拦才是挣扎位） | 纯表演层（npm test/soak 全绿，#228 断言兼容） | ✅ 本 PR |
| **M55（#261／#185 阶段三：共用层改造）** | ① 键盘继续路径：`跳到正文`（越过侧栏）＋`跳到行动`（.scene-acts 段落）＋替换式交互焦点回收＋折叠守卫；② 共用件泛化：`<<firstTime>>` 首遇门控（守林人/酒馆/女巫小屋改用）＋hallResult/hallPick/.scene-acts/sgBackToActs 归共用层；③ 空白归一 `normalizeBlank`（块容器与块元素旁清 br、连续空行封顶 2）＋内容 widget nobr（damage/sitecheck/econ/give/flip/hall*）；④ 修两处 `//` 注释泄漏（地下宴会厅/flip widget——会渲染成正文）＋ integrity 注释泄漏门＋render-all 空行/折叠/跳转三查 | 共用层＋a11y＋门（npm test/soak 全绿；体积重签） | ✅ 本 PR（不关伞票） |
| **M56（#262／#185 阶段四批一：通用结果留屏＋自动行动区）** | ① 包 `<<goto>>`：导航前从 wikifier 缓冲取本次结果文本（去骰面）存 `$pc.ev.last_result{p,text}`，重渲染由 `:passagerender.sgResult` 插回行动区正上方——goto 式流程结果不再被吞（书房/工坊/天文台/寻杖抽查）；② `autoActs`：末尾链接簇（≥2）自动包 `.scene-acts`，分组＋`跳到行动`全库生效；结果槽插入点＝行动容器前／首个链接簇前；③ `actOut`/`sceneFeedback` widget（`src/11-scene.twee`）留给 replace 式流程 | 共用层机制（npm test/soak 全绿；render-all 83 格 0 失败；体积重签） | ✅ 本 PR（伞票/阶段四不关） |
| **M57（#185 验收自查补漏）** | 按伞票验收 10 条逐条核验；补三处实证缺口：① `观星者·图` 重渲染重放介绍＋重给星图 → 首分支加 `not inv[完整星图]` 门（条 5/6）；② `.tavern-actions`（酒馆/女巫小屋范本）未纳入跳转与返回定位 → 加入 `跳到行动`／`sgBackToActs` 选择器，`autoActs` 遇显式容器不再包（条 4/7）；③ 结果槽无时代字段 → `last_result` 增 `era` 并在渲染期比对（条 9 跨时代串反馈）；渲染期状态写逐条复核（均为一次性守卫；`龙·再冲` 彩蛋位重掷登记为适配理由） | 自查＋文案/门（npm test/soak 全绿；三处探针验证） | ✅ 本 PR（伞票不关） |
| **M58（#262／#185 阶段四批二）** | ① 首遇门控按时代覆盖 8 处长介绍段（塔门/宴会·过去/地下宴会厅/工坊/天文台/顶楼/观星者/当时的女巫）——返回只给短句（条 5）；② **结果不吞门**进 walker：点击前后 diff `gold/hp/inv` → 屏上须有对应文本（结局页豁免），真实状态 0 违规；③ 结果槽改「下一次渲染显示一次后清空」——跨段导航（洞穴动武→森林边缘）也留屏，首轮即抓出并修掉该违例 | 文案＋机检（npm test/soak/render-all 全绿） | ✅ 本 PR（阶段四不关） |
| **M59（#262／#185 阶段四批三·批四）** | 余段逐段登记（一章余段 3／二章余段核对／结局 ×15＋章节出口＋设定集 ×8＋车卡 ×2）；新增**段落登记门**（`test/integrity.mjs`：每个内容段落必须登记在 `docs/ui-inventory.md`——「新场景自动进清单」的静态一半，另一半在 coverage 门）；登记口径＝模板归属＋共用层用法＋适配理由（结果留屏/行动区/首遇门控/空白归一四大件，余段多数无需改源） | 文档＋门（npm test/soak 全绿） | ✅ 本 PR（阶段四收口，伞票不关） |
| **M60（#263／#185 阶段五：真实浏览器验收）** | 新增 `test/browser.mjs`——**零依赖 CDP**（Node 22 内建 fetch/WebSocket）直连 Chrome for Testing：视口 ×3（360/390/1280）× 代表场景 ×4（战斗首屏/战斗回合/门厅留屏/守林人返回）＝ 24 项断言＋截图存证；断言＝行动入口在视口内、首屏无近整屏空白、回合反馈相邻（<60px）、结果留屏可见、返回不重放介绍、无横向溢出（含 200% 放大）；`scripts/chrome-deps.sh` 免 root 补系统库；`npm run browser` 并入 `npm run soak`（缺浏览器自动跳过） | 浏览器门（24/24 通过；npm test/soak 全绿） | ✅ 本 PR（伞票剩阶段六） |
| **M61（#264／#185 阶段六：最终收尾）** | ① 兼容验证：新增 `test/saveui.mjs`（6 fixture × 2 时代 × 6 代表段落：不凭空结果槽/不串反馈/不崩/首遇门控安全）＋安全网修「已车卡档 hp≤0 渲染空屏」（限定 abilities 存在，保 C1 收尾）；② 差异复核：`scripts/ui-migration-diff.mjs` → `docs/ui-migration-diff.md`（可见正文漂移仅 8 段首遇短入口，未登记 0）；③ 未覆盖清单：`docs/ui-coverage-gaps.md`（四轴采样依据＋未覆盖项去向 #271/#272）；④ 文档：README 测试章节（39 路线／新门／browser 命令）＋design-review D6 复审记录；⑤ 机制：段落登记门（#278）＋render-all/coverage 自动纳入新段＋browser 显式白名单说明 | 兼容门＋文档（npm test/soak 全绿） | ✅ 本 PR（伞票标 status/verify，不关） |
| **M62（#284①②③ 中的 ①③）** | ① `test/browser.mjs` 增**真机键盘序列用例**（CDP `Input.dispatchKeyEvent` 发 Tab/Enter）：首个跳转「跳到正文」→「跳到行动」→ 行动区；全程不入关闭折叠区；Enter 触发交互后结果在屏且焦点回收正文；含**反例自测**（注入关闭 details＋可聚焦链，先证检查器认得出）；③ `docs/ui-coverage-gaps.md` 增「三口径」段（自动化浏览器＝24 项＋键盘＋saveui＋walker；手机实机＝未做；人工试玩＝未做）（② CI 接线按域归 ci 会话：viewport-smoke job 追加 `node test/browser.mjs`） | 浏览器门＋文档（npm test/soak 全绿） | ✅ 本 PR（#284 不关，待 T 席核销） |
| **M40（#219 D×6：P3 小项合并）** | ① 龙·喊名 label/文案去「书里那个名字」悬空引用（v17 删星名页后无人知晓其名）→「不知哪来的旧话」；② 工坊付钱购买仅过去（现在＝炉膛凉了三百年提示；缺钱另有摊手文案）；③ 顶楼交付链接条件化：不齐＝就地留屏反馈缺哪样（delivery_short once-flag），齐＝进交付；④ 银月之赐「回去归还」仅现在（过去＝「在现在的酒馆里，先翻转回去」提示）；⑤ 焚塔者补因果：撑塔的星力顺着杖走、断口白光一泄；⑥ 守林人「空着的左臂」歧义改「照他尺寸打的护臂没去取」＋「替我谢谢他」补「要是你还能回到炉火旺的那一晚」；link-whitelist 喊名 label 同步；routeEraBranches 补 D③ 断言＋预置双件覆盖交付过去支 | 文案＋条件渲染（npm test/soak 全绿） | ✅ 本 PR |
| **M39（#219 A3＋C1×2：仪式上演＋终局引导）** | **A3**：新段 `宴·仪式`（星图覆水/老妇人平常话/龙听着——canon 补正 #11）＋`宴·散场`（灯灭人散→护符自己凉→回现在塔门，不耗星力，新 widget `eraPresent`）；宴会·过去加「看厅中央」入口（once：ritual_seen）；**C1①** 归位被拦补时代指引（「只开在宴还没散的那一晚」）＋现在·地下宴会厅（有花未喂）出提示；**C1②** flip widget 首次翻转免费（star.first_free，Pc.defaults 同步；rules ⑩ 改双断言）；路线 37（宴散自动回/首翻免费/次翻收费）；覆盖基线 79/83 重签；体积基线重签 842342（内容增量） | twee＋widget＋路线（npm test/soak 全绿） | ✅ 本 PR |
| **M38（#219 B1×3：叙事逻辑跳跃）** | ① **夺杖检定**：顶楼「抢他的杖」改 `顶楼·夺杖`（运动 DC15）——过＝直落讨伐（结局叙述的那一仗自此有起头）；败＝挨 4 伤退回可重试（秘典带药膏自动抵伤入负例路线 36）；② **书房施术门**：`below_seen`（下过地下任一时代即置）＋`书房·施术`（历史 DC13）——没见过它只给不可点的提示文；念对＝劣化封印、念错＝反噬 2 伤可重试；③ **塔门（过去）准入**：宴当晚开门迎客——删「现在的认可对三百年前门卫生效」，推门进去无条件、守林人拦人链接保留（钥匙动线不变）；注意 `hall_seen` 是门厅休眠分支的引用名，新旗标避开用 `below_seen` | twee＋sitecheck＋路线 36（npm test/soak 全绿） | ✅ 本 PR |
| **M37（#219 P1：谜底泄两处＋过去无雾）** | **A1**：图鉴·日记线索 label「那一夜发不动＝烧的是它自己的力气」终局前泄底 → 改「缺的从来不是咒（差在哪，纸上没写）」（与 Truth.claims.failure_cause 对齐）；顺手清同类泄底：「时光护符」「传送术卷轴」own label 去掉「初代巫女」字样；**机检补盲**：codex 门从只扫 def.hint 扩展到**也扫 clue label**（DUALREAD 禁词「它自己的力气」allow 设定集·术语）。**A2**：塔门（过去）雾影链接未门控 → 删（过去无雾，canon 补正 #9）；无 keeper.met 时过去塔门补「有人从门里出来，拦住你」（→守林人，与 #198 场景同源）；8 条 past 路线改走拦路；坏哨图鉴 fight 线索转现在侧专属（routeStudyKnock 补雾战交互覆盖）；覆盖基线重签（过去雾战 3 格合法缩水→79/81） | 文案＋门＋路线（npm test/soak 全绿） | ✅ 本 PR |
| **M36（#185 阶段一：UI 展示层全量清单）** | **66 内容段落全登记**（docs/ui-inventory.md）：模板代号 T-叙事/场景/战斗/子对话/结局/资料/车卡 七类；逐段标注时代变体、共用件已用（socpanel/fightpanel/ending/codex-hub/flip）、进入行为现状与 #185 实测缺口（战斗首屏空白、守林人返回重放＋「获得钥匙」重现、门厅观察结果不留屏、键盘路径）；**酒馆/女巫小屋三区布局＝T-场景迁移范本**；阶段二试迁移候选四场景已圈定（门厅探索／守林人交涉／雾战战斗／守林人·送子对话） | 文档（静态扫描全量源码＋#185 实测对照） | ✅ 本 PR（不关伞票） |
| **M35（#189：规则属性三件套）** | **跨步骤状态不变量**（walker 每步挂检 ＋ 独立单测）：① **单调**——同局内 `dragon.defeats` 只增不减（#178 重进洗状态的回归防线）＋ `awake` 只进不退；② **once-only**——23 个一次性旗标（study_found/forge_seen/staff_found/goblin_gone/hoard_looted/family_favor/…）置位后永不回落（结算不因重访被吞）；③ **前置蕴含**——持有物蕴含来源链：好哨⇒还过杖、好哨⇒坏哨已交出（#177）、完整星图⇒seer_gave、卷轴⇒日记同源、喂花⇒到过宴会厅、钥匙⇒见过守林人。落地＝`test/invariants.mjs`（纯逻辑可单测）＋ walker 每局独立 hist 挂检＋ `test/invariants.unit.mjs` 入 npm test（负例①②③a③b④ 红、正例①②③ 绿） | 单测 ＋ soak 20+20（40 局×每步） | ✅ 本 PR |
| **M34（#178：唤醒不洗状态）** | **同一头龙：重进不回血、败次不清零**：`:: 唤醒` 原无条件 `hp=60; defeats=0`，`:: 龙·战` 的 `_fresh` 同款——战斗中退出去（后撤 crit）再「叫醒它」＝洗败次（rage 清零）重打。→ 两处复位都加 `not $pc.dragon.awake` 门（首唤才初始化；重进＝同一场的延续，龙不回血、败次保留）；唤醒段补「未再睡去」态文案（「它身上的伤还开着，雾也没再聚拢」）；**路线 35**：预置 hp=30/defeats=2 重进 → 断言血与败次原样保留、龙·战 fresh 不再复位 | 路线 35 ＋ 全量 npm test ＋ soak | ✅ 本 PR |
| **M33（#177：换哨查持有）** | **正文不收你根本没有的东西**：`女巫·换哨.apply` 原直接 `delete 坏哨；好哨=true`，无持有检查——而门厅摘哨是可选步，「无坏哨换哨」真实可达（她收走一支你不存在的哨）。→ **willing 加 `pc.inv['坏哨']`**（图＋杖＋哨三齐才肯），why 改「哪样没凑齐，说什么都白说」；当时的女巫段补指引「能拿来换的那支哨，还挂在门厅的钉子上」（soc-no 不可点＋可行动指引）；rules 单测加无哨不肯断言；路线 23 改四门槛（无好感→无图→**无哨**→取哨后成功，含现在↔过去取哨动线）；路线 自愿的长眠 补摘哨步 | rules ＋ 路线 23 ＋ 全量 npm test ＋ soak | ✅ 本 PR |
| **M31（#196：中性骰金路径）** | **失败路径的体验首次入测**：既有金路径用 `newGame(0.99)`＝d20 恒天然 20——所有检定必成，「失败路径」（重试/软锁/数值压力）从未被测过（#195 的发现大多由此漏网）→ **路线 34 中性骰金路径**（0.5＝d20 恒 11）：① 酒馆问路·游说（冷淡 DC12 · 11+0 必败）→ 断言其余手段仍在（B2「这一手更难」≠封门）；② 守林人问花＝护符凭据→友好 DC7 · 中性骰也问得动（凭据的意义）；③ 寻杖·问孩子必败 → 断言另两条路仍在 → 翻（DC13 必败）→ #199 带伤拿到（−1 血）；④ 全链仍达 **结局 送星归位**（失败不改变可达性） | 路线 34 ＋ 全量 npm test ＋ soak | ✅ 本 PR（栈于 #205 之上） |
| **M30（#202：设定集开关挪页脚）** | **UI 开关不再混进内容目录**：「计算过程 开/关」（SgUI.renderToggle）原排在设定集主目录链接区（死亡结局后打开设定集第一眼就是它）→ 挪到「回到故事」之后，新样式 `.codex-foot`（0.78em 弱化灰字＋上分隔线，置底）；路线 12 的开关翻转断言不受影响（label 未变、就地重画不变） | 全量 npm test | ✅ 本 PR |
| **M29（#201：药膏自动门槛）** | **缺口满 4 点（一副的恢复量）才自动烧**：原规则「受伤且存活即自动用」在满血/轻伤时白烧 8 金的药膏（实测满血 18/18 挨 3 → 自动烧一副回 3 点，纯浪费）——玩家没有「攒着」的选择 → \`<<damage>>\` 门槛改 \`缺口 gte 4\`（缺口满一副才烧 → **零过量治疗**，恢复恒为 +4 不再裁上限）；侧栏说明同步「缺口满 4 点时自动用，一副恢复 4 点」；properties 属性检验改门槛断言（轻伤不烧 / 缺口满 4 必烧 / 致死不烧）＋ C1b-0 轻伤不烧单例 | properties（40 步随机序列）＋ 全量 npm test ＋ soak | ✅ 本 PR |
| **M28（#200：龙战直冲线口径）** | **数值即意图（单挑必败线保留），只补预警**：实测定位＝不是数值失控——`龙·战` round>2 走脚本化收束（threshold 恒真：首次进地下宴会厅即置），先「尾扫」再给**唯一选项**「爬起来，再冲一次」→ `龙·终击`（运动 DC20·天然20·劣势＝1/400 荣耀击杀）或 `<<damage 99>>` 必死；入口本就有「退出去｜再度沉睡」出口 → **A 案**：① 唤醒·无哨支「动手」前补预警「这具身体不是一把刀对付得了的，除非正好劈进那道缝」；② 决死线补「除非刀正好劈进那道缝，不然这一下回不来」；数值、检定、彩蛋概率一律不动 | 全量 npm test ＋ soak | ✅ 本 PR |
| **M27（#199：位点失败纪律）** | **带伤失败必有解，无解决不许带伤**：全游戏"失败带伤"位点本就遵循「伤＝代价，结果照拿」（硬掰失败也拿到哨子 / 下风失败也捧起花土 / 哥布林失败也退场），唯独书房·摸暗格、寻杖·翻、工坊·识货/记号 四处是"空手＋可无限重试"（实测摸暗格 8 连点掉 8 血）→ 四处失败档改**带伤也拿到**（磕出匣子 / 抽出杖 / 焐明白护臂）＋免费问询路保留（敲墙/断代/问孩子/看人可重试）；**新机检门 ⓪p**（`--sitedisc`：失败档含 `<<damage>>` 必须同时有 `<<give/<<set/<<goto`——负例：拆掉寻杖的 set → 红）；路线 27 改断言 ＋ **路线 33 书房免伤路**（敲墙零掉血拿到日记） | ⓪p 门（负例验过）＋ 33 路线 ＋ soak 20+20 | ✅ 本 PR |
| **M26（#198：守林人初遇场景归位）** | **塔门外初遇，工作台归还顶楼**：#175（v17 补正 #6）把工作台从塔的一层搬上塔顶瞭望位时，把塔门外拦人的初遇段场景也一起搬了上去——但玩家此刻尚未进塔（现在：塔门进不去；过去：明写“你进不去”），空间断裂。修正：① `:: 守林人` 初遇段改「从塔里下来拦你」（剪灯芯的剪子／袖口灯油／门缝漏火光落在脚边）；② 旧木桌 / 半卷手稿 / 灯归还「顶楼」段（交付“星图摊开在桌上”自此有桌）；③ 钥匙“放在桌上”→“搁进你手里”；④ 子段 ·守/·送/·信 的灯/手稿/窗外动作同步改写（canon 口径不变：拦人上山、瞭望位是岗位）| 全量 npm test（含密度/门7/32 路线/soak） | ✅ 本 PR |
| **M25（经济闭环：金币不许为负）** | **支付门补齐 + 刷钱点一次性**：① 花钱点 6 处中 5 处无门（酒馆买传闻/买火把、女巫问门道、工坊打造护臂、交涉筹码「请她喝一轮/金币买路」——实测 gold=0 全部打穿到 −3…−10；药膏原有门）→ **全部补 `<<if $pc.gold gte price>>`**（柜台链接）＋ **`leverOpen` 单点收口**（带 econ 的筹码买不起不亮）；② **龙·巢边「识货」+10 无一次性门可无限刷**（实测 +10→+20→+30）→ `hoard_looted` 旗标；③ **哥布林战斗两分支都不设旗标**（打跑/钻缝后仍在，可刷 +3）→ `goblin_gone`（退场后角落空置仍可通行）；④ **walker 不变量下界 −100 → 0**（负例：拆掉买传闻门 → soak 红「gold=−5」）；⑤ smoke 经济闭环断言（金币 0 时 4 链接 + 1 筹码不亮；识货 +10 只此一次；哥布林退场不可刷） | smoke 经济闭环 ＋ walker gold≥0（负例验过）＋ 32 路线 ＋ soak 20+20 | ✅ 本 PR |
| **M24（#179 后续：出口在最后）** | **全场景「推进剧情的选项在最后」**：① 酒馆/女巫小屋**已读折叠整体上移到行动区之上**（布局＝正文→交涉→已读折叠→本次回答→行动区；#184 只顾了新听路径，翻旧账读到底仍无出口——操作者实测踩中 tav_dragon 句）；② **门7**（render-all 静态版）：每段最后一个可点之后不许压 ≥30 字正文，**按文本节点走查**（twee 裸链接 + `<br>` 不包块级元素，元素级兄弟走查漏裸文本——负例验证过）；③ **walker 同款不变量**（soak 真实旗标状态，覆盖「折叠区装了几十条传闻」这类状态依赖布局——负例：折叠区挪回行动区下方 → 「出口不在最后: 酒馆 78 字」）；④ 白名单 `test/exits-whitelist.json`（结局页 ending-foot UI 脚注 / `<<flip>>` 过场）；⑤ smoke 加折叠区在行动区之上 + 展开态出口断言 | render-all 门7 ＋ walker 不变量（负例②）＋ 门7 负例① ＋ 32 路线 ＋ soak 20+20 | ✅ 本 PR |
| **M23（#179 复发修复）** | **场景内阅读方向恒向下**：补全 #180 的「本次回答」区——新听到的 `[data-heard]` 条目由 sgFeedback **搬进紧贴行动区的 `.fresh-heard` 槽**（酒馆/女巫小屋），读完就是下一批选项，不再回头向上找；角标复算抽 `updateBadges()`（渲染后＋搬移后两处共用），空折叠整块隐藏；女巫小屋「向她打听」组归位进 `.tavern-actions`（顺手修 #181 的容器外遗留；`<<if wq_seen>>` 门两态都成对进出——坑20）· smoke 三区方向断言（槽位/归档/焦点/其后可点 ≥5） | 十一门 ＋ 四道内容门 · 32 路线 · smoke 方向断言 | ✅ 本 PR |
| **M22（v17 补正 #7）** | **星账不可感知**：日记只剩「缺的从来不是咒」· 守林人删『路费』层 · 天文台改纯现象（「量的是什么，纸上没写」）· 宴会厅删回指 · 交付/真结局收束改口 · 图鉴与道具文案去账目口吻 · **谜底＝设定集·术语「雾 · 谜底」，`SgCodex.seenFinal()`（终局后）门内** · 机检：DUALREAD +4（含 allowlist）＋**谜底门**（负例验过）＋三命题改锚 ＋ 设定集四页路线行为断言 | 十一门 ＋ 四道内容门 ＋ `--craft` · **32 路线** | ✅ 本 PR |
| **M20（#168 批次四）** | **机检 ⑤–⑩ 全落地**（`scripts/audit.mjs --craft`：⑤重复台词 ⑥标点与斜体 ⑦措辞密度 ratchet（`test/density-baseline.json`）⑧道具名主张；`test/coverage.mjs` 门6 链接级覆盖 + `test/link-whitelist.json`），**并抓出五处真缺陷**：⑤ 抓到批次三把车卡句写进了 `设定集·守塔的人家` · ⑥ 抓到 `书房` 的 `''` 不成对（斜体吃到段尾）· ⑧ 抓到 `法杖`／`龙穴`／`女巫的门道` 三处别名回流 · ⑩ 新增**路线 31「酒馆·把桌子听遍」**（八桌 + 旧画逐一点过）· 三律/术语/塔三页密度 23–46‰ → 0 | 十一道 ＋ 四道内容门 ＋ `--craft` 四门 · **31 路线** | ✅ 本 PR |
| **M19（#168 批次三）** | **P2 × 12 句式与声音**：「看了…一眼」10 → 5 处 · 「像在＋动词」收尾砍三处 · 守林人两段重复收尾句只留一处 · 结局两段同一句收束改一 · **新增 `.sys-note`**（机制提示 12 处归系统侧）· **新增 `.cx-term`**（设定集术语／塔页定义改换行小标题）· 「换个法子」骨架 8 → 4 且去旗标口吻 · 守林人台词去写作纪律与链路说明 · 结局页主语跳换修正 · 当时的女巫「像在等…」三句收二并正量词（`Truth` 锚句同步）· 龙战分支误写"雾"修正 · **道具页只列身上已有的**（`Game.Items.defs` 过滤，新增路线断言防泄底）· 设定集删拼图解法 · 车卡去设计者备注与取名句 | 十一门 ＋ 四道内容门 · 30 路线 | ✅ 本 PR |
| **M18（#168 批次二）** | **C/D 组**：洞穴"老猎人的话"换成他真说过的（`第三块板子是脆的`）· 守林人补"从大门走" ＋ **`CALLBACKS` 2 → 4 条**（表新增 `scope`，支持 `$pc.keeper.met`）· 书房（过去）删"你认得的手稿 / 还要再过些年才写得完" · 雾影旁白去交底（`mist_is_guard` 第二通路改挂 `雾之魔物·退`）· 女巫"行头是一代一代传下来的"删（§4.3：红鲱鱼不给解释）· 护符台词改"回到三百年前，站在原地" · 女巫"我去过，三次"删（那是初代巫女）· 守林人改"我家看了它三百年" ＋"路费"挂日记 · 三百年前删雾（老妇人 / 喂花）· §10 明令删的"等它回来吃饭 / 碗筷"清掉 · 观星者删谜底句（`mist_is_fare` 通路 3 → 2）· 宴会·过去补龙影 ＋ 喂花改"它醒着看你" · 劣化封印入口改守林人家的封印术 · 设定集（术语/塔/结局）去泄底 · **P1-29**：`书房·追问` 跑腿挂 `$era` | 十一门 ＋ 四道内容门 · 30 路线 | ✅ 本 PR |
| **M17（#168 批次一）** | **文本审读批次一**：`酒馆`「不老的女人」断链修复（`<<link>>` 标签内半角引号 → 改「」）· `洞穴` 畸形闭合 `<</if>>` · `女巫小屋` 回满改**一次性**（`$pc.ev.wq_blessed`）+ 药膏价读 `Game.Economy.priceOf` · 楼层号归位（工坊 2F / 天文台 3F + 图鉴两条提示）· 花田采摘入口**只在现在侧**（`<<if $era is "past">>` 让过去那侧没有采摘块）· 火把真门控（`$pc.gear.includes("火把")`）· 「半途的林子」去"你没有钥匙"· 老巫女台词分家 · `星铁之杖`/`法杖` → **杖** · 错字与标点一批 · 结局文案按状态分叉（`星落` 的书 / `再度沉睡` 的花）· **新门四道**（断链 / 裸标记 / 楼层数字 / 满血 + 行为侧路线 `女巫小屋·只治一次`） | 四道新门 + 十一门 · **30 路线** | ✅ 本 PR |
| **M16（回指口径）** | **主角只记得他真经历过的事**：`开场` 的传闻改成「一路听来的版本」（**不再提酒馆**——那是车卡之后才第一次进门的地方）· 洞穴「想起老板娘那句话」挂 `$pc.ev.tav_tips`、女巫小屋「在酒馆那幅旧画上见过」挂 `$pc.ev.tav_painting`（没听过/没看过走另一句）· 序章那句与车卡矛盾的"没带多余的东西"删掉 · **L0 加两条机检**：`PRELUDE_BANS`（`开场` 不许出现后文才到的地方）+ `CALLBACKS`（登记在册的回指句必须落在 `<<if $pc.ev.<flag>>>` 里；**#168 批次二：2 → 4 条**，表新增 `scope`（`$pc.keeper.met`）与 `said/saidIn`——**回指的承诺必须真在某处 NPC 嘴里兑现**，只挂门控不够） | 静态门 + 全量回归 · 29 路线 | ✅ 本 PR |
| **M15（C1）** | **结局页收尾入口**：`<<ending>>` 追加 `.ending-foot` 卡片（**退回上一步** `Engine.backward()` / **读档** `sgLoadSlot` / **从头再来** `sgRestartRun`），`StoryScript` 的 `:passagerender` 把卡片挪到段落末尾 · `SgEnding.foot()` 一处渲染（新增结局自动有退路）· **修 `L` 读档不重画**（`slot.load()` 只还原状态，须 `.then(Engine.show)`）· 测试侧把可点选择器收进 `test/boot.mjs`（`CLICKABLE` / `CLICKABLE_SEL` / `LINKS`）+ `settle()` 追加 **DOM 跟 State 同步**、点击前只认"当前段落"的链接 | 全量回归 · 29 路线 | ✅ 本 PR |
| **M14（v17 补正 #5）** | **温室撤除 + 月光花改口径**（操作者拍板）：删 `温室` 段落（**段落 67 → 66**）、楼层重排（`书房` → `工坊`（同层拐角）→ `天文台`）· 花改为**自己长在塔周围、沐浴星光**（`塔外花田` 叙述不再断言"雾越浓它越旺"）· **花与雾的关系无人知晓** → 酒馆新增一条 NPC 转述的**学者猜测**（`跑生意的`）· 修 P1 内部冲突（林间小径过去不该已经有花）· 图鉴提示与 `Echoes.anchors` 同步 · canon 门加两行 + 传说覆盖门加一条 | canon 门 + 全量回归 · 28 路线 · 覆盖 76/80 | ✅ 本 PR |
| **M13（B2）** | **交涉**：`Game.Social`（`ATT/attAdj` 态度轴 · `approaches` 七种手段 · `asks` 8 件诉求 × `sites`/`levers`/`willing`/`unwilling`/`apply` · `attitude/shift/tries/dcOf/open/levers/leverOpen/roll/verdict/settle`）· 宏 `<<socresolve>>`/`<<socpanel>>` + `StoryScript` 的 `a.soc-opt` 委托点击（**选项不能用 `<<link>>`**：其内容点击时才解析，`<<for>>` 临时变量已被下一个面板覆盖）· 段落 `酒馆`/`守林人`/`观星者`/`当时的女巫`/`洞穴`/`老巫女` 改挂面板 · `$pc.soc = {att,tries,read}` · **交涉门（`--social`）** · 新增存档 fixture `s5-social.json`（`Pc.defaults` 26 键） | 十一门 + 28 路线 + 规则单测 | ✅ 本 PR |
| **M12（B1）** | **战斗动作池**：`Game.Combat`（`pools` 雾影 6 / 封印 10 / 龙 6 · `actions` 每手 `site`+`label`+`ok/crit/bad` · `eligible/offer/pick/applyEffect/siteInfo`）· 宏 `<<fightbegin>>`/`<<fightresolve>>`/`<<fightpanel>>`（回合状态 `$pc.ev.fight`）· 段落 `封印·并肩`/`龙·战` 改挂池子、`封印·涂毒`+`封印·冲` **退役** · `封印·读术式`（奥秘）给秘典留路 · **战斗动作池门（`--combat`）** · `audit --dragon` 封印战推演改为**按池内真牌取期望** | 十门 + 28 路线 + 规则单测 | ✅ 本 PR |
| **M8** | **道具图鉴**：`Game.Codex`（10 页 / 32 线索）· `SgCodex` 跨周目持久化（`localStorage['sgstory.codex.v1']`）· `<<ending "key" final\|chapter>>` 登记宏 · `设定集·图鉴` 页 · 存档位 8 → 16（`Config.saves.maxSlotSaves`） | 六门 + 场景路线 + 图鉴门 | ✅ 本 PR |
| **M7h** | **删三件闲物**（请柬 / 星名页 / 碎镜片）+ 守林人的杖移出清单 + **道具消费门 / 假代价门** | canon 门 + 全绿 | ✅ 本 PR |

> **为什么先做 M1a-1**：换骨必须与测试套重写**同一 PR**（否则 CI 红）。先把审计工具改成**文件发现式**，后续改名/拆文件不再牵动工具。

---

## 1. 文件结构（M1a-2 已落地）

```
src/
  00-meta.twee        StoryTitle / StoryData（起始段「开场」）
  10-core.twee        Rules[script] + StoryInit + Widgets[widget] + StoryCaption
  15-tables.twee      Game Tables[script]（Checks / Economy / Items / Gear / Combat /
                      Social / Codex / Truth / Echoes / Choices / Systems / Shifts /
                      Star / Dragon）+ Pc（状态形状 + migrate）
  20-chargen.twee     Chargen Data[script]（3 轮）+ 车卡 / 角色卡
  30-ch1.twee         序章 + 一章（时间）：开场 · 酒馆（+B2 交涉面板）· 森林边缘 · 洞穴（+B2 交涉面板）· 女巫小屋 · 林间小径
  40-ch2.twee         二章（手段）：塔门 · 塔外花田 · 雾之魔物 · 守林人（+B2 两个交涉面板（花 / 术）· ·送 / ·守 / **·封印** / **·信**）· 门厅 · 书房 · 工坊（二楼拐角）· 天文台（三楼）· 顶楼
  50-ch3.twee         三章（坐标）：地下宴会厅 · 宴会·过去 · 观星者（+B2 求图面板）· 当时的女巫（+B2 看哨/换哨面板）· 寻杖 · 老巫女（+B2 回绝面板）· 喂花 · 交付 · 唤醒 · **封印·并肩（B1 动作池）** · 龙·战 / 龙·再冲 · 归位
  60-endings.twee     结局（10 个出口）
  70-codex.twee       设定集（hub + 三律 / 守塔的人家 / 塔 / 道具 / 术语 / 结局 / **图鉴**）
  80-script.twee      StoryScript（存档钩子 / S·L 快捷键（读档 .then(Engine.show) 才重画）/ 结局页收尾入口 / 图鉴持久化 / 段落起始状态归一化）
  90-style.twee       StoryStyleSheet
```

**纪律**：`[script]` 段只出现在 `10-core` / `15-tables` / `20-chargen` / `80-script`；正文段只出现在 `30/40/50/60/70`。`audit.mjs` 与 `integrity.mjs` 均按**文件名排序**发现，不再硬编码路径。

**词汇宏（正文只经词汇引用机制）**：

| 宏 | 作用 | 表源 |
|---|---|---|
| `<<sitecheck 位点 [adv\|dis] [加值] [nat]>>` | 技能检定 / 豁免（`abil` 位点自动走 `<<save>>`；`dis` 位点级劣势；`nat` 天然大成功） | `Game.Checks.sites` |
| `<<dragonbar>>` | 龙血条（`$pc.dragon.hp` / `Game.Dragon.hp`） | `Game.Dragon` |
| `<<ending "键" final\|chapter>>` | **C1** 记本档结局 + 图鉴永久账，并追加**收尾卡**（退回上一步 / 读档 / 从头再来）——`:passagerender` 再把卡片挪到段落末尾 | `SgCodex.recordEnding` / `SgEnding.foot` |
| `<<socresolve "诉求">>` | **B2** 在段落渲染顶部结算待办交涉动作（掷骰 + 落账 + 写 `$pc.ev.soc_last`）——骰面因此与检定同帧 | `Game.Social.roll/settle` |
| `<<socpanel "诉求">>` | **B2** 发这一句诉求的开口方式与筹码（`<a class="soc-opt" data-ask data-how>`；点击由 `StoryScript` 委托处理，只写 `$pc.ev.soc`） | `Game.Social.open/levers/verdict` |
| `<<fightbegin "池">>` | **B1** 开一场战斗：初始化 `$pc.ev.fight`（**同池子且没打完就不重置**——点牌重渲染会再跑一遍本宏） | `Game.Combat.pools` |
| `<<fightresolve "对手位点" [龙战]>>` | **B1** 结算上一手：掷玩家那一手（`adv` 由上一手的准备给）→ 落效果 → 对手这一轮出手（`true`＝走 `Game.Dragon.dragonDamage`） | `Game.Combat` |
| `<<fightpanel "对手位点" [龙战]>>` | **B1** 这一轮的 3 张牌（每张旁边写明属性与 DC）；点牌只写 `$pc.ev.fight.act` 再 `<<goto>>` | `Game.Combat.offer/siteInfo` |
| `<<snapshot>>` | 把 `$last_check` 存进 `$pc.ev.last_roll`（玩家发起的检定在 link 里掷完就存） | — |
| `<<lastcheck>>` | 复显上一条快照的骰面（不掷骰）——`<<goto>>` 重渲染后骰面不丢 | `$pc.ev.last_roll` |
| `<<lastcheckFor "位点" …>>` | 只当 `$pc.ev.last_roll.site` 属于给定位点时才复显（**防旧骰面串到别的段落**） | `$pc.ev.last_roll` |
| `<<ending "名" final\|chapter>>` | 结局登记（本档 `$pc.ev.ending` + 永久入图鉴账本；**终局才给空页提示**） | `Game.Codex` |
| `<<econ 事件>>` | 金币收支（含 `gives` 入账、烙印/技能折扣） | `Game.Economy` |
| `<<give "道具">>` / `<<setflag "旗标">>` | 物品栏 / 世界旗标 | `Game.Items.defs` |
| `<<flip>>` | 时代翻转（原地生效、位置决定年代、耗隐藏星力、回现在雾淡） | `Game.Shifts.anchors` |
| `<<damage N>>` | 受伤（药膏自动生效 +4、归零跳结局死亡） | — |
| `<<inventory>>` | 侧栏物品栏（由 `$pc.inv` 派生） | `Game.Items.defs` |

---

## 2. 状态模型（单一源 `Pc.defaults()`，26 键）

| 组 | 字段 | 说明 |
|---|---|---|
| 车卡 | `name` `round` `picked` `mode` `classKey/classLabel` `bgKey/bgLabel` `speciesKey/speciesLabel` `abilities` `skills` `feats` `gear` `flags` | **种族只改数值**（大纲 OQ2）；3 轮三选一 |
| 数值 | `hp` `max_hp` `gold` `salves` | d20 内核 |
| 时间 | `$era`（present/past） | **原地翻转、位置决定年代**（v16 §3.3） |
| 星力 | `star.charge` · `star.spent`（**玩家不可见**） | 唯一反馈＝从过去回来时「雾淡一些」（v16 §3.5） |
| 守林人 | `keeper.met` `keeper.trust` `keeper.state`(**post/ally/seal**) `keeper.key` | 钥匙＝二章正常交涉即得（**空手也给**，v17 §5.12）；`seal`＝并肩封印线（**先打到 `sealAt` 以下**，v17 补正 #3） |
| 物品 | `inv`（对象：`{ 时光护符: true, … }`） | 拾到即入；**不集齐开锁**（v16 §5.0） |
| 证据 | `ev.*`（`mist_guard` `observation_lock` `keeper_why` `failure_cause` `star_ledger` `threshold` `coord` `letter_seen` …） | 真相链标记 |
| 交涉（B2） | `$pc.soc` = `{ att: {}, tries: {}, read: {} }` | 态度偏移（−1 敌意 / 0 冷淡 / +1 友好）· 同一手试过几次（`askId\|site` → 5×DC）· 读过谁（解锁 reader 型筹码）；待办动作走 `$pc.ev.soc`，结果留痕走 `$pc.ev.soc_last[askId]` |
| 龙 | `dragon.hp` `dragon.defeats` `dragon.awake` `dragon.venom` | 阈值触发战斗（`ev.threshold`）；封印战：**花毒液**（龙的攻击 −2）；**轮次改由 `$pc.ev.fight.round` 管**（B1 动作池把 `dragon.round` 这个空字段删了） |
| 战斗（B1） | **不在 `$pc`**：`$pc.ev.fight` = `{ pool, round, offer, act, adv, guard, skipFoe, flee, last, done }` | 每轮随机 3 选 1 的回合状态；点牌只写 `act` 再重渲染，判定在段落渲染顶部结算 |
| 图鉴（跨周目） | **不在 `$pc`**：`localStorage['sgstory.codex.v1']` = `{ clues, endings, finals }` | 线索由 `Codex.satisfied(pc)` 每段落并进账本；**不用 `settings`**（`Setting.save()` 只落盘注册过的 Setting） |
| 世界 | `world.*`（`rumor` `goblin_spared` `witch_hint` `flower_fed` `whistle_blown` `seer_asked` `old_witch_told` `mist_fought` `scroll_delivered` `fog_thin` **`family_favor`** …） | 旗标（`Game.Echoes` 覆盖门管）；**`family_favor`＝全篇唯一人情线**（还杖，v17 补正 #2） |

**迁移**：所有新增字段必须过 `Pc.migrate()`（幂等补型 + 嵌套合并），否则旧档读入即崩。`test/fixtures/saves/` **6 版**历史形状（v13 旧档 / 二章早期 / 型损 / 极简 / 药膏与时代 / **交涉前**（B2：没有 `soc` 台账））逐版断言补齐·保值·修型·幂等。

---

## 3. 段落图（v17 现状 + M8–M15，66 内容段）

### 基础设施
`StoryTitle` `StoryData` `StoryInit` `Widgets` `StoryCaption` `StoryScript` `StoryStyleSheet`

### 序章
`开场` → `车卡`（快速预设 / 逐轮细调 3 轮）→ `角色卡` → `酒馆`
`酒馆` → `森林边缘` / `女巫小屋` / `结局 平凡之路`

### 一章 · 时间
`森林边缘` → `洞穴`（哥布林：**交涉面板**（游说/欺瞒/恐吓/洞悉）＋筹码（金币 / 捡石头）→ 让路｜动武｜绕开）
`酒馆` / `守林人` / `观星者` / `当时的女巫` / `洞穴` / `老巫女` 的信息类询问全部走 **`<<socpanel>>` 交涉面板**（B2）/ `女巫小屋`（**时光护符**）
`女巫小屋` → `林间小径`（翻转教学）→ `塔门` / `结局 银月之赐`

### 二章 · 手段
`塔门` → `雾之魔物`（→ `雾之魔物·战` / `雾之魔物·退`）→ `守林人`（`守林人·送` / `守林人·守` / **`守林人·信`**（信它不会变成恶龙））→ **钥匙**
`守林人` → **`守林人·封印`（"你把它按下去，我念"；**交涉面板求术式成功才发这条** → `keeper.state=seal`）** / `门厅`（坏哨）→ `书房`（日记）→ `工坊`（二楼拐角：龙鳞护臂）→ `天文台`（三楼：观星者的书 / 星账）→ `顶楼`；花在 **`塔门` → `塔外花田`**（自由选择点）
`塔门` → `半途的林子` / `结局 半途`；`顶楼` → `结局 新任守林人` / `结局 焚塔者` / `结局 讨伐`

### 三章 · 坐标
`地下宴会厅`（现在：只有龙；`龙·巢边` 识货 / 攻击 / **`叫醒它`**）→ 翻转 → `宴会·过去`（hub）
`唤醒`（**初始化龙血量/败次**）→（`keeper.state=seal`）**`封印·并肩`**（**B1 动作池 `封印` 10 手**：每轮随机 3 选 1 → 涂毒 / 吹哨 / 读术式 / 硬扛…）→ `hp ≤ sealAt` → **`结局 送入虚空`**
`设定集·图鉴`（跨周目账本：线索齐 → 永久解锁；终局 → 空页给指向）
`宴会·过去` → `观星者`（`观星者·星` / `观星者·夜` / `观星者·图` → **完整星图**）/ `当时的女巫`（`寻杖` → **好感**；**看哨＝交涉面板**（洞悉/游说/恐吓）；`当时的女巫·换` → **好哨**，前提：已取图 + 好感——**拿不出＝unwilling，面板明写「掷骰也没用」**）/ `老巫女`（**只露面**：藏杖凶手（玩家不知道）+ 真结局光点）/ `喂花` / `老妇人`
回到现在 → `交付`（顶楼：卷轴 + 星图 → 守林人同行）→ `唤醒` → `归位` → `结局 送星归位`

### 终局（10 个出口）
真：`结局 送星归位`
降级：`结局 再度沉睡` / `结局 自愿的长眠`
非真：`结局 守林人击杀` / `结局 送入虚空` / `结局 劣化封印` / `结局 星落` / `结局 讨伐` / `结局 坠星之死` / `结局 死亡`
章节：`结局 平凡之路` / `结局 银月之赐` / `结局 半途` / `结局 新任守林人` / `结局 焚塔者`

---

## 4. 命题集 v1（`Game.Truth.claims`，12 条 · 锚句已机检在位）

每条**≥2 通路**，锚句在位（`audit --truth --check` 硬门）。

| id | 命题 | 通路（全部经 `audit --truth --check` 机检） |
|---|---|---|
| `mist_is_fare` | 雾＝它睡着时漏出来的力气——**正文无人知晓**（谜底只在设定集·术语，终局后揭开，v17 补正 #7） | 设定集·术语（`SgCodex.seenFinal()` 门内）/ 真结局收束（“他不知道，你也不知道”） |
| `dragon_is_star` | 龙不是恶龙，是一颗迷路的星——要做的不是讨伐，是送它回家 | 过去·观星者·星 / 过去·宴会·过去 / 现在·守林人 |
| `failure_cause` | 那一夜发不动＝缺的从来不是咒（差的是什么，无人知晓） | 现在·书房（日记）/ 现在·交付 |
| `observation_lock` | 能改的不是"历史"，是**没被观测过的那部分**（它睡得怎么样） | 现在·女巫小屋（家训＝须被推翻）/ 现在·书房 |
| `keeper_is_alive` | 塔顶那位就是守林人本人——活人，持钥匙，唯一能发动送行术的人 | 现在·守林人 / 现在·顶楼 |
| `mist_is_guard` | 雾中的形＝陪着守林人的守卫；攻击旅人只是误认 | 现在·雾之魔物 / 现在·守林人 |
| `coordinate_source` | 坐标＝三百年前的完整星图（带回现在即可） | 过去·观星者·图 / 现在·交付 |
| `info_gate` | **没有完整星图，她不肯换哨** → 真结局线根本不成立 | 过去·当时的女巫·换 / 过去·当时的女巫 |
| `no_seal` | 塔下**没有咒、也没有锁**——它只是睡着 | 现在·地下宴会厅 / 现在·守林人·守（**他守着事实，但不纠正叫法**） |
| `fare_is_hidden` | 星账无人感知、不可测量——唯一反馈是雾淡一些 | 现在·天文台（“量的是什么，纸上没写”）/ 真结局收束 |
| `homecoming_path` | 真结局＝坐标 + 卷轴 + 好哨 + 花 + 守林人同行 | 现在·归位（四样齐了）/ 现在·交付 |
| `farewell_letter` | 承杖先人写给观星者的信：它有名字，有家（观星者到死没拆） | 过去·观星者案头 / 现在·守林人转述 |

> **注**：`family_favor`（还杖人情）**不是 Truth 命题**——它是 `Game.Echoes` 的一环 + 换哨双门槛的旗标（见 §2）。
> M1b 将把设定书 §10 黑名单接入 `audit` 作 canon 门。

---

## 5. 测试策略（M1a-2 已重写）

| 文件 | 定位 | 现状 |
|---|---|---|
| `test/boot.mjs` | jsdom 启动 + 就绪轮询 + uncaught 监听 + **`settle()`（等 `Engine.isIdle()`——上一翻没画完就点，SugarCube 会丢掉这次点击）** + **统一退出清理**（非零视口让 SugarCube 的视口就绪轮询收尾、`await Engine.start()` 才真正等到启动完成 → `beforeExit`/`exit`/信号统一 `close()` 全部窗口；**游走器与分支场景测试都走这里**，不再自己装配 JSDOM；可点选择器也在这里定义——`CLICKABLE`/`CLICKABLE_SEL`/`LINKS`，新增一种控件只改一处） | 保留 |
| `test/render-all.mjs` | 全段落渲染（含 `$era` 双变体） | 保留（**76 格 / 66 内容段** · 渲染后**零断链/零裸标记**） |
| `test/walker.mjs` | 随机游走 + 不变量 + **位点双支清扫** | 重写（`inv` 闭集 / `star.spent` / `keeper.state`（**含 `seal`**）/ `dragon.hp`） |
| `test/integrity.mjs` | 段落图结构/死链/孤儿/宏拼写/词汇纪律 + **序章白名单**（`开场` 不许提前提后文的地方）+ **回指门**（"你想起某人说过的话"必须真听过） | 重写（`ERA_FILES` 由 `<<flip>>` 动态发现 + **楼层数字门 + 满血门**） |
| `test/rules.mjs` | d20 内核 + 车卡 3 轮 + `Pc.migrate` 矩阵 + 表契约（10 组） | 重写 |
| `test/properties.mjs` | 属性测试（判定边界全枚举 / 支配律 / 伤害界限 / 战斗伤害单调律 / 车卡形状律） | 重写 |
| `test/scenarios.mjs` | **32 条路线**（金路径 + 全部结局 + 设定集 + 龙巢边 + 时代分叉 + 封印战 + 反 S/L + 星力软限 + 结局页收尾 + **女巫小屋·只治一次** + **酒馆·把桌子听遍** + **文本上下文**） | 重写 |
| `test/smoke.mjs` | 启动 → 快速车卡 → 酒馆 → 森林 → 洞穴 + 侧栏/存档/物品栏 | 重写 |
| `test/coverage.mjs` | 渲染/交互覆盖率 ratchet | **收紧为 5 门**（基线：渲染 **76** / 交互 **80**） |

**机检十一门**：`--truth` `--canon` `--echoes` `--choices` `--interact` `--nosl` `--gear` `--combat` `--social` `--systems` `--text`（**全部进 `npm test`**）+ 数值三件套（检定成功率矩阵 / 经济时间线 / `--dragon` 龙战推演）。
**内容层机检（#168 批次一）**：`test/integrity.mjs` 加**楼层数字门**（同段内点名房间又写"N楼"必须一致；`[[上N楼|X]]` 的目标房必须就在 N 楼）与**满血门**（`<<set $pc.hp to $pc.max_hp>>` 的前一个非空行必须 `<<if ...>>`）；`test/render-all.mjs` 加**断链门**（`a.link-broken` 为 0）与**裸标记门**（渲染文本不许有字面 `<`）；`test/scenarios.mjs` 加**行为侧路线**「女巫小屋·只治一次」（第一次带伤进回满、第二次带伤进不回）。四道门都进 `npm test`。

**canon 门（M1b）**：`docs/lore-canon.md` §10「已裁剪设定」是唯一黑名单来源。门做两件事——① **行覆盖**：§10 每一行必须被 `CANON_ROWS` 认领（新增裁剪行不认领即红）；② **词扫描**：认领行禁词不得出现在 shipped 文本（正文 + 数据表字符串；`/% %/`、JS 行注释、CSS 块注释不计），否定句（如「没有亡灵」「不集齐开锁」）允许。另含定向检查：`守林人*` 段落不得出现「她」。**第 ⑤ 项＝传说覆盖门（v17 §3.9）**：设定书 §3.9 对照表的每一行必须被认领，且每条传说在正文里必须有 NPC 投放锚（`LEGENDS`，9 条 10 锚）——**登记了却没人说即红**。当前：§3.9 传说 **9 行 · 9 认领 · 10 投放锚**；§10 行 **72** · 认领 **73** 条 · 禁词 **127** 个 · 命中 0。
**行囊门 + 经济门（A 组）**：每件行囊有来源/说法/效果/发放点、`advSites` 位点真实；正文不许出现表外行囊；每条 `Economy.events` 必须有落点（`gives` / `setflag` / `give` 道具），纯收入要在 `CLAIM` 里写明理由。当前 **3 件行囊 · 10 条经济事件全认领**。`--gear --check` 已接入 `npm test`（两个负例验过）。

**反 S/L 门（M10）**：`Game.Checks.keyYields` 每个关键产出的**通路 ≥2 条且判定属性 ≥2 种**；`Checks.sites[].yields` 必须指向真产出。交涉诉求（`Social.asks[*].yield`）另算：**掷骰手段属性 ≥2 种**，**或**存在一条**免检筹码**（把对方想要的摆出来＝不必掷骰）——这就是 2024「give them what they want → no check」落成的反 S/L 保障。当前 **12 个产出 · 62 条通路**（其中 4 条是交涉里的**免检筹码**）。另有 3 条通路设计成「**失败仍到手**、只赔血」——那条靠正文人工保证，门只看通路数与属性种类。`--nosl --check` 已接入 `npm test`。

**交涉门（B2）**：每条诉求的每一手都在 `Social.approaches` 里（技能/豁免都要有开口方式）；筹码条件必须真的可能成立、`needRead` 的诉求必须有读人的手、`gives` 只能是 auto/adv；有掷骰路子的诉求必须有 `ok`/`bad`/`done`/`apply`；有免检筹码必须有 `auto` 过场；**态度阶梯就是 −5/0/+5 三档**；代价必须因手段而异（重试代价 · 态度代价 · 无代价三种都在）。当前 **8 件诉求 · 19 个开口位点 · 11 枚筹码 · 2 件「始终/条件性不肯」**。`--social --check` 已接入 `npm test`（两个负例验过）。

**互动门（M9）**：① 段落**顶层**的 `<<sitecheck>>` 必须是 `sites[x].auto` 有理由的**战斗**位点（信息类检定只能由玩家动作发起）② 顶层读 `$last_check` ⇒ 同段落顶层必须有本轮检定（判定结果必须先落旗标）③ 位点无孤儿。战斗轮次的检定由 `Game.Combat.actions[*].site` 提供（`<<fightresolve>>` 每轮掷一次对手位点）——这些位点算**玩家发起**（牌是玩家挑的）。当前 **检定 64 处（玩家发起 38（其中交涉 19）· 进场即动手 4 · 战斗动作池 22）**。交涉面板的开口方式由 `Game.Social.asks[*].sites` 认领（算玩家发起）。

**图鉴门（v17 M8）**：① 双向覆盖（`Items.defs` ↔ `Codex.items`）② 每页 ≥2 线索 + 非空提示（≤40 字、不得含 §9 双读禁断言）③ **线索不白送**（新档下必须全假）④ **线索可挣**（全收集态必须全真）⑤ **每个 `结局*` 段落必须 `<<ending "…" final|chapter>>`**。当前 **10 页 · 32 线索 · 命中 0**。

**道具消费门（v17 补正 #4）**：`Game.Items.defs` 每件必须至少一处真消费（①位点效果 ②经济事件 ③正文按持有分支 ④Truth/Echoes 锚），否则红；**假代价门**：note 里写"永失 / 换掉就"必须有真下游。当前 **10 件 · 零消费 0**。

**§9 双读断言（M6a）**：只扫正文（不扫 `[script]`/`[stylesheet]`），禁 6 条断言——`长生` · `同一个人` · `初代巫女` · `初代` · `穿越` · `晚年`。命中即红并指出段落。当前命中 0；注入测试已验证（`设定集·三律` 注入「长生 / 同一个人」→ 门红并报段落）。

**当前规模**：`npm test` 全绿（**32 条路线** · 内容段 **66** · 渲染 **76** · 交互 **80** · `audit --text` 载荷标注 66 · 叙事段去标记 ≈1.85 万字）；`npm run soak`（游走 20+20 局 + **63 位点**双支清扫）0 违法，**已接回 CI 独立 job**（`deploy` 依赖 `[test, soak]`）。

**覆盖率 5 门（M1c）**：① 基线不回退 · ② 新段落必配测 · ③ **无交互盲区**（内容段落必须有可点击到达路径）· ④ **时代双态**（按 `$era` 分叉的 **10 段**必须 present/past 都被交互踩到）· ⑤ **交互格 ≥ 渲染格**。

---

## 6. 换骨顺序（M1a-2 实际执行）

1. `10-core`（`Rules` + `Pc` + Widgets）+ `15-tables`（`Game.*`）→ `audit --check` 可跑 ✅
2. `20-chargen`（3 轮 + 3 预设）→ `rules.mjs` 绿 ✅
3. `30/40/50/60` 段落图（正文 + 锚句）→ `render-all` + `integrity` 绿 ✅
4. `70-codex` + `80/90` → `smoke` 绿 ✅
5. `test/scenarios.mjs` 路线 → 全绿 ✅（现 **29 条**）
6. `npm run soak` → 0 违法 ✅
7. 重生成 `test/coverage-baseline.json` ✅

## 7. M1a-2 遗留（已全部由后续里程碑接手清掉）

| # | 项 | 归属 |
|---|---|---|
| 1 | ~~canon 门~~ ✅ M1b 已完成 | — |
| 2 | ~~各楼层「过去 / 现在」正文分叉~~ ✅ 现 **10 段**时代分叉（覆盖率门 ④ 全踩） | — |
| 3 | ~~雾之魔物 / 守林人 的完整战斗数值~~ ✅ M12 **B1 动作池**（雾影 6 / 封印 10 / 龙 6 手，`--combat` 门） | — |
| 4 | ~~`龙鳞护臂` 打造前置 / `守林人的杖` 终局归属~~ ✅ 护臂在 `工坊`；杖**一直在守林人手里**（v17 补正 #4 移出物品栏） | — |
| 5 | ~~文本量目标（1.2–1.5 万字 / 60–80 段）~~ ✅ 现 **≈1.85 万字（叙事段去标记）/ 66 段** | — |
| 6 | ~~`npm run soak` 接回 CI~~ ✅ M1c 已完成（独立 job，`deploy` 依赖 `[test, soak]`） | — |
