# 夹具 · `m3-p1234-pilot`（`#1234` 作者面改制 · **试点段**）

**一句话**：同一个段（`01-门厅`）的**两种表达** —— **旧形态**（今天能跑的基线）＋ **新形态**（`#1234` 目标 schema），
用来给改制当**靶**：验收＝**两者渲染文本逐字节同**（纯重构 ✓）。

> 权威＝`#1234` 的 **schema 冻结**（`5815699385`）。**冲突时以那份冻结为准**，本夹具跟着改。
> 分工（按 `#1234` 冻结里的分工栏）：**本夹具＝靶**（本笔）｜**判据升级与两格能假例**（分工项 ⑤）｜**`passages.json` 编译落点与渲染口接收入参**（分工项 ③＋②）。

## 一、两棵树

```
stories/pilot-old/   ← **基线**：今天的写法（能跑 ✓）
  00-story.json ｜ data/{meta,contract,rules}.json ｜ passages/{01-门厅,02-靴子,03-里屋,04-侧厅}.md
      · 链接是**散文里内联**的：`[[翻一翻靴子|靴子]]`
      · 条件＋另一条链接在**规则行**里：`{ id:"门厅.推门", scope:"门厅", req:["inv:黄铜钥匙"], prio:2, text:"[[推门进去|里屋]]" }`
      · 段尾有渲染口：`<<rules "门厅">>`
stories/pilot-new/   ← **试点**：目标表达（等开发人员实现后能跑 ✗ 今天不能）
  00-story.json ｜ data/{meta,contract}.json ｜ passages.json ｜ passages/{01-门厅,02-靴子,03-里屋,04-侧厅}.md
      · 散文**零链接**：只剩文本 ＋ `{{提醒}}`
      · 链接与条件全在 `passages.json`：`links[{label,to,cond?}]`
      · 段 `params` 声明入参；散文里的 `{{}}` **只认本段 `params`**
```

**逐条映射（旧 ⇒ 新）**：

| 今天 | 新形态 |
|---|---|
| 散文 `[[翻一翻靴子\|靴子]]` | `links[].label` ＋ `to`（无 `cond`） |
| 规则行 `门厅.推门` 的 `req` | `links[].cond = { req:[…] }`（**同一形状、同一权威 `Sg.rules.matches`**） |
| 规则行 `text` 里的 `[[…]]` | `links[].label` ＋ `to`（**`text` 不再承载链接**） |
| 段尾 `<<rules "门厅">>` | 段的 `links[]` ＋ `present` |
| **散文里内联**的链接（**位置**有意义） | `links[].slot` —— 渲染到散文里**同名 `{{}}` 占位**处（`slot` 名与 `params` 名**同一命名空间 ⇒ 撞名则红**）；**✗ 无 `slot` ⇒ 段尾块**（＝今天规则行的行为） |
| `<<rulelist "侧厅">>`（菜单，渲染全部命中行） | `links[]` ＋ `present: "菜单"`／`"单选"` |
| 散文里的固定字面 `钥匙在靴里` | 段 `params.提醒` 的 **`default`** ＋ 散文 `{{提醒}}`（见 §二·2） |

## 二、验收（分两格，✗ 不混）

**1. 纯重构格**：`pilot-old` 与 `pilot-new` 在**同一输入**下渲染出的**正文**必须**逐字节相同**。
　（`pilot-old` 今天就能跑 ⇒ 基线读数由判据格产出；`pilot-new` 待开发人员实现后同格产出 ✓）

**2. ★"含取值"格（本试点新增的那一处）**：今天的格式**表达不了**"散文里一句随入参变的话"（＝ `#1236` 的归因缺口：
`cases/north-room/text-with-attr`）。本夹具把**旧形态里的等价写法**＝那个**固定字面** `钥匙在靴里`，
在新形态里写成 **`params.提醒.default`** ⇒ 于是"默认值相等 ⇒ 文本相等" ✓
　⇒ 这一格的读数是**两条**：① 不传 `提醒` ⇒ 出默认文案（与基线同 ✓）② **传** `提醒="别进屋"` ⇒ 出替换后的文案（旧形态**给不出**这一读数 ✓ —— **这正是新格式要买的东西**）

> **✗ 本夹具不手抄期望文本**：基线渲染文本由判据格**跑出来**（✗ 不从散文里抄 ⇒ 避免"抄"与"跑"两套真源）。

## 三、三个待裁点的处置（前两个**已裁**，第三个**暂定**）

1. **内联链接 vs 段尾链接** ⇒ **已裁**（`#1234` `5815823928`）：`links[]` 增**可选 `slot`**。
　给 `slot` ⇒ 渲染到散文里**同名 `{{}}` 占位**处（本夹具 `门厅` 就是这条：散文里 `{{靴子口}}` ⇔ `links[].slot="靴子口"`）；
　✗ 没给 ⇒ **段尾块**（＝今天规则行的行为）；`slot` 名与 `params` 名**同一命名空间** ⇒ 撞名则**红**。
　⇒ 于是"今天散文内联链接＝新形态的 `slot`"／"今天规则行链接＝无 `slot`"**两态可分**，且**逐字节同可达成** ✓
2. **`present`／`prio`／`prereq` 三维**另立最小样本 ⇒ **已裁并已落**（`#1234` `5815823928`）：新增段 **`04-侧厅`**（两棵树都有）。
　· 它有 **2 条链接**（`侧厅.左门` `prio:3`／`侧厅.右门` `prio:2` 且 **`prereq:["侧厅.左门"]`**）⇒ 单选 vs 菜单、`prio` 先后、顺序维**各有对象** ✓
　· 旧形态对应写法：`<<rulelist "侧厅">>` ＋ 两行规则（第 2 行带 `prereq`）⇒ 两树**同语义** ✓
　· 能分两态的读数（由判据格给）：**选左门之前**只渲染 1 条（右门的 `prereq` 未满足）／**选过左门之后**渲染 2 条；
　　`present` 翻成"单选" ⇒ 只剩 `prio` 最高的那条（✗ 与"菜单"不同形）；`prio` 对调 ⇒ 单选下渲染的那条换人 ✓
　★ 仍**✗ 不许**拿"只有 1 条规则行"的段（`门厅`）去证这三维 ✓
3. **`passages.json` 落点与粒度** ⇒ **暂定**（`#1234` `5815823928`）：**`data/passages.json`**（一份，进既有 `data/*.json` 自动发现面）。
　Operator 原话是"一个场景…**会有一个 json**"（像**每段一件**）⇒ **待一句话定**；若改粒度，本夹具跟着搬／拆（夹具一次性、成本低 ✓）。

## 四、今天怎么跑（✗ 新形态今天跑不了）

```bash
# 基线（旧形态）：今天就能跑 —— 清三层 ⇒ build ⇒ 渲染
#   （夹具 `test/fixtures/m3-chk-e2e/run.sh` 就是那条命令的现成样板：清三层 ⇒ build ⇒ 跑用例）
# 新形态：等开发人员把 `passages.json` 的编译落点与渲染口接收入参做出来后，同一条命令应能跑 ⇒ 两态才可比 ✓
```

## 五、来源与漂移政策

- `pilot-old` 的**内容**取自 books 仓 `stories/north-room`（基线 `a57d08a`），**只做三处裁剪**：
  ① 只留试点段 `01-门厅` 与它的两个链接目标（`02-靴子`／`03-里屋`，删掉它们自己的规则行以外内容 ⇒ 只留最小可跑）
  ② `00-story.json`／`data/meta.json` 的 slug 与标题改成夹具体的
  ③ 删 `audit.json` 与 `15-tables`／`16-notes`／`18-chargen`（本试点不用）
  ⇒ **books 仓冻结期内**，本夹具是**唯一**能落这次改制的靶（✗ 不动 books ✓）
- 段面变化时**重取副本**并更新本节来源行 ✓

## 六、四样检查（本笔）

```
① 归因门 rc=0（先 `git add` 再跑）　② 文档格式门 rc=0　③ 链（零故事）全绿 —— 本夹具**不新增会红的段**（新形态今天不可跑 ⇒ 判据随实现落地 ✓）
④ 净树：跑完非新增脏件 0
```
