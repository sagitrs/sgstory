{
	"note": "去权威化口径门（`test/attribution-gate.mjs`，票 `#752`）的白名单。键＝`<仓库相对路径>::<token id>`。每条必须写 reason 与 ticket；不再命中会被判「腐烂」并报红（逼你删）。**起始为空** —— 这条口径不需要豁免：不该改的历史目录已按目录整目录排除，文档里讲这条规矩本身用单行 `deauth-exempt:` 标记（理由必填且在输出里留痕）。",
	"entries": {}
}
