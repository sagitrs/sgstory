:: StoryTitle
面夹具

:: StoryData
{
	"ifid": "9A61C4D0-1F2E-4B77-9C33-6E5A0D2B7F41",
	"format": "SugarCube",
	"format-version": "2.37.3",
	"start": "开场",
	"zoom": 1
}

:: StoryIdentity [script]
// 故事身份（**单一源**）：`Sg.store` 的故事键命名空间用它（`<slug>.` 前缀 ⇒ 各故事互不串档 ✓）。
// ⚠️ 本件是**测试夹具（非内容故事）**（`#1004` B2b）：它把引擎接入契约的每种面声明一次，供测试当输入。
//    段名沿用旧故事**只因消费者钉死了它们**（`Engine.play('森林边缘')` 这类）；**正文全部新写** ——
//    读到这里的人请勿把它当成"漏删的旧故事"。
window.Sg ??= {};
window.Sg.storyId = { slug: 'face-fixture' };
