# 基线报告：E4 节奏密度 · C4 路线相异度

> 探索票 **#295**（伞 #297）。**只读报告＋基线 ratchet**，不是 CI 硬红：先入基线，数字稳定后再谈阈值。
> 数据源：`build/route-traces.json`（`test/scenarios.mjs` 跑 40 条路线时顺带落盘的轨迹：段落序列／点击数／首个不可逆点／当时情报旗标）。
> 复现：`npm test`（生成轨迹）→ `npm run report:rhythm`（出报告）→ `--check` 与基线比对 → `--selftest` 反例自证。

## 判据

- **E4 节奏密度**：五拍（Five Room Dungeon 的经典五段：入口与门槛／谜题或交涉／诡计或挫折／高潮／奖励与揭示）齐备；开局 → **首个不可逆点**（花田致死位点 / 龙战）的**交互数**与**当时情报覆盖**在带内——ratchet 防退化（不许把不可逆点推得更远，也不许推得更早＝铺垫变少）。
- **C4 路线相异度**：不同**游玩路线**的屏文重叠不过高（OPDC「支持多系统」的 twine 等价）——防未来加内容时路线趋同。

## 度量口径（两处踩坑后的修正，值得记住）

1. **截断轨迹不进 C4**：只用了几次点击的专项用例，其轨迹天然是长路线的**前缀**，包含率（inter/min）必然 = 1.000——那是伪信号。C4 只比**走完结局的完整路线**（本次 21 条 / 40 条），截断用例只进 E4 表。
2. **先聚「游玩路线家族」，再比家族之间的距离**：测试用例里有大量「同一段路径的多个用例」（如「平凡之路」×「结局页收尾」相似度 0.994——它们本来就该一样）。直接报两两最大值只会测出**用例重复度**而非内容趋同。故按相似度 ≥ 0.9 自动归并成家族（本次 **16 族**），**家族间**相似度才是真信号。

## E4 结果

（路线 40 条；首个不可逆点＝花田/龙战（先到者））
  走到首个不可逆点：15 条路线；交互数 7–20；当时情报旗标 4–13
  路线｜点击数｜首不可逆点｜当时情报｜结局
   交付后互锁（#259）｜3｜—｜—｜(未到结局)
   跨周目粘性（#271）｜3｜—｜—｜(未到结局)
   女巫小屋·只治一次｜4｜—｜—｜(未到结局)
   平凡之路｜4｜—｜—｜结局 平凡之路
   洞穴动武｜6｜—｜—｜(未到结局)
   银月之赐｜6｜—｜—｜结局 银月之赐
   半途｜8｜—｜—｜结局 半途
   花田死亡｜8｜花田@7｜4｜结局 死亡
   结局页收尾（C1）｜9｜—｜—｜结局 平凡之路
   新任守林人｜12｜—｜—｜结局 新任守林人
   焚塔者｜12｜—｜—｜结局 焚塔者
   花田·哥布林情报｜12｜花田@11｜6｜(未到结局)
   讨伐｜12｜—｜—｜结局 讨伐
   劝杀三路可败可收（#230）｜13｜—｜—｜(未到结局)
   唤醒不洗状态（#178）｜13｜龙战@13｜10｜(未到结局)
   守林人击杀｜13｜—｜—｜结局 守林人击杀
   老妇人｜13｜—｜—｜(未到结局)
   夺杖检定（#219 B1①）｜14｜—｜—｜(未到结局)
   死亡｜14｜龙战@12｜10｜结局 死亡
   龙·巢边｜14｜—｜—｜(未到结局)
   酒馆·把桌子听遍｜15｜—｜—｜(未到结局)
   书房免伤路（#199）｜16｜—｜—｜(未到结局)
   坠星之死｜16｜龙战@12｜10｜结局 坠星之死
   宴散自动回未来（#219 A3/C1②）｜16｜—｜—｜(未到结局)
   打听·碰壁与请酒｜16｜花田@15｜13｜(未到结局)
   劣化封印｜21｜—｜—｜结局 劣化封印
   星落｜21｜龙战@17｜13｜结局 星落
   情报自己问（免检暗格）｜22｜—｜—｜(未到结局)
   设定集四页｜23｜—｜—｜结局 平凡之路
   送入虚空｜26｜花田@16｜9｜结局 送入虚空
   再度沉睡｜27｜—｜—｜结局 再度沉睡
   文本上下文（时代与日记）｜27｜花田@8｜4｜(未到结局)
   自愿的长眠｜29｜—｜—｜结局 自愿的长眠
   时代分叉｜30｜花田@8｜4｜(未到结局)
   换哨双门槛｜36｜—｜—｜(未到结局)
   非酋不读档（换属性路）｜43｜花田@17｜11｜(未到结局)
   中性骰金路径（#196）｜67｜花田@20｜11｜结局 送星归位
   金路径 送星归位｜70｜花田@19｜11｜结局 送星归位
   图鉴·永久解锁｜72｜花田@19｜11｜结局 送星归位
   乱翻的代价（星力软限）｜75｜花田@19｜11｜结局 再度沉睡

  五拍齐备度（Five Room Dungeon 口径，人工标注一次）：
   ① 入口与门槛：可达段落 13｜被 40 条路线走到｜村与林的门槛：打听准入、守卫（哥布林/雾之魔物）与第一道塔门
   ② 谜题与交涉挑战：可达段落 20｜被 27 条路线走到｜拿钥匙/上楼/守林人交涉，以及第三章的双时代调查（寻杖/喂花/观星者）
   ③ 诡计与挫折：可达段落 5｜被 15 条路线走到｜代价与回退：花田致死位点、劣化封印的岔口、观星者算不出图的僵局
   ④ 高潮：可达段落 5｜被 12 条路线走到｜决战拍：封印并肩 / 龙战 / 唤醒
   ⑤ 奖励与揭示：可达段落 19｜被 23 条路线走到｜真相落地与收束：交付、归位、观星者·星，以及全部结局页
## C4 结果

（字符 5-gram；只比走完结局的 21 条完整路线，另有 19 条截断用例不计入）
  游玩路线家族（相似度 ≥ 0.9 自动归并）：16 族
   · 3 条：中性骰金路径（#196） / 图鉴·永久解锁 / 金路径 送星归位
   · 3 条：新任守林人 / 焚塔者 / 讨伐
   · 2 条：平凡之路 / 结局页收尾（C1）
   · 1 条：乱翻的代价（星力软限）
   · 1 条：再度沉睡
   · 1 条：劣化封印
   · 1 条：半途
   · 1 条：坠星之死
   · 1 条：守林人击杀
   · 1 条：星落
   · 1 条：死亡
   · 1 条：自愿的长眠
   · 1 条：花田死亡
   · 1 条：设定集四页
   · 1 条：送入虚空
   · 1 条：银月之赐
  跨家族平均相似度 0.467｜最大 0.894｜家族内最大 0.994（家族内高是预期的：同一段路径的多个用例）
  最相似的跨家族 5 对（真正的「趋同」危险区）：
   0.894  乱翻的代价（星力软限） × 金路径 送星归位
   0.892  坠星之死 × 死亡
   0.888  劣化封印 × 星落
   0.855  乱翻的代价（星力软限） × 图鉴·永久解锁
   0.844  中性骰金路径（#196） × 乱翻的代价（星力软限）
## 基线数字（`scripts/rhythm-baseline.json`）

| 指标 | 基线 | 说明 |
|---|---|---|
| E4 走到首个不可逆点的路线数 | 15 | 少于基线＝有路线不再经过不可逆点 |
| E4 交互数区间 | 7 – 20 | 出带＝不可逆点被推近/推远 |
| E4 当时情报旗标数 | 4 – 13 | 低于下限＝信息覆盖退化 |
| C4 游玩路线家族数 | 16 | 下降＝有独立路线的内容被并掉 |
| C4 跨家族平均/最大相似度 | 0.467 / 0.894 | 上升＝不同路线趋同 |
| 五拍可达段落数 | ①13 ②20 ③5 ④5 ⑤19 | 下降＝拍子被削弱/删段 |

## 反例自证（`--selftest`，必须变红才说明断言有牙）

| 反例 | 操作 | 期望 | 实况 |
|---|---|---|---|
| **R1 路线趋同** | 把一条**单成员家族**路线（`乱翻的代价（星力软限）`）的正文整条换成金路径的复制 | `c4-convergence` 红 | 家族 16 → **15** ✓ 红 |
| **R2 五拍缺拍** | 抹掉全部结局页（＝⑤ 奖励与揭示） | `beat-coverage` 红（⑤） | ⑤ 可达段落 19 → **0** ✓ 红 |
| 正例 | 真实轨迹 vs 基线 | 无 failure | 0 failure ✓ |

> **R1 首版不咬合的记录**：第一版反例是「把一个家族的路线复制给另一个家族的路线」→ 家族数 16 → 16，断言抓不到。原因：那条路线只是**换了个家族**（旧家族剩下的人自成一族）。这正说明「门必须行为化」的必要——反例不是走过场，它当场找出了断言的缺口。修正后牺牲品必须是**单成员家族**（独立内容消失），家族数才会真的下降。

## 本轮的观察（供后续开修复票/维度立项）

- **③ 诡计与挫折是最薄的一拍**：只有 5 个可达段落、15 条路线经过（对比 ② 20 段/27 条）——第三章的「代价与回退」主要靠花田致死位点与观星者僵局撑，支点偏少。**只登记，暂不开票**（是否加内容属设计取向，需 Admin/D 席定）。
- **首个不可逆点最早 7 击**（花田死亡线），最晚 20 击（中性骰金路径）——带较宽，说明**玩家信息量差异大**（4–13 个旗标），这是有意的分支设计，不是缺陷；入基线是为了防未来把这条带无意拉大或压扁。
- **家族规模**：金路径家族 3 条用例 / 新任守林人家族 3 条用例，其余多为单条——即使扣掉用例重复，仍有 16 个互不相同的游玩路线，相异度健康。

---

# 附录：就地行动存读档一致性门（#300 §5 机检）

> 来源：操作者 P1 票 **#300**（门厅原地取物后立即 S/L 丢进度）＋ T 席范围扫描。**测试基建，不含产品修复**。

## 为什么需要它（漏测的机理）

| 现有资产 | 为什么抓不到 P1 |
|---|---|
| `test/saveui.mjs`（90 行） | 只做 `Game.Pc.migrate(fixture)` → 渲染 → 点击后断言 `slots <= 1 && hasText`，**从不调用 `Save.browser.slot.save()` / `Sg.save.load()`** |
| `test/browser.mjs` | 门厅键盘用例在 Enter 之后即停，**不按 S/L** |
| `test/scenarios.mjs` | 路线只断言剧情分支与旗标，**不含存读档往返** |

即：**「操作后状态」与「存档快照」之间的一致性从未被断言过**——这是「全绿 ≠ 关键状态覆盖」的又一实例。

## 两个门

| 门 | 文件 | 何时跑 | 当前状态 |
|---|---|---|---|
| **静态门**（登记完整性） | `test/saveload-inventory.mjs` | **在 `npm test` 链里** | ✅ 绿——它保证的是「**被登记**」，不是「已修好」 |
| **行为门**（存读档保值） | `test/saveload.mjs`（`npm run test:saveload`） | 独立跑；**接入 npm test 的时机＝#300 修复合入的 PR** | ❌ **红＝缺陷基线证据** |

### 静态门保证两件事

1. **就地行动站点不得漏登记**：凡 `<<link>>` 体内含 `<<replace` 且改状态（`<<give`/`<<damage`/`<<set $`）的站点都必须进 `test/saveload-sites.json`；新增同类站点会被立刻抓住（＝#300 票面「不要只对门厅与洞穴打特判」的机械保证）。
   - **口径限制（已显式登记）**：扫描器只看得见 link 体内的**直接**变更；门厅「把墙上那支哨子摘下来」的 `give` 藏在 widget `<<hallResult>>` 里，属**间接站点**，只由行为门盯（静态门仅校验该 widget 仍存在）。
2. **检定 key 必须有归宿**：每个 `<<sitecheck "KEY">>` 的 KEY 要么出现在某处 `<<lastcheckfor>>` 参数里，要么在豁免表写明**理由＋票号**（空豁免＝红灯）。当前 25 个 key：23 个有复显、8 个登记豁免（2 个是已实测缺陷＝#300 P2、5 个待裁定、1 个静态误报）。

### 行为门当前结果（`npm run test:saveload`，缺陷基线）

```
就地行动站点 4 个：0 保值 / 4 不保值
  门厅「把墙上那支哨子摘下来」   丢 inv[坏哨] · 丢 ev[last_roll]
  门厅「先看清钉子是怎么卡的」   丢 ev[hall_seen, last_roll]
  门厅「摘哨子（widget hallPick）」丢 inv[坏哨] · 丢 ev[hall_seen, last_roll]
  塔外花田「伸手去摘最靠里的那一朵」丢 inv[月光花] · 丢 ev[last_roll]
```

四项断言（物品／旗标／HP／金币／检定记录）**逐站全丢**，且**不掉 HP/金币**——与 #300 的引擎级根因分析一致：保存的是 `_history` 快照，同页 `<<replace>>` 的变更进不去。

## 用法

```bash
npm test                      # 含静态门（登记完整性）
npm run test:saveload         # 行为门：#300 修复前应为红（缺陷基线证据）
```

## 附：dist 过期守卫（2026-09-12 实测咬过一次）

`test/*.mjs` 跑的是**构建产物** `dist/index.html`，不是 `src/*.twee`。源码改了、忘了 `npm run build` 时，
所有 jsdom 断言都基于**旧游戏** → 得到与源码不符的**假红/假绿**。

实际案例：pull 了 G3（#302）之后直接跑 `node test/scenarios.mjs`，得到「#291 G3：两侧证据齐了却问不出（门不可达）」的**假红**——
真实原因只是 `dist/index.html` 落后源码 4 分钟。重建后同一条路线通过（覆盖 85 格）。

现在 `test/boot.mjs` 在任何脚本启动时都会校验 `dist` 是否比 `src/*.twee` 新，过期则**大声报错并给出修复命令**
（不自动 build——那会掩盖问题）。`npm run test:saveload` 已改为先 build 再跑。

---

# 附录：I1「投入—回报对称性」首轮报告基线（#291）

> 数据源：`node scripts/audit.mjs --investment`（G2/G3/G4 三门＋自证）与其中的 G1/G5/G6 只读报告。首次入基线 2026-09-12。

## 门（行为化，含反例自证）

| 缺口 | 判据 | 门 | 反例自证 |
|---|---|---|---|
| **G2 失败须生成内容** | 关键位点失败档必须产出信息（旗标/情报），不是白扣血 | 关键位点失败档扫描 **14 处** | 写了但没回收 → 检出 >0 ✓ |
| **G3 推理须能做门** | 门的条件须同时引用 ≥1 过去侧与 ≥1 现在侧的**主动取得**证据旗标；不给提示按钮 | 跨时代合龙门 **1 处**（老巫女：`failure_cause` × `seer_asked`/`coord`）＋ 时代域表（past 15 段 / branch 6 段 / 登记门 1 处） | 单侧证据 → 选项**不存在** ✓（T 席独立复验，见下） |
| **G4 立场须被记住** | 表达型立场选择须写入状态且有终局消费点 | 立场写入＋回收检查 | 写了但没回收 → 检出 >0 ✓ / 选项已不在段落 → 检出 >0 ✓ |

## 只读报告（G1/G5/G6）

| 缺口 | 本轮数字 | 口径说明 |
|---|---|---|
| **G1 时间可感** | 时钟类状态 **0 处**（设计如此：canon §3.5 星账不可测量）；现象层钩子（雾/星力叙述）**3 段** | 边界：不加时钟数字，只做现象层 |
| **G5 重玩换视角** | 二周目专属知识层 **4 处**（谜底门） | 边界：不破 §4.0/§9 |
| **G6 空间记忆锚** | 含空间锚句的段落 **23 段**（不引入地图系统，锚走文字） | 边界：不做插图美术 |

## G3 的 T 席独立复验（不依赖实现方自证）

同一份 dist（新鲜构建）下，直接以玩家可见行为验收：

| 检查 | 结果 |
|---|---|
| 只有现在侧（`failure_cause`）→ 选项 | 不存在 ✓ |
| 只有过去侧（`seer_asked`）→ 选项 | 不存在 ✓ |
| 两侧齐（`failure_cause`＋`seer_asked`）→ 选项 | 出现 ✓ |
| 两侧齐（`failure_cause`＋`coord`）→ 选项 | 出现 ✓ |
| 点击前屏上是否可见「烧的不是柴」 | 不可见 ✓（无提示按钮、无剧透） |
| 点击后 | 写入 `ev.witch_fire_hint` ✓ ＋ 只言片语落屏（「烧的不是柴。……是等一个不在场的人把话说完。」）✓ |
| 证据旗标是否被消费 | 未消费 ✓（可重复检视） |

> 踩坑记录：首次复验得到「门不可达」的**假红**，原因是 `dist/index.html` 落后 `src/*.twee` 4 分钟（`test/*.mjs` 跑的是构建产物）。已在 `test/boot.mjs` 加过期守卫。

## 补记：G1「时间可感」现象层落地（#291 → PR #324）＋ T 席独立验证

G1 的正例落点（不含任何数字，守 canon §3.5 星账不可测量）：

| 钩子 | 触发条件 | 实测 |
|---|---|---|
| 雾递进 | 翻转累计 ≥3 后回到现在 | 「雾比上回更**薄了一层**，林子里的声音听得清楚了些。」 |
| 代价感 | 翻转入口（同上阈值） | 「（你翻得越来越熟了。可每次回来，身上都像被**抽走了一点点什么**。）」 |
| 守林人中途台词 | `star.spent ≥ 3` 且**第二次**见守林人 | 「你来回跑了几趟了。……雾薄了。可薄下去的东西，不全是你带回的。」 |

**T 席独立验证**（不跑实现方断言）：雾递进 ✓ · 代价感 ✓ · 三处文案**均不含数字** ✓ · 阈值有效（`star.spent<3` 时台词不出现）✓。
> 复现注记：守林人台词位于**第二次见面**的分支（首次见面分支里没有它）——直跳段落只走首见分支，会误判为「不可达」；这是探针写法问题，不是缺陷。

# 附录：#296 走查（A5 回读友好 · B5 教学曲线）

走查日期 2026-09-12。判据与采样依 #296 票面；**能机检的部分已落门** `test/reread.mjs`（入 `npm test`）。

## A5 回读友好：四个面 × 门控实况

| 面 | 门控（实现机制） | 实测 | 判定 |
|---|---|---|---|
| **图鉴**（10 条目 / 32 线索） | 每条线索一个 `test(pc)` 谓词（`Game.Codex.items`） | **零状态档（`Game.Pc.defaults()`）下 0 条为真**；空图鉴记录下 0 条目 `isUnlocked` | ✅ 可回看不泄底 |
| **结局账本**（图鉴页） | 未解锁条目渲染成 `？？？`；提示句只在 `Sg.Codex.seenFinal()` 后给；页内明示「重新开始游戏也会保留已解锁的内容」 | 跨周目保留＝**设计**（I1-G5 重玩换视角），且已向玩家明示 | ✅ |
| **设定集固定页**（8 页） | 「谜底级」内容由 `Sg.Codex.seenFinal()` 门控；其余页是 `Truth.claims` **声明的证据面** | 谜底级命题 `mist_is_fare` 的 codex 站点（`设定集·术语`）**确实带门**；`keeper_is_alive`／`no_seal` 的 codex 站点（`设定集·三律`／`设定集·塔`）在命题表里被**声明为第 3 条线索** → 是「给定」不是泄底 | ✅ 一致 |
| **已读折叠区**（酒馆「已听到的传闻」） | 按 `$pc.ev.*` 旗标逐条渲染 | 未听到的条目**不在 DOM 里** | ✅ |
| **日记** | 图鉴条目 `日记` 的 3 条线索各有谓词（拿到 / 知成因 / 知观测锁） | 同图鉴 | ✅ |

**唯一「仅登记」项（按 #247 要求写理由）**：结局账本只给「已达成终局 N 个」，不列出**是哪几个**。
不判定的理由：这是**有意的非剧透选择**（列出名单会泄露结局名与数量结构）；票面判据只要求「可回看、可确认」——数量可确认即满足。想让玩家回看名单属**增强**，不是缺陷。

### A5 的机检部分（`test/reread.mjs`）

| 判据 | 内容 |
|---|---|
| R1 | 零状态档下图鉴**不得有任何线索谓词为真**（行为断言，非字符串） |
| R2 | 空图鉴记录下**不得有任何条目 `isUnlocked`** |
| R3 | `Truth.claims` 声明的 `type:'codex'` 站点段落**必须存在**（声明的证据点不许是空头） |
| R4 | `via` 标为「终局后揭开 / 唯一揭开处 / 谜底」的 codex 站点**必须真带 `Sg.Codex.seenFinal()` 门** |
| R5 | 任何设定集页面若出现「谜底」字样，**该页必须带门**（防「新写的谜底忘加门」） |

自证 10 例（5 条判据各带会红的反例）。**真产物探针**：① 把术语页的 `seenFinal()` 换成 `<<if true>>` → **2 项红**；② 把「时光护符·来历」谓词改成 `() => true` → **1 项红**；恢复即绿。

> **过程留档（值得记住）**：自证**首次运行时全绿，但那是空判**——我把 `t(ok, msg)` 按 `(msg, ok)` 调用，字符串恒真、断言全废。修正参数顺序后立刻暴露 1 条真失败（R2 反例的期望值写错）。**空判连自证本身都会骗过**：这就是「写完反例必须先跑一遍确认它变红」的价值。
> R4/R5 的边界（有意漏报）：只判**已被声明**的谜底（`Truth.claims` 的 `via` 措辞 ＋ 页面自称「谜底」），不猜散文——设计方新增一处「本该门控却从未声明」的谜底，本门看不见；宁可漏报也不假报。

## B5 教学曲线：五种机制 × 首遇代价

对标 Tomb of the Serpent Kings 的「安全教学 → 试用 → 考验」。

| 机制 | 首次出现 | 首遇代价 | 安全教学段 | 判定 |
|---|---|---|---|---|
| **d20 检定** | `森林边缘`（`30-ch1.twee:205`，`<<sitecheck "森林·察觉">>`，DC 10 / 察觉，`15-tables.twee:13`） | **无资源代价**；失败只丢一条**风味观察**（雾中灯火与悬空金币），且该观察**不在任何关键命题的站点里**（已核：`Truth.claims` 无相关条目）→ 不属「关键结论」 | `<<check>>` UI 自解释：骰面＋目标＋修正＋「计算过程」开关（`10-core.twee:140`） | ✅ 首遇不惩罚 |
| **时间翻转** | `30-ch1.twee:455`（`<<flip>>`，`10-core.twee:330`） | **头一次翻转不收费**（`$pc.star.first_free`，#219 C1②「造它的人留的一点余量」） | 第 3 次起给**现象层**成本句：「身上像被抽走了一点点什么」（不显数字） | ✅ 安全教学 |
| **交涉（态度/筹码）** | 酒馆「老板娘·进塔」（`30-ch1.twee:53`；面板 `10-core.twee:498`） | 失败**分级**（`Game.Social.settle`）：重试代价只加在「话说死了」的手（游说/历史）；欺瞒/恐吓＝态度降级；**表演/洞悉/察觉＝只是没接上话**（可重试） | 面板同时给两条安全路：「开口」与「筹码」（`drink` 花金币 auto 成功 / `read` 优势需先读人） | ✅ 首遇可重试且有筹码 |
| **星力预算** | 同「翻转」（`Star.spent`） | 预算 4→6（软限方案 A），每次翻转 −1；**首翻免费** | 不显数字：只给「雾淡了一些」与代价感句 | ✅ |
| **封印战（战斗）** | `40-ch2.twee:156` 雾之魔物（**机制**首遇） | 首遇提供**免战出路**：`[[慢慢放下手，退开一步]]`；战前有坏哨提示（「它认得这个形状」） | 回合池每轮随机 3 选 1，每个动作是一次属性化检定；龙战（`50-ch3.twee:598`）是**终局考验**，不是机制首遇 | ✅ 首遇不惩罚 |

**结论：五种机制均有「安全教学 → 试用 → 考验」，未发现「首遇即惩罚」。**
B5 **不做机检**（按 #247 原则写明理由）：「教到什么程度算够」是审美判断，做文本启发式会产出大量假阳性（#266/#269 类空判的教训）——故只登记走查结论＋证据行号。

## 采样覆盖（票面要求）

- **四条路线**（真结局／封印／抢杖／坏结局）：A5 四个面与 B5 五种机制的首遇段落**都在主线唯一路径上**（不随路线分叉）→ 四路线共享同一批首遇；
- **两态**：旧档载入的 A5 风险由既有 `test/saveload.mjs`（物品/旗标/HP/检定记录保值矩阵）覆盖；跨周目由图鉴 localStorage 设计承担（页内已明示）。

## 发现

**本轮未发现缺陷**：A5 门控一致（谜底被门住、给定被声明）、B5 五机制均有安全教学。**未开修复票。**

# 附录：#407（D9 交代自洽）首轮扫描①③面

## ③口吻面（世界内 vs 作者层）：**词表机检不成立**，降级为走查清单（附实测证据）

**扫描口径**：在世界内文本面（`src/70-codex.twee` 8 页设定集 ＋ `30-ch1`/`40-ch2`/`50-ch3`/`60-endings` 正文）里搜作者层标记词：
`玩家 / 设计 / 作者 / 本作 / 这个游戏 / 不该 / 我们 / 维度 / 机制 / 系统`。

**结果（0 真阳性）**：

| 词 | 命中 | 判定 |
|---|---|---|
| 玩家 | 2 | **全在源码注释 `/% %/` 里**（如 `50-ch3:196` 的 #365 批注）——玩家看不到，属扫描器假阳性（与 `test/globals.mjs` 学到的一样：**先剥注释**） |
| 不该 | 2 | `30-ch1:245`「只知道不该伸手」·`50-ch3:255`「纸不该旧成这样」——都是**世界内语气** ✓ |
| 我们 | 13 | 全在 NPC 台词里（「今晚我们送一位迷路的客人回家」等）✓ 世界内正当用法 |
| 设计 / 作者 / 本作 / 维度 / 机制 / 系统 | 0 | — |

**更关键的是**：词表**抓不到 #406 那个实例**。`设定集·三律` 的问题不是"出现了'设计'二字"，而是它把
**canon §33 的设计禁令**（"没有血统、没有天选、没有神秘组织"）与**主题句**（"道具会老，所以真相也会老"）
用世界内读物口吻写了出来——**没有任何标记词**。

**结论（按 #247：不做机检必须写理由）**：③面**不做词表机检**（实测 0 真阳性 ＋ 漏掉已知实例），改为：

1. **声明制**：段落/页面声明 世界内 / 作者层（设定集页与 `docs/**` ＝ 作者层；正文与 NPC 台词 ＝ 世界内）；
2. **人工走查判据（三问）**——世界内文本必须通过：
   - **这话谁说得出口？**（世界里有没有一个具体的人能说这句）
   - **他在哪知道的？**（他的信息来源在故事里存在吗）
   - **他说这话图什么？**（动机是否落在他的利害上）
   三问任一答不上来 ⇒ 作者层口吻混入 ⇒ 改写或挪进 `docs/`。

## ①来源面：已机检（`test/premise-source.mjs`）

`#407` ①面已落地为门（声明 `premise` ⇒ 断言前提词出现在**授予依据旗标的段落**里）。首轮实测：`witch_fire`（#404）
命中断言 ⇒ 以 `⏳ [已知缺陷 #404]` 报告；`--strict` 为该票的红证。**②面**：渲染期不变量已入 `render-all`（实测 0 命中），
点击态由 #403 断言持有（门厅两支），**通用版待做**（记在 #407）。**④面**（场合）待做：`Game.NPC` 登记簿补 `venue/role` ＋ 走查清单。
