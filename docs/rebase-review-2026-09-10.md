# 上游同步与变基复核（2026-09-10）

本次按用户要求重新拉取 `sagitrs/sgstory` 的 `main`，先合入 `9f5a379`；收到再次拉取的要求后，继续变基到 `d95c83a0c48d34a0c8553b75349b11fcfc5e9dfc`（#174）。原文案、布局及反馈定位三条提交均已重放。变基前 `dedd396` 保留在本地备份分支 `codex/feedback-before-rebase`。

## 冲突处理

- 保留上游 #170–174 的一次治疗、花田和抄书人时代限制、回指前提、楼层校验、社交上下文和正史修正。
- 保留上游收回提前揭底说明的调整、设定集小标题样式与按实际行囊显示道具；不恢复已删除的身份解释和结局配方。
- 保留本地文案中与上游兼容的修正，以及卡片排版、手机折行、原生存档按钮、减少动画和同页新反馈定位。
- 酒馆传闻沿用上游的「不老的女人」标签；采花沿用“退到上风头，连土一起端起来”；焚塔选项统一使用“杖”。测试同步显示标签，链接覆盖白名单仅替换已有的“归还护符”出口名称，没有扩大匹配范围。
- 对照最新上游，六份剧情源文件的段落名称、行动目标及条件/状态宏逐项相同；见[边界核对](evidence/rebase-review-2026-09-10/rule-boundaries.json)。

## 新文字检查与密度基线

上游 #174 首次加入按段落计算的措辞密度基线。此前已完成的文案改写改变了段落长度，同样一个括号或破折号也可能因分母缩小而提高密度。合并时先收紧新增比喻与括号，再按上游提供的 `--craft --update-density` 重签最终文本快照；检查代码与拒绝条件保持原样。

使用同一套去注释、去宏、去链接和去标签口径，对照 `d95c83a` 检查全部 66 个内容段落：**每个段落**的破折号、“像”和左括号原始次数均未增加。总次数分别为 155 → 125、59 → 53、93 → 90。部分比例上升仅来自该段落文字缩短；其余变小的比例也写入新快照，作为后续上限。逐段数据见[密度比较](evidence/rebase-review-2026-09-10/density-comparison.json)。

## 本次验证

- 最终 `npm test` 通过，包括上游新增的文字工艺门及 31 条分支路线。
- 66 个内容段落全部渲染与交互覆盖；渲染 76 格、交互 80 格；10 个时代分叉双态齐全；链接级覆盖 76 格全部对账通过。
- 本次补齐 `origin/main` 引用，并在允许读取本地 Git 基线的环境中运行完整检查，六道覆盖门均实际执行。
- `npm run soak` 通过：一章 20 局、塔 20 局，覆盖 66 格、63 个位点双支清扫，不变量与渲染错误为 0。
- 独立 Chromium 复测覆盖 360/390/768/1440 四个宽度、36 个版式状态，沿实际点击路线进入酒馆、洞穴、守林人和过去的观星者；无横向溢出、宏错误或错误目标链接。8 次传闻定位、64 次跨页/跨时代导航、4 次键盘快存、4 次读档，以及桌面/手机交涉失败后请酒均通过。证据：[浏览器复测](evidence/rebase-review-2026-09-10/browser.json)。
- 浏览器证据记录的是 `31d7309` 加本次适配修改的工作树构建，起止文件校验值一致。所有浏览器目录隔离，退出后关闭并清理；未覆盖 Firefox、WebKit 或真实手机。
- 完整日志：[最终测试](evidence/rebase-review-2026-09-10/npm-test.log)、[随机游走](evidence/rebase-review-2026-09-10/soak.log)。

历史文本与表现测试仍保留在[文本审校记录](text-review-2026-09-10.md)和[表现复审记录](visual-review-2026-09-10.md)，不把变基前的浏览器结果当作本次新结果。
