# 既有章节文案审校（2026-09-10）

基线：`sagitrs/sgstory` 的 `main`，提交 `80d160b467cb01328584d6bf60ff01e70d8ded34`。叙事依据为 `docs/lore-canon.md` v17。

## 范围

仅审校该上游已有的角色创建、三章、结局、设定集与公共提示。不移入 fork 独有章节、素材、构建产物或本地化实现。

保留全部段落标识、链接目标、条件、状态写入、数值和规则。按钮文案改变时仅同步测试中的文本定位。另修正洞穴原有的闭宏拼写 `<</<</if>>` 为 `<</if>>`。

## 两轮审校

第一轮按章节检查叙事、人物口吻、道具提示与正史一致性，并完成一次完整 `npm test`。重点包括楼层名称、守林人寿命、采花风向、坏哨不能发声、书信收发方向、药膏恢复上限与交涉筹码说明。

第二轮交换章节负责人交叉审阅，修正现任守林人错用过去时代口吻、术式位置误导、缺少火把或阅读前提却直接作出的叙述、未点击却写成已翻转、时代共用场景及结局睡眠状态。保留 NPC 的误读、人物身份伏笔与隐藏星力规则。主审另外复核公共提示和章节之间的衔接。

## 验证

- 两轮各执行一次完整 `npm test`；最终修订通过构建、完整性检查、12 项审计、规则/属性测试、全段落渲染、冒烟与 29 条分支路线。
- 66 个内容段落全部获得渲染和交互覆盖；10 个时代分叉段落双态覆盖齐全（渲染 76 格、交互 80 格）。
- 本地覆盖率工具未取得 `origin/main` 基线，自动跳过“新增段落必配测”子门；另直接对照上游 `80d160b`，确认无新增段落，全部段落标识、链接目标及规则宏保持一致（显示标签及上述闭宏拼写修复除外）。
- Chromium 实际完成开场 → 三轮角色创建 → 酒馆检查，覆盖 1440×844 与 390×844，未出现页面运行错误。浏览器 HOME、XDG 与崩溃目录隔离。
- 手机宽度存在既有横向溢出：原上游与修改版均为 `scrollWidth=621`、`clientWidth=390`；桌面宽度均无溢出。本次不改布局。
- `npm run soak` 通过：一章 20 局、塔 20 局，覆盖 65 格，63 个位点双支清扫，不变量与渲染错误为 0。
- `git diff --check` 通过。

## 边界

本次不调整既有玩法条件。审阅中观察到真结局入口对喂花、换哨入口对持有坏哨，以及重复进入唤醒段落重置龙生命的条件问题，未在这份文案 PR 中改动；它们需要独立的规则复现、设计确认与专项测试。

自动审计与路线测试不能替代人工阅读；本次也不声称对既有规则做了完整缺陷审查。

后续用户另行授权了本地试玩表现测试与修复；上述手机溢出等表现问题的修复及新验证见[本地试玩表现复审](visual-review-2026-09-10.md)。本记录保留文本审校阶段的原始验证边界。

随后按用户要求同步并变基到上游 `d95c83a`；冲突取舍与新增检查的适配见[上游同步复核](rebase-review-2026-09-10.md)。
