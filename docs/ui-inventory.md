# UI 展示层全量清单（#185 阶段一）

> 数据源：main 全量 twee 静态扫描 ＋ #185 实测走查结论（`0ba4811`/`adf0fb4`）。
> 模板类型＝#185「建议的共用展示约定」的分类。**迁移目标**列给出的模板代号供阶段二试迁移对表用。
> 共 66 个内容段落（不含 Widgets/Tables/Script/Style 等非内容段），与 #185 盘点一致（车卡 2＋一章 8＋二章 19＋三章 19＋结局 10＋设定集 8）。

## 模板代号

| 代号 | 用途 | 现有可复用基础 |
|---|---|---|
| **T-叙事** | 单一正文＋出口 | 顶楼/塔外花田式的短段 |
| **T-场景** | 正文→交涉→已读折叠→本次回答→行动区 | **酒馆/女巫小屋三区布局（#181/#184/#186 已落地）＝迁移范本** |
| **T-战斗** | 完整回合：检定→出招→敌方回应→生命→下一轮 | `fightresolve`/`fightpanel`（三处战斗共用） |
| **T-子对话** | 话题页；返回恢复话题区与阅读位置 | 守林人·送/守/信、观星者·星/夜/图 等 |
| **T-结局** | 结局卡＋返回入口 | `<<ending>>`＋`.ending-card`（10 结局＋章节出口共用） |
| **T-资料** | 设定集/图鉴 | `.codex-hub`（8 页） |
| **T-车卡** | 角色创建 | 车卡/角色卡/开场 |

## 一章（8）

| 段落 | 模板现状 | 时代变体 | 共用件已用 | 进入行为现状 | 缺口（#185 实测/清单） | 迁移目标 |
|---|---|---|---|---|---|---|
| 开场 | T-车卡 | — | — | 一次性 | — | T-车卡 |
| 酒馆 | **T-场景**（范本） | — | socpanel＋已读折叠＋行动区 | 三区布局已落地 | 键盘继续路径（Tab 经侧栏绕行） | T-场景 |
| 森林边缘 | T-叙事 | — | sitecheck | 静态 | — | T-叙事 |
| 洞穴 | T-叙事＋交涉 | — | socpanel＋sitecheck | 静态 | — | T-场景 |
| 女巫小屋 | **T-场景**（范本） | — | 已读折叠＋行动区 | 三区布局已落地 | 同酒馆 | T-场景 |
| 林间小径 | T-叙事 | ✓flip | flip | 静态 | — | T-叙事 |
| 结局 平凡之路 | T-结局 | — | ending | 一次性 | — | T-结局 |
| 结局 银月之赐 | T-结局 | — | ending | 一次性 | — | T-结局 |

## 二章（19）

| 段落 | 模板现状 | 时代变体 | 共用件已用 | 进入行为现状 | 缺口 | 迁移目标 |
|---|---|---|---|---|---|---|
| 塔门 | T-叙事 | ✓双态正文 | flip | era 条件渲染 | — | T-叙事 |
| 塔外花田 | T-叙事 | — | sitecheck | 静态 | — | T-叙事 |
| 半途的林子 | T-叙事 | — | — | 一次性 | — | T-叙事 |
| 雾之魔物 | T-叙事 | — | — | 态分支 | — | T-叙事 |
| 雾之魔物·战 | T-战斗 | — | fightpanel | **首屏近 700px 空白**；回合反馈被空白分隔（#185 实测） | 战斗间距与回合完整展示 | **T-战斗（阶段二试迁移候选）** |
| 雾之魔物·退 | T-叙事 | — | — | 一次性 | — | T-叙事 |
| 守林人 | T-场景（无折叠） | ✓双态 | socpanel | **子对话返回重放全部介绍＋「获得钥匙」提示重现**（#185 实测） | 返回恢复话题区；give 需幂等门 | **T-场景（阶段二试迁移候选）** |
| 守林人·信/送/守/封印 | T-子对话 | — | — | 返回=重渲染主页 | 返回阅读位置 | T-子对话 |
| 门厅 | T-叙事 | ✓双态 | sitecheck | **观察成功后结果不留在画面**（#185 实测：只剩选项变化） | 结果留屏 | T-场景（阶段二试迁移候选） |
| 书房 | T-叙事 | ✓双态 | sitecheck | era 条件渲染 | 同门厅类 | T-场景 |
| 工坊 | T-叙事 | — | sitecheck＋econ | era 条件链接 | 同上 | T-场景 |
| 天文台 | T-叙事 | ✓双态 | sitecheck | era 条件渲染 | 同上 | T-场景 |
| 顶楼 | T-叙事 | — | — | keeper.key 条件链接 | — | T-叙事 |
| 结局 新任守林人/焚塔者/半途 | T-结局 | — | ending | 一次性 | — | T-结局 |

## 三章（19）

| 段落 | 模板现状 | 时代变体 | 共用件已用 | 进入行为现状 | 缺口 | 迁移目标 |
|---|---|---|---|---|---|---|
| 地下宴会厅 | T-叙事 | ✓双态 | flip | era 条件渲染＋threshold | — | T-叙事 |
| 龙·巢边 | T-叙事 | — | econ | 一次性旗标后空置 | — | T-叙事 |
| 宴会·过去 | T-场景（无折叠） | ✓ | flip | 出口殿后已过门7 | — | T-场景 |
| 老妇人 | T-子对话 | — | — | 返回=重渲染 | 同守林人类 | T-子对话 |
| 观星者 | T-场景（无折叠） | — | socpanel | 静态 | 同酒馆类 | T-场景 |
| 观星者·星/夜/图 | T-子对话 | — | — | 返回=重渲染 | 同上 | T-子对话 |
| 当时的女巫 | T-场景 | — | socpanel＋socresolve | 状态分层文案 | — | T-场景 |
| 当时的女巫·换 | T-子对话 | — | — | 返回=重渲染 | 同上 | T-子对话 |
| 寻杖 | T-叙事 | — | sitecheck | 态分支（staff_found） | — | T-叙事 |
| 老巫女 | T-子对话 | — | socpanel（unwilling） | 返回=重渲染 | 同上 | T-子对话 |
| 喂花 | T-叙事 | — | — | 一次性 | — | T-叙事 |
| 交付 | T-叙事 | ✓ | flip | 条件渲染 | — | T-叙事 |
| 唤醒 | T-叙事 | ✓双态 | — | #178 已改「未再睡去」态 | — | T-叙事 |
| 归位 | T-叙事 | ✓ | — | #203 已补喂花门槛分支 | — | T-叙事 |
| 封印·并肩 | T-战斗 | — | fightpanel | 同雾战类 | T-战斗 | T-战斗 |
| 龙·战 | T-战斗 | — | fightpanel | 同上 | T-战斗 | T-战斗 |
| 龙·再冲 | T-叙事 | — | sitecheck | 单选项决死 | — | T-叙事 |

## 结局（10）与设定集（8）、车卡（2）

| 段落 | 模板现状 | 缺口 | 迁移目标 |
|---|---|---|---|
| 结局×10（60-endings）＋章节出口×3 | `<<ending>>`＋结局卡＋ending-foot（门7 白名单） | — | T-结局（已基本达标） |
| 设定集＋7 子页 | `.codex-hub`；开关已挪页脚（#202/#209） | — | T-资料（已基本达标） |
| 车卡/角色卡 | 预设卡 | — | T-车卡 |

## 阶段五（已交付，#263）：真实浏览器验收接入

**驱动**：`test/browser.mjs`——**零依赖 CDP**（Node 22 内建 `fetch`＋`WebSocket`），直连 Chrome for Testing；不引入 puppeteer/playwright，也无需新增 devDependency。

**覆盖矩阵**：视口 360×667 / 390×844 / 1280×844 × 4 个代表场景（战斗首屏／战斗回合／门厅留屏／守林人返回）×「操作前后」——共 24 项断言、每视口 4 张截图（`build/browser-evidence/`）。

| 断言 | 阈值 | 结果 |
|---|---|---|
| 战斗首屏第一项行动在视口内 | `top < 视口高` | ✓（86–182px） |
| 战斗首屏无近整屏空白 | 首块 `top < 260` | ✓（40–48px） |
| 战斗回合反馈相邻成块 | 相邻块最大间距 `< 60px` | ✓（13–14px） |
| 门厅观察结果留屏且可见 | 结果元素 `top < 视口高` | ✓（405–478px） |
| 守林人返回不重放首遇介绍 | 文本无「我就是守林人」 | ✓ |
| 守林人返回行动区在视口内 | `top < 视口高 × 0.8` | ✓（26–161px） |
| 开场/放大文字 200% 无横向溢出 | `scrollWidth ≤ innerWidth + 1` | ✓ 全视口 |

**接入与工具准备**：`npm run browser`；已并入 `npm run soak`（缺浏览器/缺系统库时打印跳过原因并返回 0，CI 友好）。
- 系统库：`npm run browser:setup`（＝`scripts/chrome-deps.sh`，`apt-get download` ＋ `dpkg -x` **免 root** 就地解包到 `~/.cache/sgstory-chrome-deps`）
- `test/browser.mjs` 自动发现该目录（或 `SG_CHROME_LIBS` / `/tmp/chromedeps`）并把 `LD_LIBRARY_PATH` 交给浏览器子进程——使用者**不需要手工 export**；预检失败时直接给出上面的准备命令
- 浏览器本体：Chrome for Testing（默认扫 `~/.cache/puppeteer/chrome/*/chrome-linux64/chrome`，也可 `CHROME_PATH` 指定）

**未覆盖（如实登记）**：手机实机、真实触屏手势、多周目（#271）、跨周目 localStorage 粘性行为；像素级「视觉审美」判断仍以人工试玩为准（本门只判可读性/可达性/溢出）。

## 伞票验收对照（#185 验收条件 10 条，逐条核验）

| # | 验收条 | 状态 | 依据／缺口 |
|---|---|---|---|
| 1 | 段落全登记＋模板或适配理由＋关键状态覆盖 | ✅ | ui-inventory 66 段（模板现状/迁移目标两列）；render-all 83 格 era 双态＋38 路线＋walker/soak |
| 2 | 检定/消费/受伤/取物结果完整可读 | ✅ 机制层 | 通用结果留屏（保replace 与 goto 两类）＋各自显示路径；逐段落确认随批二-四走查 |
| 3 | 战斗回合连续、无近整屏空白、行动入口可达 | ✅ | 结构层（nobr＋空白归一＋行动区）＋**真浏览器实测**（回合间距 13–14px、首屏行动 86–182px 可见） |
| 4 | 长回答后一次操作回行动、多条记录不拉远 | ✅ | `sgBackToActs`＋`跳到行动`＋`autoActs`（本轮补 `.tavern-actions`） |
| 5 | 子对话返回接着原话题、无重复介绍/旧奖励 | ✅ | `firstTime`（守林人/酒馆/女巫小屋）＋`观星者·图` 去重放（本轮） |
| 6 | 一次点击只结算一次；展开/折叠/回看不改状态 | ✅ | 渲染期写均为一次性守卫（唤醒/门厅/地下/守林人/龙·战）；`观星者·图` 重复 give 本轮修；`龙·再冲` 彩蛋位重掷＝登记适配理由 |
| 7 | 鼠标键盘均可「结果→下一步」、折叠区不入 Tab | ✅ | 阶段三（跳转链接×2／折叠守卫／替换式交互焦点回收）＋render-all 三查 |
| 8 | 桌面/360-390px/放大文字可读 | ✅ | 真浏览器视口矩阵 3 档 × 200% 放大：无横向溢出、关键入口在视口内 |
| 9 | 切时代/回退/读档明确且不串反馈 | ✅ | 结果槽带 `era` 比对（本轮）；旧档安全网归一；前置蕴含 6 条＋存档回归在 tests |
| 10 | 既有测试继续通过；浏览器持续验收；报告区分覆盖 | ✅ 部分 | 自动化全绿＋浏览器门已并入 `npm run soak`；「报告区分手机实机/人工试玩」在 #264 收尾 |

## 阶段四批二（已交付，#262）：首遇门控覆盖长介绍段＋「结果不吞门」机检

**A. 首遇门控（验收条 5「不因重复初次介绍造成误解」）**——按**时代**分别留一次（`<<firstTime \`"key_" + $era\`>>`），返回只给一句短入口：
| 段落 | 门 key | 返回短句 |
|---|---|---|
| 塔门 | `tower_gate_present/past` | 「门缝里那点火光还在。」／「门里人声还热。」 |
| 宴会·过去 | `banquet_intro` | 「厅里还是那么多人。」 |
| 地下宴会厅 | `cellar_present/past` | 「那条龙还在睡。」／「宴还在……」（喂花后仍带安睡态） |
| 工坊 | `forge_present/past` | 「工坊还是老样子。」／「炉火还旺。」 |
| 天文台 | `observatory_intro` | 「穹顶还在转。」 |
| 顶楼 | `tower_top_intro` | 「守林人还站在瞭望位边上。」 |
| 观星者 | `seer_intro` | 「他没抬头。手还压在那半张图上。」 |
| 当时的女巫 | `witch_intro` | 「她还坐在长桌尽头。」 |
（守林人／酒馆／女巫小屋在阶段三已门控。）

**B. 「结果不吞门」机检（验收条 2 的机器证据，回 #265）**——`test/walker.mjs` 每步点击前后 diff 资源：
- `gold` 变化 ⇒ 屏上须有「金币」；`hp` 下降 ⇒ 须有伤害/疼痛/药膏类文本；`inv` 新增 ⇒ 须出现道具名或「获得/物品栏」提示；
- 例外：导航到结局页（死亡/结局自身叙述结果）；
- 真实状态 20+20 局 0 违规（首轮即抓出并修掉洞穴动武跨段两处——结果槽改为「下一次渲染显示一次后清空」，跨段也留屏）。

## 阶段四批三／批四（已交付，#262）：余段登记＋结局/设定集/资料页对账＋段落登记门

**登记口径**：每段给出「模板归属 ＋ 共用层用法 ＋ 适配理由（不迁移时）」。共用层统一提供：结果留屏（`sgResult`／`actOut`）、行动区与键盘跳转（`autoActs`／`scene-acts`／`tavern-actions`）、首遇门控（`firstTime`）、空白归一（`normalizeBlank`）——因此余段**多数无需逐段改源**，只需登记归属与理由。

### 批三：一章余段（3）
| 段落 | 模板 | 共用层用法 | 适配理由 |
|---|---|---|---|
| 村中井台 | T-叙事 | 结果留屏（econ/检定）＋空行归一 | 短段，无行动区需求（出口单链） |
| 废哨站 | T-叙事 | 结果留屏（`<<give>>` 哨）＋空行归一 | 同上；哨子入袋有 `<<give>>` 提示 |
| 林缘空地 | T-叙事 | 结果留屏＋空行归一 | 同上 |

### 批三：二章余段（逐段核对）
| 段落 | 模板 | 共用层用法 | 适配理由／备注 |
|---|---|---|---|
| 塔外花田 | T-场景 | `replace #fanlin-act` 原地留屏＋结果槽 | 采摘动作在 #237 已改回「失败＝土散花折」 |
| 雾之魔物 / ·退 / ·战 | T-叙事＋T-战斗 | 战斗宏 `nobr`＋回合紧凑块＋行动区 | 战斗像素级验收在 #263 |
| 寻杖 | T-场景 | 行动区（autoActs）＋结果留屏 | 失主在场（#231） |
| 书房 | T-场景 | 结果留屏（施术/搜检）＋「已搜过」状态门 | 施术失败＝结果槽留屏（#270） |
| 交付 / 唤醒 / 归位 | T-叙事 | 结果留屏＋条件分支门（`delivery_short`／`whistle_blown`／喂花门槛） | 一次性守卫复核见验收条 6 |
| 龙·巢边 | T-叙事 | 结果留屏（econ 拾取）＋`hoard_looted` 门 | — |
| 半途的林子 | T-叙事 | 空行归一 | 一次性出口段 |

### 批四：结局／资料／车卡（登记为组＋逐名）
| 组 | 段落名 | 模板 | 状态 |
|---|---|---|---|
| 结局 ×10＋章节出口 ×3 | 结局 平凡之路／结局 银月之赐／结局 新任守林人／结局 焚塔者／结局 半途／结局 送星归位／结局 再度沉睡／结局 自愿的长眠／结局 守林人击杀／结局 送入虚空／结局 劣化封印／结局 星落／结局 讨伐／结局 坠星之死／结局 死亡 | T-结局 | `<<ending>>`＋结局卡＋`ending-foot` 页脚；结果由结局自身叙述（结果不吞门豁免） |
| 设定集 ×8 | 设定集／设定集·三律／设定集·守塔的人家／设定集·道具／设定集·结局／设定集·图鉴／设定集·塔／设定集·术语 | T-资料 | `.codex-hub`；开关在页脚（#202/#209） |
| 车卡 ×2 | 车卡／角色卡 | T-车卡 | 预设卡＋`chargen-card`；一次性，无首遇门 |

**立项后新增段的登记**：宴·仪式／宴·散场（#233）、守林人·劝杀（#230）、守林人·封印／守林人·守（#204）、观星者·夜、村中井台／废哨站／林缘空地（#177/#178 批次）。另：守林人·送／守林人·信／守林人·封印（T-子对话，返回定位由 `sgBackToActs` 覆盖）。

## 阶段四批一（已交付，#262）：通用「结果留屏」＋自动行动区

**新增共用层机制（全库一次受益，不再按段落打补丁）**
- **通用结果留屏**：包一层 `<<goto>>`——导航前从 wikifier 缓冲里取本次点击新渲染出的结果文本（去掉骰面，骰面仍由 `<<lastcheckFor>>` 复显），存进 `$pc.ev.last_result`（带段落名）；重渲染时由 `:passagerender.sgResult` 把结果槽放回**行动区正上方**。goto 式流程的「检定/受伤/取物结果被跳转吞掉」自此全线解决（书房/工坊/天文台/寻杖等已被抽查验证）。
- **自动行动区**：没显式声明 `.scene-acts` 的段落，`:passageend` 自动把「末尾那串链接」包成 `.scene-acts`（≥2 条链接）→ 分组样式＋`跳到行动`跳转自动生效；不足 2 条时不包（结果槽改插在首个链接簇之前，出口仍在最后——门7 不破）。
- 结果槽位置：优先行动容器之前 → 否则首个链接簇之前；`<<actOut>>`/`<<sceneFeedback>>` widget 保留给 replace 式流程（门厅）。

**剩余批次**（按章分批）：二章余段（塔门/塔外花田/寻杖/交付/唤醒/归位/顶楼/结局×3）、三章 19 段、一章 8 段、结局＋设定集 18 段——每段迁移后基线同批一。

## 阶段三共用层（已交付，#261）

1. **键盘继续路径**：`跳到正文`（body 首元素，越过 `#ui-bar`）＋带 `.scene-acts` 段落的 `跳到行动`；替换式交互（`<<replace>>`）焦点随旧节点消失时回收给本次反馈；关闭的折叠区把焦点交还 `summary`。
2. **共用件泛化**：`<<firstTime "flag">>`（首遇门控，守林人／酒馆／女巫小屋已改用）；`hallResult`/`hallPick`（结果留屏）＋`.scene-acts`＋`sgBackToActs`（前缀规则，本就通用）；结果留屏反馈区样式并入共用层。
3. **非叙事空行**：正文/宏/模板三源——共用层 `normalizeBlank`（块容器与块元素旁清 `<br>`、连续空行封顶 2）＋ 内容 widget `<<nobr>>`（damage/sitecheck/econ/give/flip/hall*）＋ **两处 `//` 注释泄漏**（地下宴会厅／flip widget，会当正文渲染）修复。
4. **机检**：`test/render-all.mjs` 三查（连续 ≥3 `<br>`／关闭折叠区含可聚焦控件／行动区缺跳转链接）；`test/integrity.mjs` 增「JS 注释泄漏门」（twee 正文禁 `//`，允许 `//斜体//`）。

## 阶段二试迁移（已交付，#185 对表「代表场景试迁移」）

1. **探索 · 门厅 ✅**：取物/观察结果 `replace #hall-act` 原地留屏（结果在行动区正上方），配 `hallResult`/`hallPick` 单源 widget；「看准了不必用蛮力」的成功简化链保留（hall_seen）。
2. **交涉/场景 · 守林人 ✅**：首遇＝完整相认一次性（`keeper_intro` 门控：不重放介绍、不重重给钥匙）；`socresolve` 提出门外（每次渲染结算待处理交互）；话题/面板/出口收进 `.scene-acts` 行动区。
3. **战斗 · 雾之魔物·战 ✅（结构层）**：`fightbegin/fightresolve/fightpanel` 三宏 `<<nobr>>` 化——非叙事换行不再转 `<br>`（三处战斗共用，首屏空白与回合间隔的机械来源）；CSS 补 `.fight-log/.fight-acts` 紧凑块。**像素级验收（截图对照）留待「真实浏览器验收接入」阶段**。
4. **子对话返回 · 守林人·送/守/信 ✅**：`sgBackToActs`——前段名＝本段名＋「·」前缀即视为话题子页，返回落 `.scene-acts`（滚动＋焦点，只动界面状态）；规则通用，观星者·星/夜/图等后续迁移零改动受益。

每场景迁移后基线：38 路线＋render-all／walker／门7 全绿；体积基线重签（四件套增量，PR 说明）。

## 已登记、留待阶段三＋的缺陷（不在本清单 PR 修）

- ~~战斗首屏空白、回合反馈分隔~~（结构层已修：三战斗宏 nobr；像素验收待浏览器阶段）；
- ~~守林人「获得钥匙」提示重现~~（首遇门控已修）；
- ~~门厅观察成功后结果不留屏~~（replace 留屏已修）；
- ~~键盘继续路径~~（阶段三已修：`跳到正文`／`跳到行动` 两条跳转链接＋折叠守卫＋替换式交互焦点回收）；
- ~~非叙事空行~~（阶段三已修：共用层空白归一＋widget `<<nobr>>`＋两处 `//` 注释泄漏修复＋`test/integrity.mjs` 注释泄漏门＋render-all 空行/折叠/跳转三查）。

## 引擎段登记（随机制上移新增；本清单的"内容段落"口径不变）

| 段落 | 归属 | 说明 |
|---|---|---|
| `短战斗 Widget` | **引擎**（`src/engine/50-present/12-shortfight.twee`） | `#608`：S3 短战斗机制从 `stories/hollow-cave/10-cave.twee` 上移；**无玩家可见正文**（只有 widget 定义与文档注释）⇒ 不进"内容段落"计数，但它在 `ui-migration-diff` 的段落表里是新名字，按该门口径在此登记 | <!-- path-exempt: 该故事已按 #1004 删除（历史记录，不抹）-->
| `敌人回合 Widget` | **引擎**（`src/engine/50-present/12-shortfight.twee`） | `#705` 片二：**一个完整回合**的机制（玩家 5e 攻击骰 ⇒ 敌人反击 ⇒ 骰式伤害**落部位** ⇒ `slotAbsorbAt`）＋ 一行日志（`#701` 的日志面素材）；**无正文**（0 字符） ⇒ 已登记 |
| `机制·shortFight·尾` | 故事 3（结构段） | `#608`：短战斗结算后的**故事侧**动作（`<<caveNext>>`）；无正文（`payload: 信息`） |

## 现存故事段落登记（`#1004` B 段后 —— ⚠️ 本节是**登记门**要的那一半，历史清单见上文）

> ⚠️ 为什么另起一节 ✗：本文件上文（`## 一章` / `## 二章` / `## 三章` / `## 结局与设定集` / …）
> 是**已删故事**（`mist-forest`／`hollow-cave`）的 66 段历史清单 ✓ —— 它们**保留**是为了不抹掉那段走查记录 ✗；
> 而 `test/integrity.mjs` 的**段落登记门**（`#262`）判的是**默认故事**（`DEFAULT_SLUG`，现为 `minimal-demo`）的内容段落 ✓
> ⇒ 现存两故事的段落必须在册 ✓，故在此登记 ✓。
> 口径同上文：只算**内容段落**（不含 `Widgets`／`Tables`／`Script`／`Style` 等非内容段 ✓）。

### `minimal-demo`（引擎自检用 · 5 段）

| 段落 | 模板现状 | 共用件已用 | 说明 |
|---|---|---|---|
| 开场 | T-叙事 | — | 入口（`00-story.json::entry`）✓；口内可选：`往前走一步`／`看图鉴` |
| 岔路 | T-叙事 | — | 二选一演示 |
| 左路 | T-叙事 | — | 演示分支 |
| 右路 | T-叙事 | — | 演示分支 |
| 图鉴 | T-资料 | — | **本故事自带的**最小"可选面"演示（`Sg.Codex` 缺席时的那一页 ✓） |

### `face-fixture`（**面夹具** · 27 段 —— `#1004` B2b 片二 ＋ B 段后 `DEFAULT_SLUG` 指向它）

> ⚠️ 这个 slug **不是内容故事** ✗：它是**测试夹具** ✓（`00-story.json::subtitle` 已写明 ✓），
> 把"**删掉两个内容故事后仍有真消费者**"的接入面**每样接一次** ✓。
> ⚠️ **段名沿用旧故事** ✗ —— 那是因为 20+ 件消费者把段名**钉死**了（`Engine.play('森林边缘')` 这类 ✓；
> 裁定见 `#1004` ✓）⇒ **正文全部新写** ✓，**不是**"漏删的旧故事" ✗。
> 登记在此是因为**默认故事**（`DEFAULT_SLUG`）现在是它 ✓ ⇒ `test/integrity.mjs` 的段落登记门判的就是这一组 ✓。
>
> ⚠️ **非内容段**（不在下表，但 `scripts/ui-migration-diff.mjs` 逐段核 ✓）：**`Fixture Widgets`**（→ `stories/face-fixture/12-hooks.twee` 的 `:: Fixture Widgets [widget]` ✓）——
> `#1004` B2b 期间相对 `origin/main` **新增 16 行** ✓。**登记因由** ✓：旧那几行是**旧内容故事**的 widget 兼容面 ✓，
> 现在写的是夹具自己的 widget（时代翻转钩子 ＋ `era-drained` 提示 ✓）⇒ 正文**全部新写**（同本节「段名沿用旧故事、正文全部新写」口径 ✓）
> ⇒ 这 16 行是**预期的**漂移 ✓，**不是**回退或误改 ✓。

| 段落 | 作用（面） |
|---|---|
| 开场 | 入口（`00-story.json::entry`）＋ 车卡入口 `踏上旅途` |
| 车卡 | 选择方式（预设卡 ×N ＋「快速成型」） |
| 车卡·成型 | 车卡收束（`applyPreset`／`finalize`）→ 进正戏 |
| 酒馆 | 中转 hub（通向各个面） |
| 森林边缘 | 位点／检定（`<<sitecheck>>`） |
| 洞穴 | 位点入口 |
| 洞穴·战斗 | 战斗面（`pools`／`actions`／`fightresolve`） |
| 门厅 | 取物（场地状态 vs 背包） |
| 门厅·看钉 | 取物结果留屏 |
| 塔外花田 | 一次性拾取 |
| 守林人 | 交涉面（`Social.asks`／`socpanel`） |
| 女巫小屋 | 交涉／经济（`<<econ>>`／`<<price>>`） |
| 老巫女 | 跨时代合龙（`era='past'` ＋ notes） |
| 观星者 | 子对话（话题页返回） |
| 地下宴会厅 | 存档矩阵用段 |
| 书房 | 阅读面（`reads`） |
| 岔口 | 事件池／三选一（`roads`） |
| 塔门 | 波次／重置（`waves`）／游走器起点 |
| 封印·并肩 | 战斗（`Combat` 满配） |
| 图鉴 | 道具图鉴（`Sg.Codex`） |
| 设定集 | 资料页（`Codex.items` 的非谜底页） |
| 设定集·术语 | 资料页 · 术语 |
| 设定集·三律 | 资料页 · 谜底级（带 `seenFinal()` 门） |
| 设定集·结局 | 资料页 · 终局级（带 `seenFinal()` 门） |
| 结局 平凡之路 | 结局卡（`<<ending>>`） |
| 结局 送星归位 | 结局卡（`<<ending>>`） |
| 结局 死亡 | **可选面**：引擎在 `<<damage>>` 里以 `Story.has("结局 死亡")` 守卫的那一页（`#491` 判据 4） |

### `night-ferry`（P4 · 用编辑器做出 · 11 段）

| 段落 | 模板现状 | 共用件已用 | 说明 |
|---|---|---|---|
| 渡口 | T-叙事 | — | 入口（`00-story.json::entry`）✓ |
| 付钱 | T-叙事 | — | 分岔 A |
| 撑篙 | T-叙事 | — | 分岔 B |
| 船头 | T-叙事 | — | 合流 |
| 河心 | T-叙事 | — | 合流 |
| 举灯 | T-叙事 | — | 分岔 ✅→抵岸 |
| 等浪 | T-叙事 | — | 分岔 ❌→沉船 |
| 靠岸 | T-叙事 | — | 变体 |
| 翻船 | T-叙事 | — | 变体 |
| 结局 抵岸 | T-结局 | `<<ending>>` | `final` |
| 结局 沉船 | T-结局 | `<<ending>>` | `final` |

⚠️ **缺口声明** ✗（B 段的代价，照实登记 ✓）：现存两故事**都不含**车卡（`hasChargen()` 皆 `false` ✓）、
交涉面板、战斗、位点（`<<sitecheck>>`）、笔记／伏笔、经济事件、装备 ＋ 部位异常 ＋ 波次／事件池（`T-场景`／`T-战斗`／
`T-子对话`／`T-车卡` 四个模板**在当前样本里都没有对象** ✓）⇒ 上表"共用件已用"一列因此基本为空 ✓ ——
**这是收敛的代价，不是这段模板已验收** ✓。
