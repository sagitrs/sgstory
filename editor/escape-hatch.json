{
	"note": "K4 逃生舱登记表（`#762` 车道 C）：契约成员里**含任意逻辑**（分类器 C 桶）的必须在此逐条登记（理由＋票号）。清单外出现即红；已不是 C 桶的登记也红（清单只许收缩，收缩留痕）。B 桶（需声明式扩展）与 D 桶（可下沉引擎）都**不算**逃生舱。",
	"hatchFiles": [
		"stories/face-fixture/12-hooks.twee"
	],
	"hatchFilesWhy": "**手写逃生舱文件**（`#787` 翻面）：生成物只装得下 A 桶成员 ⇒ 非 A 桶（B/C）必须住**手写**文件。这些路径是「完整契约源」的一半：分类器与 K4 判逃生舱时会把它们与数据面文件一起看（否则「成员搬出手写文件」会被误读成**登记腐烂**的假红 ✗）。这些文件**不带**生成标记 ⇒ K4-④ 的逐字节比对不选它们 ✓；守它们的是本条登记 ＋ 行为等价门（与生成物**一起**对冻结基线比）。",
	"hatches": [
		{
			"member": "overBudget",
			"slug": "mist-forest",
			"reason": "它是「**数值比较 vs 声明预算**」的谓词：`(pc?.star?.spent ?? 0) > Game.Star.budget` —— 预算本身已住声明（`Game.Star.budget` ✓，故事侧只搬了判定），但**比较本身**现有条件词汇表达不了：`Sg.rules.ops` 只有 `gte/lte/oneOf`（**没有严格 `>`** ✓），而 `budget` 是**运行期字段** ⇒ 硬写成 `budget+1` 会造**第二份真源** ✗（比「多一处例外」坏得多）。当前**只有这一处**（不足「先数后立」门槛）⇒ **不立词** ⇒ 落**契约面** ✓。**何时该删**：若「值与声明阈值比较」这个概念凑到第三处 ⇒ 应扩**既有**条件词汇（一个 term／算子）并**删除本条** ✓（见 `#785` 的观察项 —— 登记不是失败，是让例外**可枚举** ✓）。",
			"ticket": "#785"
		},
		{
			"member": "socialHooks",
			"slug": "mist-forest",
			"reason": "它是**三处 per-field hooks**（`{ '<askId>': { apply: (pc)=>…, unwilling: (pc)=>… } }` ✓），引擎**逐字段先问 hook**（`apply`／`done`／`willing`／`unwilling` ✓）。**声明式不可预见** ✗：钩子体是**读任意游戏状态的闭包**（如 `!(pc.inv['完整星图'] && pc.world.family_favor && pc.inv['坏哨'])` ✓）⇒ 这不是「现有 kind 缺字段/种类」（**B** ✗），而是「上面都装不下」（**C** ✓）；且**各形状仅 1 处** ⇒ 不足「先数后立」门槛 ⇒ **不立词** ✓。**何时该删**：若引擎将来支持**声明式 per-field hook**（或该形状凑到第三处 ⇒ 扩既有词汇）⇒ **撤回本条** ✓（见 `#785` —— 登记不是失败，是让例外**可枚举** ✓）。",
			"ticket": "#785"
		},
		{
			"member": "overBudget",
			"slug": "face-fixture",
			"reason": "与 `mist-forest` 的那条**同形**（`#1004` B2b 把 `overBudget` 搬进夹具后，它是同一处例外）：`(pc?.star?.spent ?? 0) > Game.Star.budget` —— 值本身住声明面（`Game.Star.budget` ✓），但**严格 `>` 的比较**现有条件词汇表达不了（`Sg.rules.ops` 只有 `gte/lte/oneOf`），而 `budget` 是运行期字段 ⇒ 硬写 `budget+1` 会造第二份真源 ✗。⚠️ **本夹具条目与被删的 `mist-forest` 条目是同一处例外的搬家** ⇒ `mist-forest` 那条随故事删除应当**撤回**（已在本片报给发起者 ✓）。",
			"ticket": "#1004"
		}
	],
	"why_empty": "**本表只登记 C 桶**（真逃生舱：含任意逻辑、既不能用声明式 kind 表达、也不是引擎能力）。**当前有登记项（见 `hatches`）**：它记录的是**尚不该立词**的例外（处数不足「先数后立」门槛 ⇒ 落契约面），并写明**何时该删除**（概念凑够处数 ⇒ 扩既有词汇、删本条）✓。**不写数字**：B/D 的实测清单与数量由门打印（`node editor/k4.mjs` 的『欠账（B 待补 kind）／欠账（D 待下沉引擎）』两行），本文件只讲口径（手写数字必腐烂 ✗）。⇒ 不要读成「登记表短 ⇒ 没欠账」：本表只记**必须靠任意逻辑**的那一类。",
	"refusedFacesWhy": "**\"不数据化的面\"**（`#215` `18508321` 裁定 ✓）：这些面**不是不会写** ✗，而是**当下不划算／不该由本线做** ✓ —— 登记它们，是让后人看到\"**已知的欠账**\"✗，而不是\"没人在意\"✓。判据两条 ✓：① 条目必带 `file`／`why`／`ticket`／`paths`（`paths` ＝ **退路 ＋ 重开条件** ✓）；② **只许收缩** ✗ —— 登记的件若**已带 `@generated`**（＝其实已数据化 ✓）⇒ **红**（删掉该条，那条注释归它的迁移 PR ✓）。⚠️ **别拿既有两个容器装它** ✗：`hatches` 按**契约成员名**登记 C 桶 ✓（本族登记的是**文件/面** ⇒ 会当场判\"登记腐烂\" ✗）；`hatchFiles` 会被 `editor/equiv.mjs` **拼进等价门的产物侧** ✓（＝宣称\"这些手写件属于生成契约\" ✗）⇒ 那是**改判据的输入** ✗。口径 ✓：本线目标**不是\"手写面清零\"** ✗ —— 是「**可数据化的面必须数据化** ✓；真文本／逻辑面可以留 ✗，但必须**被登记、可计数**」✓（`editor/k4.mjs` 会打印本表条数 ✓）。",
	"refusedFaces": [
		{
			"file": "stories/minimal-demo/00-meta.twee",
			"why": "**同上**（与 `mist-forest/00-meta.twee`／`hollow-cave/00-meta.twee` **同类** ✓）：有效可数据化内容 ≈ 2–3 行 ✗",
			"ticket": "#985",
			"paths": "**三条路** ✓：(i) **把三段都数据化**（`StoryTitle` 散文 ＋ `StoryData` 的 ifid/format ✗ ＋ `StoryIdentity` ✓）—— 当下不走的理由：`StoryData` 是 **Twine 格式元数据** ✗（**不是故事数据** ✓：`format`／`format-version`／`zoom` 属 Twine 参数 ✓）、`StoryTitle` 是**散文** ✓（设计稿 §3「文本留文本」✓）、`StoryIdentity` 只 **1 行**且**已被 `store-keys` 门把住** ✓；(ii) **只数据化 `StoryIdentity`** ✓ —— 收益 1 行 ✓ < 新面成本（schema ＋ 发射器 ＋ 等价基线 ＋ 门 ✓）；(iii) **本阶段不动 ＋ 登记为欠账** ✓ ← **当下采用** ✓。**重开条件** ✗：当「手写面清零」成为显式目标、或 `StoryData` 需要按故事机检时 ✓"
		},
		{
			"file": "stories/night-ferry/00-meta.twee",
			"why": "**元数据面**（`night-ferry` ✓，17 行）：与 `stories/mist-forest/00-meta.twee`／`stories/hollow-cave/00-meta.twee` **同类** ✓ —— `StoryTitle`＝散文 1 行 ✓／`StoryData`＝**Twine 格式元数据**（`ifid`／`format*`／`zoom` ✗ 不是故事数据 ✓）／`StoryIdentity`＝1 行且**已被 `store-keys` 门把住** ✓ ⇒ 可数据化内容 ≈ 2–3 行 ✗",
			"ticket": "#991",
			"paths": "**三条路** ✓：(i) 三段都数据化 ✗（`StoryData` 是 Twine 参数、`StoryTitle` 是散文 ✓）；(ii) 只数据化 `StoryIdentity` ✓（收益 1 行 ✗ < 新面成本 ✓）；(iii) **本阶段不动 ＋ 登记为欠账** ✓ ← **采用** ✓。**重开条件** ✗：当「手写面清零」成为显式目标、或 `StoryData` 需按故事机检时 ✓"
		},
		{
			"file": "stories/face-fixture/00-meta.twee",
			"why": "**元数据面**（夹具 ✓）：与 `stories/minimal-demo/00-meta.twee`／`stories/night-ferry/00-meta.twee` **同类** —— `StoryTitle`＝散文 1 行／`StoryData`＝Twine 格式元数据（`ifid`／`format*`／`zoom` 不是故事数据）／`StoryIdentity`＝1 行且已被 `store-keys` 门把住 ⇒ 可数据化内容 ≈ 2–3 行。",
			"ticket": "#1004",
			"paths": "**三条路**：(i) 三段都数据化（当下不走：`StoryData` 属 Twine 参数、`StoryTitle` 属散文）；(ii) 只数据化 `StoryIdentity`（收益 1 行 < 新面成本）；(iii) **本阶段不动 ＋ 登记为欠账** ← 采用。**重开条件**：当「手写面清零」成为显式目标、或 `StoryData` 需要按故事机检时。"
		},
		{
			"file": "stories/face-fixture/10-fixture.twee",
			"why": "**车卡模块 ＋ 段落面**（夹具）：照 `mist-forest/20-chargen.twee` 的先例 —— `Chargen Data [script]`（`rounds`／`presets`／`pick`／`finalize`）是**故事侧逻辑**（apply 函数读任意状态），段落是**散文**。⇒ 可数据化的是「声明面」，逻辑与正文当下不划算。",
			"ticket": "#1004",
			"paths": "**三条路**：(i) 把 `Chargen` 数据化（`rounds`／`presets` 进 `data/` ＋ 新 kind 装 `apply` 闭包 —— 那是 `escape-hatch.json` 的 C 桶，需先立词）；(ii) 段落正文进 `data/`（设计稿 §3 明写「文本留文本」⇒ **不走**）；(iii) **本阶段不动 ＋ 登记为欠账** ← 采用。**重开条件**：当「车卡面」在第二个故事里再出现一次、或 C 桶「闭包 apply」凑到第三处时，按 `#785` 的口径扩词。"
		},
		{
			"file": "stories/face-fixture/13-codex.twee",
			"why": "**图鉴界面面**（夹具）：照被删的 `stories/mist-forest/72-codex-ui.twee` 的同一条登记 —— 它读 `Game.Codex.items`（故事数据）并做渲染与存档，是**故事侧界面面**；可数据化的是「条目/线索声明」（已住 `data/tables.json` 的 `Codex.items` ✓），界面本身当下不划算。",
			"ticket": "#1004",
			"paths": "**三条路**：(i) 界面数据化（把 `render()` 的 HTML 形状也搬进 `data/` ＋ 新 kind —— 那是 C 桶，需先立词）；(ii) 并进引擎（`Sg.Codex` 对所有故事成立吗？**不成立**：它读故事自己的 `Game.Codex.items`，`#574` 正是为把它留在故事侧才从 `80-script.twee` 搬出来的 ✓）；(iii) **本阶段不动 ＋ 登记为欠账** ← 采用。**重开条件**：当第二个故事也带图鉴面、或 C 桶「渲染闭包」凑到第三处时，按 `#785` 的口径扩词。"
		}
	],
	"proseFaces": [
		"stories/minimal-demo/10-demo.twee",
		"stories/night-ferry/10-ferry.twee"
	],
	"proseFacesWhy": "散文面（**不可数据化** ✗）：设计稿 §3「**结构进 JSON、文本留文本**」✓ ⇒ 正文留文本是**设计**，不是欠账 ✓。⚠️ 与 `refusedFaces`（**可数据化但当下不划算** ✓）**不是同一类** ✗ —— 两条判据**分开** ✓，别合并成一个豁免表 ✗。白名单**只许收缩** ✗：其中某件若**变成 `@generated`**（＝它其实已数据化）⇒ K4 判红并点名 ✓。"
}
