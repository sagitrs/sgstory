{
	"note": "引擎门里**允许出现**的故事专有字面量的白名单（`#602`）。键＝`<门文件>::<token>`，值＝**理由（必须带票号）**。本文件是**数据**（放 JSON 而不是写进门代码：那样检查器自己就会被自己的白名单命中——实测过一次）。白名单腐烂（声明了却不再命中）⇒ 门会报，逼你删。 【`#660` 片三-0】新增**成员档**白名单：键形 `<引擎文件>::Game.<表>.<成员>`（含 `this.<成员>` 形态）；每条都必须写**移除计划**——接缝做完就删，腐烂（不再命中）⇒ 门报。",
	"allow": {
		"consequences.mjs::图鉴": "桶名标签：本门的 `codex` 桶（收藏面）——与最小示例的段落名同词纯属巧合（#602）",
		"waves.mjs::图鉴": "契约描述：`RESET_KEEPS` 的\"不清什么\"清单里含收藏面（引擎侧契约，非故事数据）（#602）",
		"src/10-core.twee::Game.Combat.siteInfo": "**移除计划：片三-1（Economy 接缝）后随 Combat 片清（#660）**——`<<sitecheck>>` 词汇宏读故事的动作表取位点信息",
		"src/10-core.twee::Game.Economy.priceOf": "**移除计划：片三-1（#660）**——`<<econ>>` 走 `Game.Economy.apply`，而 `priceOf` 是**故事侧**的折扣函数（同族的 `this.priceOf` 见 21-resolve）",
		"src/10-core.twee::Game.Social.ask": "**移除计划：片三-Social（#660）**——`<<socresolve>>` 词汇宏按诉求取面板数据",
		"src/80-script.twee::Game.Social.ask": "**移除计划：片三-Social（#660）**——`Sg` 侧渲染交涉面板时读诉求",
		"src/engine/40-sim/21-resolve.twee::Game.Economy.priceOf": "**移除计划：片三-1（#660）**——`apply` 调故事的折扣函数",
		"src/engine/40-sim/21-resolve.twee::Game.Economy.this.events": "**移除计划：片三-1（#660）**——`priceOf` 里 `this.events[id]`：故事事件表被合进 `Game.Economy`（本档**形态 C**）",
		"src/engine/40-sim/21-resolve.twee::Game.Economy.this.priceOf": "**移除计划：片三-1（#660）**——`apply` 里 `this.priceOf(id, pc)`（同上）"
	}
}
